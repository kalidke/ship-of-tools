# Tag-driven release (ADR 0030 §3 + clone addendum): build platform
# artifacts, smoke-test them, publish a GitHub Release with git-cliff notes.
# The julia bundle is RETIRED — installs clone the repo at the release tag
# (the checkout is the resource tree + the manual), so there is no curated
# subset to get wrong.
#
# Trigger paths:
#   - tag push v*        → full pipeline incl. publish (use scripts/release.sh)
#   - workflow_dispatch  → builds + checks ONLY (pipeline validation without
#                          tagging; artifacts land as workflow artifacts)
#
# Matrix policy (ADR 0030): linux/windows x86_64 are blocking; macOS aarch64
# is experimental (continue-on-error) until someone dogfoods it.

name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build:
    name: build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.experimental }}
    outputs:
      # All matrix legs compute the identical string; last-writer-wins is fine.
      version: ${{ steps.ver.outputs.version }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # ubuntu-22.04, NOT -latest: the FE binary's glibc floor is the
          # build image's (2.35 = covers 22.04+). Building on 24.04 produced a
          # 2.39-floor binary that failed on the entire fleet (caught by the
          # first manual artifact smoke of v0.2.0). sotd is built MUSL STATIC
          # below, so the daemon runs on any distro (canary box is 20.04).
          - os: ubuntu-22.04
            target: linux-x86_64
            experimental: false
          - os: windows-latest
            target: windows-x86_64
            experimental: false
          - os: macos-14
            target: macos-aarch64
            experimental: true
    steps:
      - uses: actions/checkout@v7
        with:
          # build.rs stamps the clean on-tag version only when `git describe
          # --tags --exact-match` can see the tag object.
          fetch-tags: true
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ runner.os == 'Linux' && 'x86_64-unknown-linux-musl' || '' }}
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust
      - name: Resolve version
        id: ver
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            echo "version=0.0.0-ci.${GITHUB_SHA::9}" >> "$GITHUB_OUTPUT"
          fi
      - name: Test
        shell: bash
        run: cd rust && cargo test --workspace --locked
      - name: Build release binaries
        shell: bash
        run: cd rust && cargo build --release --workspace --locked
      - name: Build sotd (musl static, linux)
        if: matrix.target == 'linux-x86_64'
        shell: bash
        run: cd rust && cargo build --release -p sot-backend --target x86_64-unknown-linux-musl --locked
      - name: Package (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          v="${{ steps.ver.outputs.version }}"
          stage="sot-$v-${{ matrix.target }}"
          mkdir "$stage"
          if [ "${{ matrix.target }}" = "linux-x86_64" ]; then
            # sotd ships MUSL STATIC (runs on any distro, incl. the 20.04
            # canary); the FE keeps the runner's glibc floor (2.35).
            cp rust/target/release/sot rust/target/x86_64-unknown-linux-musl/release/sotd "$stage/"
          else
            cp rust/target/release/sot rust/target/release/sotd "$stage/"
          fi
          cp deploy/sotd.service "$stage/"
          tar -czf "$stage.tar.gz" "$stage"
      - name: Package (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $v = "${{ steps.ver.outputs.version }}"
          $stage = "sot-$v-${{ matrix.target }}"
          New-Item -ItemType Directory -Path $stage | Out-Null
          Copy-Item rust/target/release/sot.exe, rust/target/release/sotd.exe $stage/
          Compress-Archive -Path $stage -DestinationPath "$stage.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: bin-${{ matrix.target }}
          path: |
            sot-*.tar.gz
            sot-*.zip
          if-no-files-found: error

  julia-check:
    # The retired bundle job's REAL value, kept: prove the julia envs resolve
    # and load at this exact ref (this check caught the Julia-1.11 @K_str
    # break on its first run). Installs instantiate from the checkout, so
    # "resolves + loads at the tag" is the release-blocking property.
    name: julia envs resolve + load
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: "1.12"
      - name: Instantiate + load check
        run: |
          julia --project=julia/kernel -e 'using Pkg; Pkg.instantiate()'
          julia --project=julia/repl   -e 'using Pkg; Pkg.instantiate()'
          julia --project=julia/kernel -e 'using ShipToolsKernel; println("kernel ", pkgversion(ShipToolsKernel))'
          julia --project=julia/repl   -e 'using ShipToolsRepl;   println("repl ok")'

  smoke:
    # Boot the ACTUAL linux artifact and prove it (a) runs on an old-glibc
    # host — the container pins 20.04/glibc 2.31, same as the oldest fleet
    # box — (b) self-identifies with the exact tag version (catches -dev
    # mis-stamping from missing tags at checkout), and (c) answers a hello
    # handshake. v0.2.0 shipped a linux binary that failed (a) on the entire
    # fleet; this job exists so that never recurs.
    name: smoke test (linux artifact)
    needs: [build]
    runs-on: ubuntu-latest
    container: ubuntu:20.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: bin-linux-x86_64
      - name: Boot artifact + version + hello
        shell: bash
        run: |
          trap 'rc=$?; if [ "$rc" -ne 0 ]; then echo "=== sotd.log ==="; cat sotd.log 2>/dev/null || true; fi; exit "$rc"' EXIT
          apt-get update -q && apt-get install -y -q python3 ca-certificates > /dev/null
          tar xzf sot-*-linux-x86_64.tar.gz
          d=$(ls -d sot-*-linux-x86_64/)
          expected="${{ needs.build.outputs.version }}"
          got="$("$d/sotd" --version)"
          echo "sotd --version: $got (expected version: $expected)"
          # Exact version match is only meaningful on TAG runs — a dispatch
          # run names artifacts 0.0.0-ci.<sha> while the binary embeds its
          # real git-describe version; there "it runs and self-identifies"
          # is the assertion.
          case "$expected" in
            0.0.0-ci.*) case "$got" in "sotd "*) ;; *) echo "BROKEN --version OUTPUT"; exit 1;; esac ;;
            *)          case "$got" in "sotd $expected ("*) ;; *) echo "VERSION MISMATCH"; exit 1;; esac ;;
          esac
          mkdir -p scratch
          "$d/sotd" --token smoke-token --tcp 127.0.0.1:28753 --project-root ./scratch --label smoke > sotd.log 2>&1 &
          for i in $(seq 1 30); do python3 -c "import socket; socket.create_connection(('127.0.0.1',28753),1)" 2>/dev/null && break; sleep 1; done
          python3 - <<'EOF'
          import json, socket
          s = socket.create_connection(("127.0.0.1", 28753), timeout=10)
          f = {"v":1,"id":1,"kind":"req","op":"hello","payload":{"client_id":"smoke","last_seen_revision":0,"protocol":1,"app_version":"smoke","token":"smoke-token"}}
          s.sendall((json.dumps(f)+"\n").encode())
          p = json.loads(s.makefile().readline())["payload"]
          assert "error" not in p, f"hello rejected: {p}"
          assert p.get("app_version"), f"no app_version in HelloRes: {p}"
          print("hello OK — backend", p["app_version"])
          EOF

  publish:
    name: publish release
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build, smoke, julia-check]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          path: assets
          merge-multiple: true
      - name: Checksums
        run: cd assets && sha256sum * > SHA256SUMS
      - name: Release notes (git-cliff, conventional commits)
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --latest --strip header
        env:
          OUTPUT: RELEASE_NOTES.md
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          pre=""
          [[ "$GITHUB_REF_NAME" == *-* ]] && pre="--prerelease"
          gh release create "$GITHUB_REF_NAME" assets/* \
            --title "Ship of Tools $GITHUB_REF_NAME" \
            --notes-file RELEASE_NOTES.md $pre
