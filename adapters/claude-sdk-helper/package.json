{
  "name": "@ship-of-tools/claude-sdk-helper",
  "private": true,
  "version": "0.1.0",
  "description": "Ship's Log P2 adapter: a per-capsule Node process that owns one Claude Agent SDK session across turns, speaking NDJSON helper-protocol 1 over stdio.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/main.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.241"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2"
  }
}
