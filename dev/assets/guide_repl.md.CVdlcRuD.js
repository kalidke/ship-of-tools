import{_ as t,o as s,c as i,ao as a}from"./chunks/framework.ucUddl5u.js";const u=JSON.parse('{"title":"The REPL","description":"","frontmatter":{},"headers":[],"relativePath":"guide/repl.md","filePath":"guide/repl.md","lastUpdated":null}'),o={name:"guide/repl.md"};function r(n,e,d,l,h,c){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="The-REPL" tabindex="-1">The REPL <a class="header-anchor" href="#The-REPL" aria-label="Permalink to &quot;The REPL {#The-REPL}&quot;">​</a></h1><p><em>This is the deep dive: the frame schema, dispatch semantics, and figure-rendering mechanics. For the drawer&#39;s layout, keys, and how to drive it day-to-day, see <a href="/ship-of-tools/dev/guide/interface#The-REPL-Pane">REPL Pane</a>.</em></p><p>Ship of Tools keeps a <strong>persistent Julia REPL</strong> available throughout a session. It is your interactive Julia session — the same bindings, the same loaded packages — but its output is structured, and the figures it produces render inline in the native window rather than as text.</p><p>The REPL drawer toggles with <code>Ctrl+J</code>.</p><p>The drawer is Ship of Tools&#39; own input line, not Julia&#39;s REPL: <code>]</code> at the start of an empty line enters pkg mode (<code>pkg&gt;</code>, run through <code>Pkg.REPLMode</code>), Backspace on an empty pkg line leaves it, Up/Down recall earlier inputs, and Ctrl+C interrupts a running evaluation. There is no <code>?</code> help mode, no <code>;</code> shell mode and no Tab completion; use the Terminal drawer or the agent pane for shell commands.</p><p>The REPL and the kernel run the <code>julia</code> that <code>SOT_JULIA_BIN</code> names, else juliaup&#39;s default channel, else a <code>julia</code> on <code>PATH</code>; neither passes <code>--startup-file=no</code>, so Julia&#39;s own default applies and the depot&#39;s <code>config/startup.jl</code> (<code>DEPOT_PATH[1]/config/startup.jl</code>, usually <code>~/.julia/config/startup.jl</code>) loads — a <code>using Revise</code> there works.</p><h2 id="A-supervised,-separate-process" tabindex="-1">A supervised, separate process <a class="header-anchor" href="#A-supervised,-separate-process" aria-label="Permalink to &quot;A supervised, separate process {#A-supervised,-separate-process}&quot;">​</a></h2><p>The REPL is a Julia process supervised by the backend daemon, distinct from the Julia kernel that does project introspection. They are separate on purpose:</p><ul><li><p>The <strong>kernel</strong> owns dispatch tables, mode trees, indexing, and AST hashing.</p></li><li><p>The <strong>REPL</strong> owns your interactive state — variables you defined, packages you loaded.</p></li></ul><p>Because they are different processes, <strong>killing the REPL does not kill the kernel.</strong> You can tear down and restart your interactive session — to clear state or recover from a wedged computation — without losing the project view, the mode trees, or the index. (In-memory REPL bindings are not expected to survive a restart; static content on disk always does.)</p><h2 id="Dispatching-code" tabindex="-1">Dispatching code <a class="header-anchor" href="#Dispatching-code" aria-label="Permalink to &quot;Dispatching code {#Dispatching-code}&quot;">​</a></h2><p>You drive the REPL two ways, without retyping code:</p><ul><li><p><strong>Run a whole <code>.jl</code> file</strong> from Files mode. <code>Shift+R</code> <code>include</code>s the cursored file into the current persistent session, so its definitions land in your interactive state. <code>r</code> first resets the REPL into the file&#39;s own project — walking up to the nearest <code>Project.toml</code> — then runs it there.</p></li><li><p><strong>Type at the prompt</strong> in the REPL drawer. <code>Enter</code> submits the input; <code>Shift+Enter</code> inserts a newline, so you can build a multi-line entry before submitting.</p></li></ul><p>Per-line and per-block dispatch — running just the line at the cursor, or the surrounding top-level form computed by the kernel — are <strong>planned</strong>, not yet built.</p><p>A long-running evaluation does not block the dispatch loop: it runs on its own task, so you can interrupt it mid-eval. An interrupt schedules a real <code>InterruptException</code> onto the running evaluation — the same semantics as <code>Ctrl-C</code> in the stock REPL — which surfaces as an <code>error</code> frame.</p><h2 id="agents-repl" tabindex="-1">How agents use the REPL <a class="header-anchor" href="#agents-repl" aria-label="Permalink to &quot;How agents use the REPL {#agents-repl}&quot;">​</a></h2><p>Claude Code sessions run code in the same REPL you use, the session&#39;s own, through the <code>sot-fe repl</code> command that the <code>julia-repl</code> skill teaches them. That REPL starts with <code>--project</code> set to the session root&#39;s directory when it has a <code>Project.toml</code>, else the shim&#39;s environment. The skill is installed for Claude Code only (see <a href="/ship-of-tools/dev/start/install#install-footprint">What the installer changes</a>); Codex sessions have no REPL skill yet. The REPL runs one evaluation at a time; a request that arrives while another runs comes back <code>busy</code> (see <a href="/ship-of-tools/dev/concepts/sessions#session-model">Hosts, sessions, REPLs</a>).</p><table tabindex="0"><thead><tr><th style="text-align:right;">Command</th><th style="text-align:right;">What it does</th></tr></thead><tbody><tr><td style="text-align:right;"><code>sot-fe repl run &lt;ws&gt; &lt;path&gt;</code></td><td style="text-align:right;">run a <code>.jl</code> file and return its output</td></tr><tr><td style="text-align:right;"><code>sot-fe repl eval &lt;ws&gt; --code &#39;&lt;julia&gt;&#39;</code></td><td style="text-align:right;">run a chunk of code and return its output</td></tr><tr><td style="text-align:right;"><code>sot-fe repl status [&lt;ws&gt;]</code></td><td style="text-align:right;">report whether the workspace has a live REPL</td></tr><tr><td style="text-align:right;"><code>sot-fe repl interrupt &lt;ws&gt;</code></td><td style="text-align:right;">stop a running evaluation, keeping loaded packages</td></tr><tr><td style="text-align:right;"><code>sot-fe repl run &lt;ws&gt; &lt;path&gt; --fresh</code></td><td style="text-align:right;">restart the REPL into the file&#39;s project, then run it</td></tr></tbody></table><p><code>&lt;ws&gt;</code> is the workspace id, label or slug; a session finds its own id in <code>$SOT_WORKSPACE_ID</code>. The call returns the collected stdout, stderr, value and errors to the agent. Figures are written to files under <code>&lt;workspace&gt;/.sot/runs/&lt;run_id&gt;/</code> and the agent gets their paths; it can then put one in your preview pane with <code>show-result &lt;path&gt;</code>. Nothing adds <code>.sot/</code> to your project&#39;s <code>.gitignore</code> — add it yourself.</p><p>What this means for you:</p><ul><li><p><strong>Every agent run appears in your REPL drawer</strong>, labelled with whatever label the caller passed with <code>--origin</code>, or &quot;session&quot; when none is passed. There is no quiet mode.</p></li><li><p><strong>The REPL is shared.</strong> Your drawer, the session&#39;s agent and its subagents evaluate into the same <code>Main</code>. Definitions an agent makes are there when you type next.</p></li><li><p><strong>One evaluation at a time.</strong> A run that arrives while another is going is refused as <code>busy</code>, not queued.</p></li><li><p><strong>Headless.</strong> The REPL has no display; CairoMakie figures come back as files, and interactive figures use WGLMakie with <code>wglshow</code>.</p></li><li><p><strong>Revise is not loaded.</strong> Ship of Tools does not load Revise; a <code>using Revise</code> in your startup.jl still runs. Without it, re-running a script picks up edits to that script, but an edit to a package&#39;s <code>src/</code> takes effect only after it is included again.</p></li></ul><h2 id="Structured-output-frames" tabindex="-1">Structured output frames <a class="header-anchor" href="#Structured-output-frames" aria-label="Permalink to &quot;Structured output frames {#Structured-output-frames}&quot;">​</a></h2><p>The REPL&#39;s display shim emits <strong>structured, typed frames</strong> instead of a single undifferentiated text stream, so the frontend can render each kind of output correctly. Frames are newline-delimited JSON; the shape borrows IJulia&#39;s <code>display_data</code>, flattened so every field sits at the top level of the frame. The backend wraps each frame as a <code>repl.frame</code> event on the main protocol stream.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Frame kind</th><th style="text-align:right;">Carries</th></tr></thead><tbody><tr><td style="text-align:right;"><code>stdout</code></td><td style="text-align:right;"><code>{text}</code> — streamed incrementally as the evaluation prints</td></tr><tr><td style="text-align:right;"><code>stderr</code></td><td style="text-align:right;"><code>{text}</code> — streamed incrementally</td></tr><tr><td style="text-align:right;"><code>value</code></td><td style="text-align:right;"><code>{mime, text}</code> — the last expression&#39;s value rendered as text</td></tr><tr><td style="text-align:right;"><code>image</code></td><td style="text-align:right;"><code>{mime, data_base64, bytes}</code> — e.g. <code>image/png</code></td></tr><tr><td style="text-align:right;"><code>browser</code></td><td style="text-align:right;"><code>{url, open}</code> — a loopback URL for a live browser-served artifact (<code>wglshow(fig)</code>, a <code>BrowserView</code>); <code>open</code> says whether the frontend should auto-open a tab</td></tr><tr><td style="text-align:right;"><code>error</code></td><td style="text-align:right;"><code>{message, stacktrace: [{file, line, fn}, …]}</code></td></tr><tr><td style="text-align:right;"><code>done</code></td><td style="text-align:right;"><code>{eval_id, elapsed_ms}</code> — always the last frame for an evaluation</td></tr></tbody></table><p><code>stdout</code> and <code>stderr</code> stream as the bytes arrive, so you see output as it is produced rather than only at the end. The <code>done</code> frame is the completion signal, correlated by <code>eval_id</code> so the backend can clear the UI spinner for that evaluation. Errors are structured rather than raw stderr text, which lets the frontend render stacktraces with <code>file:line</code> links. Adding a new MIME (HTML, LaTeX, custom) is a frontend-side render change — the frame schema is open via the <code>mime</code> field.</p><h2 id="Figures-render-inline" tabindex="-1">Figures render inline <a class="header-anchor" href="#Figures-render-inline" aria-label="Permalink to &quot;Figures render inline {#Figures-render-inline}&quot;">​</a></h2><p>When the last expression is showable as an image — a CairoMakie <code>Figure</code>, a <code>Plots.Plot</code>, or anything that implements <code>show(io, MIME&quot;image/png&quot;(), x)</code> — the REPL emits an <code>image</code> frame, and the frontend draws it <strong>inline through the preview layer</strong>: the Ship of Tools renderer paints it into the window.</p><p>It is never reduced to a terminal graphics protocol (sixel, kitty, half-blocks). Rendering visual output natively is a core premise of the project, and it applies to REPL figures exactly as it applies to file previews. See <a href="./../design/rendering">Frontend Rendering</a>.</p><h2 id="Interactive-figures-in-the-browser" tabindex="-1">Interactive figures in the browser <a class="header-anchor" href="#Interactive-figures-in-the-browser" aria-label="Permalink to &quot;Interactive figures in the browser {#Interactive-figures-in-the-browser}&quot;">​</a></h2><p>Static plots render inline (above); an <strong>interactive</strong> figure — one you pan, zoom, or rotate — belongs in a real browser. Call <code>wglshow(fig)</code> on a WGLMakie figure:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> WGLMakie</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">wglshow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">surface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.4</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.4</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (x, y) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sqrt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                axis </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (; type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Axis3)))</span></span></code></pre></div><p><code>wglshow</code> serves the figure over Bonito on a loopback port (<code>SOT_WGL_PORT</code>, default 1241) and returns a <code>BrowserView</code> — which makes the frontend open the figure in your OS browser, no URL to copy. On a remote backend the page and its WebSocket ride the control tunnel through the daemon proxy (ADR 0035) — no dedicated forward — so pan/zoom/rotate work whether the backend is local or remote. The server lives as long as the REPL, and calling <code>wglshow</code> again replaces it.</p><p>WGLMakie and Bonito are resolved from <em>your own</em> project env at call time (<code>using WGLMakie</code> first) — Ship of Tools ships no plotting dependency of its own, so the REPL stays light until you ask for an interactive figure.</p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><a href="/ship-of-tools/dev/guide/interface#The-REPL-Pane">REPL Pane</a> — the drawer&#39;s layout, keys, and how to drive it.</li></ul>`,35)])])}const g=t(o,[["render",r]]);export{u as __pageData,g as default};
