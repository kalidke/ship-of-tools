import{_ as t,o as s,c as a,ao as i}from"./chunks/framework.ucUddl5u.js";const g=JSON.parse('{"title":"sot-comm protocol — v1","description":"","frontmatter":{},"headers":[],"relativePath":"ref/comm.md","filePath":"ref/comm.md","lastUpdated":null}'),o={name:"ref/comm.md"};function n(r,e,l,d,h,c){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="sot-comm-protocol-—-v1" tabindex="-1">sot-comm protocol — v1 <a class="header-anchor" href="#sot-comm-protocol-—-v1" aria-label="Permalink to &quot;sot-comm protocol — v1 {#sot-comm-protocol-—-v1}&quot;">​</a></h1><p>Session-to-session messaging for Ship of Tools. A fork of the <code>agent-comm</code> user skill with the single-session jail removed and a durable inbox fallback added, so sessions can address each other across capsule rows and across machines.</p><p>This file is the <strong>contract</strong>. Every client — the Claude skill today, a Codex or Gemini adapter or the in-app Ship of Tools <code>Tool</code> plugin later — implements <em>this</em>, so all clients are mutually addressable through the same registry and inboxes.</p><h2 id="Layout-runtime,-under-SOT_COMM_HOME,-default-/.sot-comm" tabindex="-1">Layout (runtime, under <code>$SOT_COMM_HOME</code>, default <code>~/.sot-comm</code>) <a class="header-anchor" href="#Layout-runtime,-under-SOT_COMM_HOME,-default-/.sot-comm" aria-label="Permalink to &quot;Layout (runtime, under \`$SOT_COMM_HOME\`, default \`~/.sot-comm\`) {#Layout-runtime,-under-SOT_COMM_HOME,-default-/.sot-comm}&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.sot-comm/</span></span>
<span class="line"><span>  bin/                     # installed scripts (the reference client)</span></span>
<span class="line"><span>  registry.json            # who is reachable + liveness  (source of truth for discovery)</span></span>
<span class="line"><span>  .registry.lock/          # mkdir-based spinlock for registry writes</span></span>
<span class="line"><span>  inbox/&lt;name&gt;.jsonl       # durable per-recipient queue (append-only)</span></span>
<span class="line"><span>  read/&lt;name&gt;.cursor       # per-recipient read cursor (ISO ts of last-shown msg)</span></span>
<span class="line"><span>  self/&lt;host&gt;__&lt;pane&gt;.txt  # this pane&#39;s chosen agent name (identity recovery)</span></span></code></pre></div><p>The registry and inboxes are <strong>data at rest</strong> — discovery and catch-up need a shared place to publish, not a live broker. In an optional shared-home deployment, one <code>~/.sot-comm</code> serves every host sharing that home. For cross-machine with no shared FS, point <code>SOT_COMM_HOME</code> at a git-synced directory; the same files then ride the existing bus. (Not auto-wired in v1 — the <code>.claude-bus</code> git loop can still cover separate filesystems.)</p><h2 id="registry.json" tabindex="-1">registry.json <a class="header-anchor" href="#registry.json" aria-label="Permalink to &quot;registry.json {#registry.json}&quot;">​</a></h2><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;protocol_version&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;agents&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;&lt;name&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;host&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:       </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;myhost&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// hostname -s; used for same-host delivery</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;workspace_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ws-…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the row this session runs in (SOT_WORKSPACE_ID); &quot;&quot; in a bare shell</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;repo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:       </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Ship of Tools&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;expertise&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:  [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;files&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rust-backend&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:     </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;idle&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// lifecycle: idle | spawning</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;joined&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:     </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-05-29T18:00:00Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;last_seen&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:  </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-05-29T18:04:00Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// heartbeat, bumped on send/poll/join</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;state&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:      </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;working&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ADE state-nav WORK state: working | idle | blocked | waiting | done</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;summary&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:    </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rebuilding backend + BE suite&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// one-line current / just-finished work (&quot;&quot; when none)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;status_at&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:  </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-06-15T19:32:13Z&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         // when state/summary were last written; nav ages a stale &quot;working&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><strong>Liveness</strong> is heartbeat-based, not pane-based: an agent is <em>live</em> if <code>now - last_seen &lt;= SOT_COMM_STALE_SECS</code> (default 600). This is what lets a session on one machine consider a session on another reachable. <code>host</code> + <code>workspace_id</code> are what a same-host live delivery types into.</p><p><strong>Work-state</strong> (<code>state</code> + <code>summary</code>, stamped by <code>status_at</code>) powers the ADE <em>state-nav</em> at-a-glance view, and is distinct from the lifecycle <code>status</code> above. It is written by <code>comm-status.sh &lt;state&gt; [&quot;summary&quot;]</code> (merge-only; self-gating — a silent no-op in any session that is not a joined comm agent). Two writers by design: the model pre-announces long work (<code>comm-status.sh working &quot;&lt;one-liner&gt;&quot;</code> when it judges a turn will run &gt;~30s), and a global <code>Stop</code> hook floors each turn-end to <code>idle</code>. The daemon joins these fields onto <code>workspace.list</code> (as <code>agent_state</code> / <code>agent_summary</code> / <code>agent_status_at</code>, keyed by the workspace&#39;s <code>agent_name</code>) so the frontend — which cannot read this registry directly — renders <code>summary</code> as the per-session glance, colored by <code>state</code> and aged off <code>status_at</code>.</p><h2 id="Message-frame-inbox-JSONL,-one-object-per-line" tabindex="-1">Message frame (inbox JSONL, one object per line) <a class="header-anchor" href="#Message-frame-inbox-JSONL,-one-object-per-line" aria-label="Permalink to &quot;Message frame (inbox JSONL, one object per line) {#Message-frame-inbox-JSONL,-one-object-per-line}&quot;">​</a></h2><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;from&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;name&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;to&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;name&gt;|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;repo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;repo&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;msg&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;text&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;ts&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-05-29T18:04:00Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>ISO-8601 UTC timestamps sort lexically — the read cursor is just the <code>ts</code> of the last message shown.</p><p><code>to</code> ranks the line for the recipient&#39;s inbox Monitor: their own name = directed, wakes the session; <code>&quot;&quot;</code> = broadcast copy (relay cc traffic or <code>comm-send --broadcast</code>), files silently for the next <code>comm-poll</code>. A line with NO <code>to</code> key is legacy (pre-stamp, before 2026-06-12) and reads as directed — which is why an unstamped <code>--broadcast</code> once woke the whole network at once.</p><h2 id="Delivery-—-two-modes,-chosen-by-reachability,-always-visible" tabindex="-1">Delivery — two modes, chosen by reachability, always visible <a class="header-anchor" href="#Delivery-—-two-modes,-chosen-by-reachability,-always-visible" aria-label="Permalink to &quot;Delivery — two modes, chosen by reachability, always visible {#Delivery-—-two-modes,-chosen-by-reachability,-always-visible}&quot;">​</a></h2><ol><li><p><strong>Durable always:</strong> every send appends the frame to <code>inbox/&lt;target&gt;.jsonl</code>.</p></li><li><p><strong>Live wake — directed sends only:</strong> a directed send to a recipient whose registry row names a workspace row on the sender&#39;s host is typed into that row through the daemon&#39;s <code>pty.input</code> (Enter appended; the send reports <code>delivered live</code> only when the daemon confirms <code>enter_sent</code>). Otherwise the recipient&#39;s own inbox Monitor (<code>/sot-session-start</code>) polls <code>inbox/&lt;name&gt;.jsonl</code> and wakes the session; a codex row is woken by <code>codex-watch.sh</code>, which injects each directed frame through the same <code>pty.input</code>. The message text is <code>[&lt;from&gt;:&lt;repo&gt;] &lt;msg&gt;</code>. <strong>Broadcasts are never typed</strong> — text+Enter is a full interrupt (it submits into the recipient&#39;s claude, costing a model turn), so broadcast copies are durable-only and surface on the next <code>comm-poll</code>, matching the Monitor&#39;s demotion rule.</p></li></ol><p><strong>Ping delivery (Claude, capsule rows, ADR 0047):</strong> a harness Monitor costs a model turn every ~30 minutes just to re-arm, so a Claude session in a capsule row wakes instead via <code>comm-wake.sh &lt;handle&gt; --deliver ping</code> — the same <code>codex-watch.sh</code> mechanism generalized, but it types ONE fixed notice line (never the message text) and lets the session read the real backlog with <code>comm-poll.sh</code> on the turn the ping wakes it. A whole batch of new directed frames still costs one wake, not one per frame (coalescing: an unread ping suppresses a further one until the recipient&#39;s poll cursor moves, capped at 10 minutes). It only types when the row&#39;s current screen shows a free prompt — typing into an open dialog or menu could answer it — so a busy screen delays the wake, never drops it. <code>comm-session-start.sh</code> starts this automatically for a capsule row; outside one, the harness Monitor is unchanged.</p><p>If live delivery isn&#39;t possible (no row for the recipient on this host, no daemon, a row that is not ready) the send reports <code>queued to inbox (…)</code> — the fallback is <strong>stated, never silent</strong>. The recipient sees it on the next <code>poll</code>.</p><p><strong>Cross-machine receive</strong> is the relay bridge <code>comm-listen.sh</code> starts: a reconnect loop (<code>comm-relay.sh bridge --name &lt;name&gt;</code>) run as a background child of the session&#39;s own process tree, pid recorded in <code>state/bridge-&lt;name&gt;.pid</code>. It lives in the session&#39;s capsule leg and dies with it; <code>--status</code>/<code>--stop</code> and <code>comm-leave.sh</code> follow the pidfile, and a start reaps any stray bridge for the handle (one without a pidfile) first, so one frame is never filed twice. A Windows frontend has no bridge — the frontend files inbound frames itself.</p><h2 id="Verbs-reference-client-bin/.sh" tabindex="-1">Verbs (reference client = <code>bin/*.sh</code>) <a class="header-anchor" href="#Verbs-reference-client-bin/.sh" aria-label="Permalink to &quot;Verbs (reference client = \`bin/*.sh\`) {#Verbs-reference-client-bin/.sh}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Verb</th><th style="text-align:right;">Script</th><th style="text-align:right;">Notes</th></tr></thead><tbody><tr><td style="text-align:right;">join</td><td style="text-align:right;"><code>comm-join.sh</code></td><td style="text-align:right;"><code>--name &lt;n&gt;</code> <code>--expertise &quot;a, b&quot;</code>; writes registry + self file</td></tr><tr><td style="text-align:right;">send</td><td style="text-align:right;"><code>comm-send.sh</code></td><td style="text-align:right;"><code>@name &quot;msg&quot;</code> or <code>--broadcast &quot;msg&quot;</code>; recipient is only the first positional <code>@arg</code>, so the message may itself begin with <code>@</code></td></tr><tr><td style="text-align:right;">poll</td><td style="text-align:right;"><code>comm-poll.sh</code></td><td style="text-align:right;">shows inbox entries newer than the read cursor</td></tr><tr><td style="text-align:right;">list</td><td style="text-align:right;"><code>comm-list.sh</code></td><td style="text-align:right;">all agents + live/stale + (me) marker</td></tr><tr><td style="text-align:right;">leave</td><td style="text-align:right;"><code>comm-leave.sh</code></td><td style="text-align:right;">removes self from registry; <code>--name &lt;handle&gt;</code> removes an orphan row (registry only — <code>comm-despawn.sh</code> is full teardown)</td></tr></tbody></table><h2 id="Naming-—-everything-derives-from-the-repo" tabindex="-1">Naming — everything derives from the repo <a class="header-anchor" href="#Naming-—-everything-derives-from-the-repo" aria-label="Permalink to &quot;Naming — everything derives from the repo {#Naming-—-everything-derives-from-the-repo}&quot;">​</a></h2><p>One rule, enforced where possible: <strong>names come from the repo, never from the task</strong>. A task-named anything is unfindable next to its repo-named siblings (a spawn labeled <code>edge-classify</code> hid the MyPackage agent, 2026-06-12).</p><table tabindex="0"><thead><tr><th style="text-align:right;">Thing</th><th style="text-align:right;">Convention</th><th style="text-align:right;">Example</th></tr></thead><tbody><tr><td style="text-align:right;">Durable BE peer handle</td><td style="text-align:right;"><code>&lt;repo-lowercase&gt;-&lt;host&gt;</code></td><td style="text-align:right;"><code>myrepo-myhost</code> (Ship of Tools on the backend host), <code>lldevtools-myhost</code></td></tr><tr><td style="text-align:right;">Spawned agent — repo checkout</td><td style="text-align:right;"><code>&lt;repo-lowercase&gt;</code> (bare, <strong>no</strong> descriptor)</td><td style="text-align:right;"><code>myrepo</code></td></tr><tr><td style="text-align:right;">Spawned agent — git <strong>worktree</strong></td><td style="text-align:right;"><code>&lt;repo&gt;-wt-&lt;shortname&gt;</code> (the <code>-wt-</code> infix is reserved for worktrees and groups them next to the parent; <code>&lt;shortname&gt;</code> names the WORKTREE, never the task). Created via the <code>/worktree</code> skill.</td><td style="text-align:right;"><code>MyAnalysis-wt-rotation</code> (worktree <code>rotation</code>)</td></tr><tr><td style="text-align:right;">Frontend address</td><td style="text-align:right;"><code>fe@&lt;host&gt;</code> — the frontend PROCESS&#39;s declared hello <code>name</code>, the target <code>sot-fe --fe &lt;host&gt;</code> scopes a directed <code>fe.command</code>/<code>open-url</code> to (two frontends on one box differ by <code>instance</code>); the frontend is a client, never a comm peer, and no session derives or joins as this name</td><td style="text-align:right;"><code>fe@laptop</code></td></tr><tr><td style="text-align:right;">Workspace label</td><td style="text-align:right;">repo basename (comm-spawn default; task-named labels are <strong>rejected</strong>)</td><td style="text-align:right;"><code>MyPackage</code></td></tr><tr><td style="text-align:right;">Workspace slug (the row&#39;s name)</td><td style="text-align:right;">derived from the label by the daemon</td><td style="text-align:right;"><code>mypackage</code></td></tr><tr><td style="text-align:right;">Second workspace on one repo</td><td style="text-align:right;"><code>&lt;Repo&gt;-&lt;suffix&gt;</code> label, deliberately</td><td style="text-align:right;"><code>MyPackage-2</code></td></tr></tbody></table><p>A bare <code>&lt;repo-lowercase&gt;</code> handle is the default for a normal repo checkout. For a <strong>git worktree</strong>, use <code>&lt;repo&gt;-wt-&lt;shortname&gt;</code> — the <code>-wt-</code> infix is reserved for worktrees (so they read as worktrees at a glance and the shared <code>&lt;repo&gt;-</code> prefix groups them next to the parent in the sessions list), and <code>&lt;shortname&gt;</code> names the worktree, never the task. Don&#39;t hand-roll it: the <strong><code>/worktree</code></strong> skill (<code>comm-worktree-new.sh</code>) creates the worktree at <code>&lt;repo-parent&gt;/worktrees/&lt;repo&gt;-wt-&lt;shortname&gt;</code> on branch <code>wt/&lt;shortname&gt;</code> and spawns the session with that handle+label, so the slug groups it correctly. (No host in the worktree handle — the parent is found by repo family, not host.) A deliberate second workspace is <code>&lt;repo&gt;-2</code>. Never a suffix on a plain repo open, and never a task name (<code>repo-fix</code> for a direct checkout was wrong on both counts: a task-ish descriptor AND a shortened base; the right handle was <code>myrepo</code>).</p><p>Task identity lives in <code>--task</code> / <code>--expertise</code> / the message body. Never reuse a handle that has a registry row, even a stale-looking one — the owner may be alive with a lagging heartbeat, and a collision makes two sessions execute the same briefs in parallel.</p><h2 id="Conventions-carried-over-from-agent-comm-keep-these" tabindex="-1">Conventions carried over from agent-comm (keep these) <a class="header-anchor" href="#Conventions-carried-over-from-agent-comm-keep-these" aria-label="Permalink to &quot;Conventions carried over from agent-comm (keep these) {#Conventions-carried-over-from-agent-comm-keep-these}&quot;">​</a></h2><ul><li><p><strong>Domain-expertise rule:</strong> state your goal or the problem; suggest a fix if you have one, but don&#39;t dictate the other session&#39;s implementation.</p></li><li><p><strong>Anti-groupthink tags:</strong> <code>[design]</code> <code>[question]</code> <code>[breaks]</code> <code>[challenge]</code> <code>[consensus]</code>. No <code>[consensus]</code> without a prior <code>[breaks]</code> in the thread.</p></li></ul><h2 id="Versioning" tabindex="-1">Versioning <a class="header-anchor" href="#Versioning" aria-label="Permalink to &quot;Versioning {#Versioning}&quot;">​</a></h2><p><code>protocol_version</code> is stamped into the registry on creation and checked on <code>join</code>. A mismatch warns loudly (no quiet degradation) and means a machine needs <code>ShipTools.update_comm()</code>. Bump this integer on any breaking change to the schema, frame, or delivery semantics.</p>`,30)])])}const k=t(o,[["render",n]]);export{g as __pageData,k as default};
