import{_ as t,o as a,c as r,ao as i}from"./chunks/framework.ucUddl5u.js";const p=JSON.parse('{"title":"Frontend Rendering","description":"","frontmatter":{},"headers":[],"relativePath":"design/rendering.md","filePath":"design/rendering.md","lastUpdated":null}'),o={name:"design/rendering.md"};function n(d,e,s,l,h,c){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="Frontend-Rendering" tabindex="-1">Frontend Rendering <a class="header-anchor" href="#Frontend-Rendering" aria-label="Permalink to &quot;Frontend Rendering {#Frontend-Rendering}&quot;">​</a></h1><p>Rendering everything ourselves is the project&#39;s reason to exist. The user&#39;s primary deployment is Windows local → SSH → tmux on a Linux remote, and no terminal graphics protocol delivers acceptable rich previews through that stack: tmux strips DCS by default, passthrough is fragile, and the universally-supported fallback (halfblocks) is too low-fidelity for figures, math, and rendered docs. If a terminal path <em>did</em> work, the user would be running yazi-in-tmux. Designing around terminal graphics would invert the premise.</p><p>For where rendering sits in the whole system see <a href="./../guide/architecture">Architecture at a Glance</a>; for what each preview kind looks like in use see <a href="./../guide/previews">Previews</a>.</p><h2 id="A-native-window,-pixels-we-own" tabindex="-1">A native window, pixels we own <a class="header-anchor" href="#A-native-window,-pixels-we-own" aria-label="Permalink to &quot;A native window, pixels we own {#A-native-window,-pixels-we-own}&quot;">​</a></h2><p>The frontend is a <strong>native Rust window</strong> (<code>winit</code>) with a GPU-backed canvas (<code>wgpu</code>). It writes no escape sequences and is not hosted in a terminal. Image fidelity is bounded by what the host GPU can display, not by what the terminal stack happens to forward. A PNG round-trips as: kernel emits bytes → backend forwards over the <a href="./protocol">line protocol</a> → frontend decodes via the <code>image</code> crate → uploads as a wgpu texture → composites into the preview rectangle.</p><h2 id="Two-layers,-one-surface" tabindex="-1">Two layers, one surface <a class="header-anchor" href="#Two-layers,-one-surface" aria-label="Permalink to &quot;Two layers, one surface {#Two-layers,-one-surface}&quot;">​</a></h2><p>ratatui is <strong>chrome only</strong>. Its <code>Backend</code> trait emits a stream of cells — <code>(char, fg, bg, mods)</code> — which fits text chrome and fits images, math, and markdown not at all. Rather than smuggle rich content through cells (sentinel chars, &quot;pretend this rect is a PNG&quot;), Ship of Tools runs two layers that share one wgpu surface, one event loop, and one layout pass but render through distinct APIs:</p><p><strong>Layer 1 — ratatui chrome.</strong> The mode-tree layout, status bar, modals, borders, labels, focus state, keymaps, scroll indicators, the cursor. Painted by a custom <code>Backend</code> impl that maps cells to glyphs in the wgpu canvas with a fixed-width font (cosmic-text) — explicitly <em>not</em> crossterm.</p><p><strong>Layer 2 — the preview surface.</strong> Takes a <code>Rect</code> from ratatui&#39;s layout and a <code>PreviewPayload</code> from the kernel and draws directly into the wgpu surface inside that rect, bypassing the cell stream. It dispatches on the payload&#39;s <code>mime</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">MIME</th><th style="text-align:right;">Renderer</th></tr></thead><tbody><tr><td style="text-align:right;"><code>text/plain</code></td><td style="text-align:right;">cosmic-text line layout, optional <code>syntect</code> highlighting</td></tr><tr><td style="text-align:right;"><code>text/markdown</code></td><td style="text-align:right;"><code>comrak</code> AST → flowed text + inline images + math blocks</td></tr><tr><td style="text-align:right;"><code>image/png</code>, <code>image/jpeg</code>, <code>image/webp</code></td><td style="text-align:right;"><code>image</code> decode → wgpu texture</td></tr><tr><td style="text-align:right;"><code>image/svg+xml</code> (incl. MathJax math)</td><td style="text-align:right;"><code>resvg</code> rasterize → wgpu texture</td></tr><tr><td style="text-align:right;"><code>application/pdf</code></td><td style="text-align:right;">page rasterize → wgpu texture</td></tr><tr><td style="text-align:right;"><code>video/*</code></td><td style="text-align:right;">frame stream → texture-per-frame</td></tr></tbody></table><p>The two layers compose by Z-order — chrome draws on top of previews where they overlap, so a focus border lands after the preview content fills its rect. Input goes to ratatui first; events that hit a region a preview widget registered for (scrolling a markdown view) are re-dispatched to that preview&#39;s handler. cosmic-text is the single source of truth for text shaping and measurement across both layers, which is what keeps cell width and box-drawing alignment consistent.</p><p>A consequence worth stating: plugins emit <code>PreviewPayload(mime, bytes, extras)</code> and never see ratatui or the wgpu surface. Adding a payload type is registering a renderer (and possibly a new MIME) in the preview dispatch — for most additions, no frontend changes beyond that.</p><h2 id="Inline-math-—-the-MathJax-sidecar" tabindex="-1">Inline math — the MathJax sidecar <a class="header-anchor" href="#Inline-math-—-the-MathJax-sidecar" aria-label="Permalink to &quot;Inline math — the MathJax sidecar {#Inline-math-—-the-MathJax-sidecar}&quot;">​</a></h2><p>Inline math is rendered <strong>server-side to MathJax SVG</strong> and shipped to the frontend as <code>image/svg+xml</code>, which the preview layer rasterizes through <code>resvg</code>. MathJax is the choice because it emits SVG; KaTeX outputs HTML, which there is no path for here. The sidecar is a small Node process (one per session) on the backend host — where Node is already common — with vendored MathJax shipped in the package. The frontend has no Node dependency. Full LaTeX <em>documents</em> (as opposed to inline math) render via <code>tectonic</code> to PDF, page-rasterized; that dependency only loads when a plugin emits document-level LaTeX.</p><h2 id="The-stack" tabindex="-1">The stack <a class="header-anchor" href="#The-stack" aria-label="Permalink to &quot;The stack {#The-stack}&quot;">​</a></h2><p>The libraries that implement both layers:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Concern</th><th style="text-align:right;">Choice</th></tr></thead><tbody><tr><td style="text-align:right;">Window / event loop</td><td style="text-align:right;"><code>winit</code></td></tr><tr><td style="text-align:right;">GPU surface</td><td style="text-align:right;"><code>wgpu</code> (Vulkan / Metal / DX12 / GLES-via-ANGLE)</td></tr><tr><td style="text-align:right;">Text shaping / layout</td><td style="text-align:right;"><code>cosmic-text</code></td></tr><tr><td style="text-align:right;">Vector / 2D drawing</td><td style="text-align:right;"><code>vello</code> (<code>tiny-skia</code> CPU fallback)</td></tr><tr><td style="text-align:right;">Raster decode</td><td style="text-align:right;"><code>image</code></td></tr><tr><td style="text-align:right;">SVG raster</td><td style="text-align:right;"><code>resvg</code></td></tr><tr><td style="text-align:right;">Inline math</td><td style="text-align:right;">MathJax SVG via a Node sidecar</td></tr><tr><td style="text-align:right;">Full LaTeX docs</td><td style="text-align:right;"><code>tectonic</code> → PDF → page raster</td></tr><tr><td style="text-align:right;">Markdown parse</td><td style="text-align:right;"><code>comrak</code> (CommonMark + GFM)</td></tr><tr><td style="text-align:right;">Syntax highlighting</td><td style="text-align:right;"><code>syntect</code></td></tr><tr><td style="text-align:right;">Chrome model</td><td style="text-align:right;"><code>ratatui</code> (custom <code>Backend</code>)</td></tr></tbody></table><p>Constraints driving these: cross-platform single binary, GPU-accelerated text and images, mature enough for a dev tool, room to grow into video and 3D plots without rewriting the surface. The dependency graph is large but boring — nothing exotic, all maintained. macOS and Linux work essentially out of the box on the wgpu side; older Windows GPUs vary, with GLES-via-ANGLE as the safety net and <code>tiny-skia</code> (CPU rasterization) as the correctness fallback before anything more drastic.</p><h2 id="What-is-deliberately-rejected" tabindex="-1">What is deliberately rejected <a class="header-anchor" href="#What-is-deliberately-rejected" aria-label="Permalink to &quot;What is deliberately rejected {#What-is-deliberately-rejected}&quot;">​</a></h2><ul><li><p><strong>Sixel, Kitty graphics, iTerm2, halfblocks — none, anywhere.</strong> <code>ratatui-image</code> and <code>crossterm</code> are removed from the frontend; the old <code>img-probe</code> scratch binary is deleted.</p></li><li><p><strong>No browser/webview</strong> in the chosen path. A Tauri/wry web frontend is kept warm only as the option-B fallback — taken only if preview requirements broaden into arbitrary HTML / iframes / WebGL. Across that swap the kernel, backend, line protocol, and core IR are unchanged; only the frontend surface differs.</p></li></ul>',20)])])}const m=t(o,[["render",n]]);export{p as __pageData,m as default};
