var documenterSearchIndex = {"docs":
[{"category":"section","location":"start/quickstart/#Quickstart","page":"Quickstart","text":"The shortest path from nothing to a first useful session. For the full picture behind any step here — requirements, roles, the from-source path, per-machine onboarding, reconnect internals — see Install Details and First Session Tour.","title":"Quickstart"},{"category":"section","location":"start/quickstart/#1.-Install","page":"Quickstart","text":"The recommended path is the one the product is built on — an agent. Start a Claude Code (or other) coding-agent session on the target machine and say:\n\nInstall Ship of Tools: fetch https://raw.githubusercontent.com/kalidke/ship-of-tools/main/docs/INSTALL-AGENT.md and follow it.\n\nIt preflights the machine, asks you where things should run, drives the installer, and verifies the result. Prefer to do it yourself? On Linux, one command:\n\ncurl -fsSL https://raw.githubusercontent.com/kalidke/ship-of-tools/main/scripts/install.sh | bash -s -- --local\n\n--local runs frontend and backend on this one box — the fastest way to a working install. (Splitting frontend and backend across machines, Windows, and building from source are all covered in Install Details.)","title":"1. Install"},{"category":"section","location":"start/quickstart/#2.-Launch","page":"Quickstart","text":"The installer creates a sot-launch wrapper and a desktop entry. Run either one. It starts the backend daemon if it isn't already running, opens the frontend window, and connects them.","title":"2. Launch"},{"category":"section","location":"start/quickstart/#3.-Attach-/-connect","page":"Quickstart","text":"The frontend and backend talk over a socket. A --local install runs both on one box and connects to the backend's per-user socket through the generated sot-launch wrapper — no SSH involved. Remote (--backend) installs forward a local TCP port to that remote per-user socket over SSH, so the remote endpoint is still owned by the selected Unix account. Either way, the default host and config live at ~/.config/sot/hosts.toml, and the frontend connects automatically on launch. If the connection ever drops (laptop sleep, network blip), press F5 to reconnect without losing session state.","title":"3. Attach / connect"},{"category":"section","location":"start/quickstart/#4.-Open-a-project","page":"Quickstart","text":"Press s for Sessions mode, then press Enter on the [+ create new] row to open the directory picker. Choose a directory, then press Enter to start a comm-aware agent session there (or Shift+Enter for a bare shell/REPL with no agent). That directory is now your active project.","title":"4. Open a project"},{"category":"section","location":"start/quickstart/#5.-Do-something-useful","page":"Quickstart","text":"A handful of keys get you moving immediately:\n\nKey What it does\nf Files mode — browse the project, preview renders as you move\nm Modules mode — structural view of the code (modules → functions → methods)\nCtrl+J toggle the REPL drawer — a persistent Julia session\nCtrl+T toggle the Terminal drawer — a local shell\nCtrl+Arrow move focus between the four panes\n? the in-app help overlay — the live, authoritative keymap\n\nThat's enough to look around, run code, and get help from inside the app. For the rest of the panes, drawers, and modes, walk through First Session Tour.","title":"5. Do something useful"},{"category":"section","location":"start/quickstart/#6.-Updating","page":"Quickstart","text":"Re-run the install command from step 1 — it is also the updater (binaries, repo checkout, and Julia environments move together). The app will also notify you when a new release exists and stage the binaries itself; details in Updating.","title":"6. Updating"},{"category":"section","location":"guide/repl/#The-REPL","page":"The REPL","text":"This is the deep dive: the frame schema, dispatch semantics, and figure-rendering mechanics. For the drawer's layout, keys, and how to drive it day-to-day, see REPL Pane.\n\nShip of Tools keeps a persistent Julia REPL available throughout a session. It is your interactive Julia session — the same bindings, the same loaded packages — but its output is structured, and the figures it produces render inline in the native window rather than as text.\n\nThe REPL drawer toggles with Ctrl+J.","title":"The REPL"},{"category":"section","location":"guide/repl/#A-supervised,-separate-process","page":"The REPL","text":"The REPL is a Julia process supervised by the backend daemon, distinct from the Julia kernel that does project introspection. They are separate on purpose:\n\nThe kernel owns dispatch tables, mode trees, indexing, and AST hashing.\nThe REPL owns your interactive state — variables you defined, packages you loaded.\n\nBecause they are different processes, killing the REPL does not kill the kernel. You can tear down and restart your interactive session — to clear state or recover from a wedged computation — without losing the project view, the mode trees, or the index. (In-memory REPL bindings are not expected to survive a restart; static content on disk always does.)","title":"A supervised, separate process"},{"category":"section","location":"guide/repl/#Dispatching-code","page":"The REPL","text":"You drive the REPL two ways, without retyping code:\n\nRun a whole .jl file from Files mode. R includes the cursored file into the current persistent session, so its definitions land in your interactive state. r first resets the REPL into the file's own project — walking up to the nearest Project.toml — then runs it there.\nType at the prompt in the REPL drawer. Enter submits the input; Shift+Enter inserts a newline, so you can build a multi-line entry before submitting.\n\nPer-line and per-block dispatch — running just the line at the cursor, or the surrounding top-level form computed by the kernel — are planned, not yet built.\n\nA long-running evaluation does not block the dispatch loop: it runs on its own task, so you can interrupt it mid-eval. An interrupt schedules a real InterruptException onto the running evaluation — the same semantics as Ctrl-C in the stock REPL — which surfaces as an error frame.","title":"Dispatching code"},{"category":"section","location":"guide/repl/#Structured-output-frames","page":"The REPL","text":"The REPL's display shim emits structured, typed frames instead of a single undifferentiated text stream, so the frontend can render each kind of output correctly. Frames are length-prefixed JSON; the shape borrows IJulia's display_data, flattened so every field sits at the top level of the frame. The backend wraps each frame as a repl.frame event on the main protocol stream.\n\nFrame kind Carries\nstdout {text} — streamed incrementally as the evaluation prints\nstderr {text} — streamed incrementally\nvalue {mime, text} — the last expression's value rendered as text\nimage {mime, data_base64, bytes} — e.g. image/png\nerror {message, stacktrace: [{file, line, fn}, …]}\ndone {eval_id, elapsed_ms} — always the last frame for an evaluation\n\nstdout and stderr stream as the bytes arrive, so you see output as it is produced rather than only at the end. The done frame is the completion signal, correlated by eval_id so the backend can clear the UI spinner for that evaluation. Errors are structured rather than raw stderr text, which lets the frontend render stacktraces with file:line links. Adding a new MIME (HTML, LaTeX, custom) is a frontend-side render change — the frame schema is open via the mime field.","title":"Structured output frames"},{"category":"section","location":"guide/repl/#Figures-render-inline","page":"The REPL","text":"When the last expression is showable as an image — a CairoMakie Figure, a Plots.Plot, or anything that implements show(io, MIME\"image/png\"(), x) — the REPL emits an image frame, and the frontend draws it inline through the preview layer: the Ship of Tools renderer paints it into the window.\n\nIt is never reduced to a terminal graphics protocol (sixel, kitty, half-blocks). Rendering visual output natively is a core premise of the project, and it applies to REPL figures exactly as it applies to file previews. See Frontend Rendering.","title":"Figures render inline"},{"category":"section","location":"guide/repl/#See-also","page":"The REPL","text":"REPL Pane — the drawer's layout, keys, and how to drive it.\nThe Orchestrator — the LLM can dispatch code to this same REPL via a tool.","title":"See also"},{"category":"section","location":"start/running/#Running-and-Relaunch","page":"Running & Relaunch","text":"This page covers starting Ship of Tools, the Terminal drawer that hosts your dev session, reconnecting after a drop, and the self-relaunch loop that lets the frontend rebuild and restart itself.","title":"Running & Relaunch"},{"category":"section","location":"start/running/#Launching","page":"Running & Relaunch","text":"Start the app through the launcher created by Per-Machine Setup (a desktop shortcut on Windows, or scripts/launch-sot.ps1 directly). The launcher is the supervisor: it owns the SSH tunnel to the backend host, starts the frontend, and watches for relaunch requests.\n\nThe default launch connects to the remote backend over an SSH local-forwarded socket — the canonical \"Windows local · Linux remote-in-tmux\" workflow. The backend is started once on the remote and survives across launches, so a second launch is fast; the SSH forward is fresh each time and torn down when the frontend exits. Pass -Local to fall back to a backend spawned on the same machine over a named pipe (offline / debugging).\n\nWhich remote you connect to comes from the persisted host choice (Hosts mode, hotkey h) resolved against .sot/hosts.toml; environment overrides (SOT_HOST, SOT_REMOTE_REPO, SOT_TCP_PORT, SOT_REMOTE_SOCKET) win over both. See Per-Machine Setup.","title":"Launching"},{"category":"section","location":"start/running/#Windows-Taskbar-Launch-Looks-Dead","page":"Running & Relaunch","text":"The Windows shortcut runs the launcher hidden, so an early failure can look like \"the taskbar click did nothing.\" Check these first:\n\n%LOCALAPPDATA%\\sot\\logs\\launch-status.txt — last launcher phase or fatal status.\n%LOCALAPPDATA%\\sot\\logs\\supervisor.log — detailed launcher, rebuild, tunnel, and frontend respawn log.\n%APPDATA%\\Microsoft\\Internet Explorer\\Quick Launch\\User Pinned\\TaskBar — the pinned .lnk; stale pins can still point at an old bare sot.exe or an old checkout.\n\nAfter writing or changing .sot\\hosts.toml, rerun:\n\npwsh -File scripts\\install-shortcut.ps1\n\nThat refreshes the desktop shortcut and repoints any existing Ship of Tools taskbar pin to scripts\\launch-sot.ps1. If .sot\\hosts.toml is missing, the shortcut can still be created, but launch will fail with \"no backend host configured.\"","title":"Windows Taskbar Launch Looks Dead"},{"category":"section","location":"start/running/#The-Terminal-drawer","page":"Running & Relaunch","text":"The frontend hosts a local OS shell in a bottom drawer, toggled with Ctrl+T. This is a local shell on the frontend machine — its canonical use is SSHing outward to backend hosts — and it works even when the backend is unreachable; it is not proxied through the daemon.\n\nThe drawer is a single slot shared with the REPL (Ctrl+J): each key toggles its own pane, and pressing the other key swaps the content.\n\nKey Drawer closed Showing this pane Showing the other pane\nCtrl+J → REPL → closed → REPL\nCtrl+T → Terminal → closed → Terminal\n\nWhen Ship of Tools is developed on itself, the dev claude session runs inside this Terminal drawer.","title":"The Terminal drawer"},{"category":"section","location":"start/running/#Reconnecting","page":"Running & Relaunch","text":"The backend is a long-lived daemon; the connection can drop (laptop wake, wifi flap, SSH timeout) without losing session state. Press F5 (transport.reconnect) to reconnect. Every connect carries the session id and the last revision the client saw, so the daemon replays missed events from a bounded ring or sends a fresh snapshot — reconnect feels like reattaching a tmux session. The supervisor keeps the SSH tunnel alive across these reconnects; only a real quit tears it down.","title":"Reconnecting"},{"category":"section","location":"start/running/#Browser-Backed-Previews","page":"Running & Relaunch","text":"W opens HTML/static sites through backend HTTP ports that must be forwarded to the frontend machine: 1236 for normal static pages, plus 1237-1240 for sites with root-relative assets. Video and Pluto use the same pattern on 1235 and 1234.\n\nIf you manually reconnect with only the backend control tunnel, the frontend can still talk to sotd but the local browser will show 127.0.0.1 refused to connect for W/video/Pluto. Open an aux tunnel as well:\n\nssh -N \\\n  -L 1234:127.0.0.1:1234 \\\n  -L 1235:127.0.0.1:1235 \\\n  -L 1236:127.0.0.1:1236 \\\n  -L 1237:127.0.0.1:1237 \\\n  -L 1238:127.0.0.1:1238 \\\n  -L 1239:127.0.0.1:1239 \\\n  -L 1240:127.0.0.1:1240 \\\n  <backend-ssh-alias>","title":"Browser-Backed Previews"},{"category":"section","location":"start/running/#Existing-tmux-sessions-are-missing","page":"Running & Relaunch","text":"By default sotd uses a private per-user tmux socket for workspace panes. If you already have long-lived sot-be-* sessions on tmux's default server, the frontend may show workspace rows but attach to fresh empty panes after a backend restart. Point the backend at the existing tmux server for that migration:\n\nexport SOT_TMUX_SOCK=\"${TMUX%%,*}\"   # from inside the old tmux server\nsotd tmux-socket-path\n\nFor a user systemd service, add the same environment variable to the service environment and restart sotd. The backend still checks the socket parent directory before spawning tmux; use this only for a tmux socket owned by the same Unix account.","title":"Existing tmux sessions are missing"},{"category":"section","location":"start/running/#Self-relaunch:-rebuild-without-dropping-your-session","page":"Running & Relaunch","text":"Ship of Tools can rebuild and restart its own frontend — so you can edit the frontend, recompile, and relaunch into the new binary without leaving the app. The moving parts:\n\nStaged-copy supervisor. The launcher copies the built sot into a staging directory (%LOCALAPPDATA%\\sot\\bin\\) and runs the app from that copy inside a respawn loop. Because the running file is the staged copy, cargo build --release can overwrite rust/target/release/ freely — no running-exe file lock — and you see build output live.\nExit-75 sentinel. The frontend requests a relaunch by exiting with code 75; any other code is a real quit. A background watcher polls for a relaunch-request sentinel file; on seeing it, the frontend exits 75 and the supervisor re-stages the (freshly built) binary and respawns with --relaunched.\nclaude --continue resume. On --relaunched, the frontend opens straight into the Terminal drawer and runs the configured [terminal] resume_command as the shell's first command. The default resumes the dev session without prompts:\n[terminal]\nresume_command = \"claude --dangerously-skip-permissions --continue /sot-fe-session-start\"\nSession continuity is decoupled from process survival: the terminal session does not need to live through the restart because claude --continue resumes it from its own store.\n\nThe one-command driver is scripts/relaunch-sot.ps1: it runs cargo build --release and drops the relaunch sentinel only on a green build — a failed build leaves the running app untouched.","title":"Self-relaunch: rebuild without dropping your session"},{"category":"section","location":"start/running/#Do-not-kill-the-frontend-to-restart-it","page":"Running & Relaunch","text":"warning: Never kill the frontend process\nWhen Ship of Tools is being developed on itself, the dev claude runs inside the frontend's Terminal drawer. Killing the frontend process therefore kills your own session along with it. To restart, always use the relaunch loop — scripts/relaunch-sot.ps1 (build → sentinel → exit-75 → re-stage → respawn), never a process kill.\n\nNote that changes to the supervisor script itself (launch-sot.ps1) are not picked up by the exit-75 in-place loop — those require a full restart of the launcher. The exit-75 path only re-stages the frontend binary.","title":"Do not kill the frontend to restart it"},{"category":"section","location":"start/running/#Next-steps","page":"Running & Relaunch","text":"A Guided Tour — walk a first session, mode by mode and drawer by drawer.","title":"Next steps"},{"category":"section","location":"guide/previews/#Previews","page":"Previews","text":"When you cursor a file in any mode, the preview pane renders it at the fidelity appropriate to its type. Every preview goes through one dispatch path: a FileType plugin claims the path with matches and returns a PreviewPayload from preview. The payload's mime tells the frontend which renderer to use; the bytes are opaque to Rust. Adding a format is a Julia-only change — see The Dispatch ABI and Writing a FileType Plugin.\n\n(Image: PDF preview) A paged PDF preview.\n\n(Image: HDF5 preview via the external plugin) An HDF5 file rendered by the demo external plugin — a preview added with zero Rust changes.","title":"Previews"},{"category":"section","location":"guide/previews/#Built-in-file-types","page":"Previews","text":"These plugins ship under julia/plugins/ and the kernel loads them at startup. Each is a built-in that travels the exact same dispatch path a third-party plugin would.\n\nExtension Plugin MIME Representation Fidelity\n.jl ShipToolsJuliaSource application/vnd.sot.tokens+json token spans (kind + text) from JuliaSyntax.jl syntax-highlighted source, no client-side re-tokenizing\n.md, .markdown ShipToolsMarkdown text/markdown raw UTF-8 bytes rendered markdown (comrak + cosmic-text), including inline math\n.toml ShipToolsTomlDoc text/x-toml raw UTF-8 bytes code/text renderer\n.json ShipToolsJsonDoc application/json raw UTF-8 bytes plain text today (pretty-print / colour planned)\n.txt ShipToolsPlainText text/plain raw UTF-8 bytes plain text renderer\n.pdf ShipToolsPDFFile image/png (+ page, page_count extras) one poppler-rasterized page paged; n/p turn pages, rasterized backend-side to fit the pane\n.mp4, .webm, .mov, .mkv, .m4v ShipToolsVideoFile image/png a single ffmpeg poster frame still poster in the pane; o opens playback in the browser\n\nPNG (and other raster images) render directly through the frontend's image quad path — the same path the PDF and video plugins reuse by returning image/png.\n\nA few notes on the table:\n\nJulia source is tokenized on the kernel side into {spans: [{text, kind}]}, so the frontend colours it without parsing Julia. The token kinds map to the frontend's colour set (keyword, comment, string, number, op, punct, type, ident, text); concatenating every span reproduces the file byte-for-byte.\nPDF is the one format whose preview takes a page parameter — the only built-in that addresses which part of a file to render. Pages rasterize on the host where the file lives (poppler's pdftoppm / pdfinfo), matching the backend-side-decode model.\nVideo deliberately does not play in the pane. A native player (HTML5 <video> with hardware decode) beats streaming decoded frames over a socket, so the pane shows a poster and o pops the real file out to the browser.\n\nWhen an external tool is missing (ffmpeg for video, poppler for PDF), the plugin returns a text/markdown note explaining the gap — never a silent blank pane.","title":"Built-in file types"},{"category":"section","location":"guide/previews/#Opens-in-the-browser,-not-the-pane","page":"Previews","text":"Some formats are interactive HTML and belong in a real browser rather than a static pane. Consistent with the video policy, these are popped out to the OS browser over a forwarded loopback port rather than rendered in-pane:\n\nHTML (sample.html) — opened in the browser.\nPluto notebooks (pluto_demo.jl, pluto_demo2.jl) — opened as live Pluto sessions in the browser. As .jl files they still get a syntax-highlighted source preview in the pane.\nQuarto documents (quarto_julia.qmd) — the rich rendered form opens in the browser; the source previews as text in-pane.\n\nThese all follow the same \"rich/interactive content lives in the browser\" policy.","title":"Opens in the browser, not the pane"},{"category":"section","location":"guide/previews/#Capturing-a-preview-region-for-the-LLM","page":"Previews","text":"For raster image previews you can zoom in, then press c to crop the visible region and hand it to the orchestrator LLM in the pane — \"what's the artifact here?\". The crop is taken from the source image on the backend and delivered to the in-pane agent, so the LLM sees exactly the region you are looking at.","title":"Capturing a preview region for the LLM"},{"category":"section","location":"guide/previews/#Formats-added-by-external-plugins","page":"Previews","text":"File types do not have to be built-in. The HDF5 worked example ships as a separate package and claims .h5 (see the sample.h5 fixture), demonstrating that the full preview surface is reachable from outside core with no changes to the kernel or the frontend. This is the same path every built-in above travels — the point of shipping core as a plugin to itself.","title":"Formats added by external plugins"},{"category":"section","location":"guide/previews/#Adding-a-new-file-type","page":"Previews","text":"A FileType plugin is small: a subtype, a matches predicate, and a preview that returns a PreviewPayload with the right MIME.\n\nmodule PngPreview\n\nusing ConceptExplorerCore\n\nstruct PngFile <: FileType end\n\nConceptExplorerCore.matches(::Type{PngFile}, path) =\n    endswith(lowercase(path), \".png\")\n\nConceptExplorerCore.preview(::Type{PngFile}, path) =\n    PreviewPayload(\"image/png\", read(path))\n\nend\n\nusing PngPreview and PNGs are previewable — no registration step and no Rust change. For the full contract and discovery rules see The Dispatch ABI and Writing a FileType Plugin.","title":"Adding a new file type"},{"category":"section","location":"contributing/#Contributing","page":"Contributing","text":"Ship of Tools has a small number of working conventions and one firm rule about where decisions are recorded. This page covers both, plus how to build and test.\n\nBefore adding a feature, read Requirements — it defines scope. This page and the design pages define structure.","title":"Contributing"},{"category":"section","location":"contributing/#Working-conventions","page":"Contributing","text":"Julia is the canonical language for plugin code, the ABI, and any Julia-aware logic. Use it expressively — lean on multiple dispatch and the type system. See The Dispatch ABI.\nRust is for plumbing — the TUI, IPC, file watching, process supervision. Keep it boring and predictable.\nPlotting is CairoMakie when generating plots in Julia.\nEat dogfood. Core handlers ship as plugins to themselves; the core modes and standard file types are methods on the same abstract types a third-party plugin extends. If core seems to want privileged access, the rule is to fix the ABI, not to special-case core. This keeps the extension surface honest.\nBoundaries are serialization seams. TreeNode and PreviewPayload carry opaque, kernel-defined payloads. Rust never learns about new entity kinds — adding a FileType (and, by design, a Mode) requires zero Rust changes. See Line Protocol.\nReactive over eager for staleness, refresh, and indexing. Visible drift (e.g., a stale-annotation badge) is a feature, not a bug.\nDefer until forced. If a feature can wait for a later phase, it should.","title":"Working conventions"},{"category":"section","location":"contributing/#The-decision-process","page":"Contributing","text":"Cross-cutting decisions get documented — never only in a code comment or a commit message. Some decisions are explicitly deferred (multi-agent coordination, remote transport variants, MCP as the internal protocol, an embedded editor, a user-preferences mechanism, automatic plot capture); when you reach for one of these, check whether it is still deferred before building it.\n\nPlan amendments go through a pull request. The phase plan is a committed document — argue changes in the diff, not in side channels. See the Roadmap for the milestone structure.","title":"The decision process"},{"category":"section","location":"contributing/#Build-and-test","page":"Contributing","text":"The repository is a Julia umbrella package with a Rust workspace inside it.\n\nJulia — run the umbrella suite and the core package suite:\n\nusing Pkg\nPkg.test()                          # umbrella package\nPkg.test(\"ConceptExplorerCore\")     # core, from its own environment\n\nRust — the frontend, backend, and protocol crates live under rust/:\n\ncargo test --manifest-path rust/Cargo.toml","title":"Build and test"},{"category":"section","location":"contributing/#See-also","page":"Contributing","text":"Requirements — the source of truth for scope.\nRoadmap — phase plan and milestones.","title":"See also"},{"category":"section","location":"design/rendering/#Frontend-Rendering","page":"Frontend Rendering","text":"Rendering everything ourselves is the project's reason to exist. The user's primary deployment is Windows local → SSH → tmux on a Linux remote, and no terminal graphics protocol delivers acceptable rich previews through that stack: tmux strips DCS by default, passthrough is fragile, and the universally-supported fallback (halfblocks) is too low-fidelity for figures, math, and rendered docs. If a terminal path did work, the user would be running yazi-in-tmux. Designing around terminal graphics would invert the premise.\n\nFor where rendering sits in the whole system see Architecture at a Glance; for what each preview kind looks like in use see Previews.","title":"Frontend Rendering"},{"category":"section","location":"design/rendering/#A-native-window,-pixels-we-own","page":"Frontend Rendering","text":"The frontend is a native Rust window (winit) with a GPU-backed canvas (wgpu). It writes no escape sequences and is not hosted in a terminal. Image fidelity is bounded by what the host GPU can display, not by what the terminal stack happens to forward. A PNG round-trips as: kernel emits bytes → backend forwards over the line protocol → frontend decodes via the image crate → uploads as a wgpu texture → composites into the preview rectangle.","title":"A native window, pixels we own"},{"category":"section","location":"design/rendering/#Two-layers,-one-surface","page":"Frontend Rendering","text":"ratatui is chrome only. Its Backend trait emits a stream of cells — (char, fg, bg, mods) — which fits text chrome and fits images, math, and markdown not at all. Rather than smuggle rich content through cells (sentinel chars, \"pretend this rect is a PNG\"), Ship of Tools runs two layers that share one wgpu surface, one event loop, and one layout pass but render through distinct APIs:\n\nLayer 1 — ratatui chrome. The mode-tree layout, status bar, modals, borders, labels, focus state, keymaps, scroll indicators, the cursor. Painted by a custom Backend impl that maps cells to glyphs in the wgpu canvas with a fixed-width font (cosmic-text) — explicitly not crossterm.\n\nLayer 2 — the preview surface. Takes a Rect from ratatui's layout and a PreviewPayload from the kernel and draws directly into the wgpu surface inside that rect, bypassing the cell stream. It dispatches on the payload's mime:\n\nMIME Renderer\ntext/plain cosmic-text line layout, optional syntect highlighting\ntext/markdown comrak AST → flowed text + inline images + math blocks\nimage/png, image/jpeg, image/webp image decode → wgpu texture\nimage/svg+xml (incl. MathJax math) resvg rasterize → wgpu texture\napplication/pdf page rasterize → wgpu texture\nvideo/* frame stream → texture-per-frame\n\nThe two layers compose by Z-order — chrome draws on top of previews where they overlap, so a focus border lands after the preview content fills its rect. Input goes to ratatui first; events that hit a region a preview widget registered for (scrolling a markdown view) are re-dispatched to that preview's handler. cosmic-text is the single source of truth for text shaping and measurement across both layers, which is what keeps cell width and box-drawing alignment consistent.\n\nA consequence worth stating: plugins emit PreviewPayload(mime, bytes, extras) and never see ratatui or the wgpu surface. Adding a payload type is registering a renderer (and possibly a new MIME) in the preview dispatch — for most additions, no frontend changes beyond that.","title":"Two layers, one surface"},{"category":"section","location":"design/rendering/#Inline-math-—-the-MathJax-sidecar","page":"Frontend Rendering","text":"Inline math is rendered server-side to MathJax SVG and shipped to the frontend as image/svg+xml, which the preview layer rasterizes through resvg. MathJax is the choice because it emits SVG; KaTeX outputs HTML, which there is no path for here. The sidecar is a small Node process (one per session) on the backend host — where Node is already common — with vendored MathJax shipped in the package. The frontend has no Node dependency. Full LaTeX documents (as opposed to inline math) render via tectonic to PDF, page-rasterized; that dependency only loads when a plugin emits document-level LaTeX.","title":"Inline math — the MathJax sidecar"},{"category":"section","location":"design/rendering/#The-stack","page":"Frontend Rendering","text":"The libraries that implement both layers:\n\nConcern Choice\nWindow / event loop winit\nGPU surface wgpu (Vulkan / Metal / DX12 / GLES-via-ANGLE)\nText shaping / layout cosmic-text\nVector / 2D drawing vello (tiny-skia CPU fallback)\nRaster decode image\nSVG raster resvg\nInline math MathJax SVG via a Node sidecar\nFull LaTeX docs tectonic → PDF → page raster\nMarkdown parse comrak (CommonMark + GFM)\nSyntax highlighting syntect\nChrome model ratatui (custom Backend)\n\nConstraints driving these: cross-platform single binary, GPU-accelerated text and images, mature enough for a dev tool, room to grow into video and 3D plots without rewriting the surface. The dependency graph is large but boring — nothing exotic, all maintained. macOS and Linux work essentially out of the box on the wgpu side; older Windows GPUs vary, with GLES-via-ANGLE as the safety net and tiny-skia (CPU rasterization) as the correctness fallback before anything more drastic.","title":"The stack"},{"category":"section","location":"design/rendering/#What-is-deliberately-rejected","page":"Frontend Rendering","text":"Sixel, Kitty graphics, iTerm2, halfblocks — none, anywhere. ratatui-image and crossterm are removed from the frontend; the old img-probe scratch binary is deleted.\nNo browser/webview in the chosen path. A Tauri/wry web frontend is kept warm only as the option-B fallback — taken only if preview requirements broaden into arbitrary HTML / iframes / WebGL. Across that swap the kernel, backend, line protocol, and core IR are unchanged; only the frontend surface differs.","title":"What is deliberately rejected"},{"category":"section","location":"ref/api-kernel/#API-—-Kernel","page":"API — Kernel","text":"The Julia kernel (ShipToolsKernel) is the plugin host process. It owns the dispatch tables, computes mode trees, indexes the project, hashes ASTs, and produces Julia-aware previews — exposing all of it to the backend daemon over an NDJSON line protocol.\n\nnote: Reference under construction\nPer-symbol API docs for ShipToolsKernel are not yet wired into this site (the kernel module is not currently a dependency of the docs build). For now:The wire protocol it speaks: Line Protocol.\nThe ABI it dispatches on: API — ConceptExplorerCore and The Dispatch ABI.Source: julia/kernel/src/ShipToolsKernel.jl.","title":"API — Kernel"},{"category":"section","location":"ref/api-shiptools/#API-—-ShipTools","page":"API — ShipTools","text":"The ShipTools umbrella package. In phase 1 its public Julia surface is the installer for the sot-comm session-to-session messaging system; the explorer itself runs as the Rust frontend and backend plus the Julia kernel and plugins, which are documented under Design & Internals and Extending Ship of Tools.\n\nModules = [ShipTools]","title":"API — ShipTools"},{"category":"method","location":"ref/api-shiptools/#ShipTools._add_comm_hook!-Tuple{AbstractString, AbstractString}","page":"API — ShipTools","text":"_add_comm_hook!(event, script)\n\nIdempotently add a Claude Code hook for event (\"UserPromptSubmit\", \"Notification\", \"Stop\", …) running ~/.sot-comm/bin/<script> to the user's ~/.claude/settings.json, preserving all existing config. Uses jq so the merge is structural, not a clobbering rewrite. Falls back to printing the exact JSON to add by hand when jq is missing or the file can't be parsed — never overwrites a file it could not safely read.\n\n\n\n\n\n","title":"ShipTools._add_comm_hook!"},{"category":"method","location":"ref/api-shiptools/#ShipTools._install_claude_hooks-Tuple{AbstractString}","page":"API — ShipTools","text":"_install_claude_hooks(srchooks)\n\nInstall the comm hook script(s) from srchooks into $SOT_COMM_HOME/bin (next to the comm-*.sh scripts they shell out to), then idempotently register the three work-state hooks in ~/.claude/settings.json.\n\nThe work-state hooks make state event-driven — instant, automatic, and free of model cooperation: UserPromptSubmit → working, Notification → blocked, Stop → idle. They replace the pane-scraping heuristic, which could not be instant (a poll), was fooled by an agent's own output, and could never tell \"blocked on the user\" from \"idle\".\n\nEach settings.json edit is a non-destructive jq merge (via [_add_comm_hook!]): it adds one entry for that event only if absent and preserves every other hook (repo-boundary-guard, tmux-send-guard, … are untouched). If settings.json is missing or unparseable, or jq is unavailable, it is left alone and the exact JSON to add by hand is printed. No-op if srchooks is absent.\n\n\n\n\n\n","title":"ShipTools._install_claude_hooks"},{"category":"method","location":"ref/api-shiptools/#ShipTools._install_launchers-Tuple{AbstractString}","page":"API — ShipTools","text":"_install_launchers(srcbin)\n\nInstall bare-command launcher scripts (e.g. ccbe) from srcbin into ~/.local/bin, making each executable. No-op if srcbin is absent. ~/.local/bin is the conventional user PATH dir; a warning fires if it is not on PATH so bare commands won't resolve.\n\n\n\n\n\n","title":"ShipTools._install_launchers"},{"category":"method","location":"ref/api-shiptools/#ShipTools._remove_stale_comm_hooks!-Tuple{}","page":"API — ShipTools","text":"_remove_stale_comm_hooks!()\n\nStrip every comm hook (any ~/.sot-comm/bin/comm-status-*.sh command) from ~/.claude/settings.json, across all events, dropping events left empty. Run before re-adding the current set ([_install_claude_hooks]) so settings ends up matching _COMM_STATE_HOOKS exactly — retiring wirings we no longer use (notably the old Notification→blocked that lit agents red on plain idle). Every non-comm hook is preserved. No-op if jq is missing or settings.json is absent/unparseable.\n\n\n\n\n\n","title":"ShipTools._remove_stale_comm_hooks!"},{"category":"method","location":"ref/api-shiptools/#ShipTools.comm_home-Tuple{}","page":"API — ShipTools","text":"Resolved runtime home for sot-comm (honors $SOT_COMM_HOME).\n\n\n\n\n\n","title":"ShipTools.comm_home"},{"category":"method","location":"ref/api-shiptools/#ShipTools.install_comm-Tuple{}","page":"API — ShipTools","text":"install_comm(; clis = [:claude])\n\nInstall sot-comm. Copies the core scripts to $SOT_COMM_HOME/bin (default ~/.sot-comm/bin) and installs the adapter for each CLI in clis (:claude skills/hooks, :codex skills/hooks/plugin). Idempotent — safe to re-run to update an existing install.\n\n\n\n\n\n","title":"ShipTools.install_comm"},{"category":"method","location":"ref/api-shiptools/#ShipTools.update_comm-Tuple{}","page":"API — ShipTools","text":"update_comm(; clis = [:claude, :codex])\n\nRe-sync an existing install from the repo source. Alias of [install_comm] (install is idempotent); run after git pull on each machine to close version skew.\n\n\n\n\n\n","title":"ShipTools.update_comm"},{"category":"section","location":"ref/config/#Configuration-Files","page":"Configuration Files","text":"The frontend reads three TOML files from .sot/, each with its own layered discovery: settings.toml (layout + terminal), hosts.toml (the host registry), and keybindings.toml (chords). Keybindings have their own page — Keybindings — so this page covers settings.toml and hosts.toml.\n\nAll three share the same single-responsibility, layered-discovery pattern: a project file in .sot/, overridable by an env var and a per-user file, with built-in defaults underneath. Missing or out-of-range values fall back to the default rather than crashing the chrome.","title":"Configuration Files"},{"category":"section","location":"ref/config/#settings.toml","page":"Configuration Files","text":"Frontend layout and terminal settings.","title":"settings.toml"},{"category":"section","location":"ref/config/#Discovery-order","page":"Configuration Files","text":"$SOT_SETTINGS — explicit path override.\n<repo-root>/.sot/settings.toml — the project's settings.\n$HOME/.config/sot/settings.toml — per-user settings.\nBuilt-in defaults.\n\nAny value that is missing or out of range silently falls back to its default — the chrome never crashes on a malformed settings file.","title":"Discovery order"},{"category":"section","location":"ref/config/#[layout]","page":"Configuration Files","text":"Layout is preset-based, keyed by the primary monitor's aspect ratio — there is no in-session reflow. The top-level [layout] table selects the active preset; three sub-tables define the presets.\n\nKey Type Values Default Meaning\npreset string auto · ultrawide · laptop · portrait auto Which preset to use. auto resolves by the primary monitor's aspect ratio at startup (> 1.9 → ultrawide, 1.5–1.9 → laptop, < 1.5 → portrait); the other three lock to that preset regardless of aspect.","title":"[layout]"},{"category":"section","location":"ref/config/#[layout.ultrawide]-/-[layout.laptop]-/-[layout.portrait]","page":"Configuration Files","text":"One sub-table per aspect bucket, each defining its columns, their widths, and the shared bottom drawer.\n\nKey Type Default (ultrawide) Meaning\ncolumns comma-list of slot names (nav · preview · llm · repl) nav,preview,llm Named column slots, left to right.\nwidths comma-list of fractions 0.167,0.333,0.5 Fractional column widths; same length as columns, renormalised to sum to 1.0 on parse.\ndrawer slot name or none repl Slot rendered in the shared bottom drawer when toggled open.\ndrawer_height fraction [0.10, 0.80] 0.35 Drawer height as a fraction of window height when open; clamped to range.\n\nLaptop defaults to 0.18,0.32,0.50 widths with a 0.40 drawer; portrait drops the llm column (nav,preview at 0.30,0.70, 0.40 drawer). Unknown keys and out-of-range values warn and fall back to the default.","title":"[layout.ultrawide] / [layout.laptop] / [layout.portrait]"},{"category":"section","location":"ref/config/#[terminal]","page":"Configuration Files","text":"Key Type Default Meaning\nresume_command string claude --dangerously-skip-permissions --continue /sot-fe-session-start Command auto-run in the Terminal drawer when the supervisor respawns the frontend after a self-relaunch (--relaunched). It resumes the session without permission prompts.\n\nThe resume_command is spelled out in full (not a personal shell shortcut) so it is portable to any machine with claude on PATH. The trailing positional /sot-fe-session-start is submitted as the resumed session's first interactive turn, which re-arms the fast-comm inbox monitor and catches the relaunch deaf-window gap — a resumed --continue is reactive and cannot self-arm a monitor, so the frontend bootstraps it via this prompt. Iterate on the bootstrap steps in that skill, not in this command.","title":"[terminal]"},{"category":"section","location":"ref/config/#Example","page":"Configuration Files","text":"[layout]\npreset = \"auto\"   # auto | ultrawide | laptop | portrait\n\n[layout.ultrawide]              # primary monitor aspect > 1.9\ncolumns       = \"nav,preview,llm\"\nwidths        = \"0.167,0.333,0.5\"\ndrawer        = \"repl\"\ndrawer_height = \"0.35\"\n\n[terminal]\nresume_command = \"claude --dangerously-skip-permissions --continue /sot-fe-session-start\"","title":"Example"},{"category":"section","location":"ref/config/#hosts.toml","page":"Configuration Files","text":"The host registry the in-app Hosts mode (hotkey h) lists and the PowerShell launcher consumes.\n\nThe format is deliberately simple — a section per host, scalar key = value lines — so the PowerShell launcher can parse it with a regex without pulling in a TOML library. Values pass through verbatim: there is no TOML escape processing, so Windows pipe paths use single backslashes.","title":"hosts.toml"},{"category":"section","location":"ref/config/#Discovery-order-2","page":"Configuration Files","text":"$SOT_HOSTS — explicit path override.\n<repo-root>/.sot/hosts.toml — the project's host registry.\n$XDG_CONFIG_HOME/sot/hosts.toml or %APPDATA%\\sot\\hosts.toml — per-user registry.\n\nThe launcher reads the single fixed path <repo>/.sot/hosts.toml (the PowerShell side does not layer).","title":"Discovery order"},{"category":"section","location":"ref/config/#Top-level","page":"Configuration Files","text":"Key Type Default Meaning\ndefault_host string (none) Host used when no last_host has been picked yet. The launcher resolves the target as: env vars (SOT_HOST etc.) → persisted last_host → default_host → error (\"no backend host configured\") if none of those resolve.","title":"Top-level"},{"category":"section","location":"ref/config/#[host.name]","page":"Configuration Files","text":"One section per host. The frontend's Hosts mode lists every [host.<name>] section; picking one writes last_host to state-<hostname>.toml, and the next launch resolves that name back to its entry here.\n\nKey Type Meaning\nssh_alias string SSH alias for the remote host (an entry in your ~/.ssh/config).\nremote_repo string Absolute path to the project repo on the remote host.\ntcp_port integer Local TCP port for the SSH-forwarded backend connection. The remote side should terminate at the per-user backend socket.\nremote_socket string Optional remote Unix socket path for the backend control channel. If omitted, launchers query sotd session-socket-path sot on the remote host.\nremote_home string Absolute home directory on the remote host.\nsocket string Local-host form — a named-pipe / socket path instead of SSH (no remote). On Windows this uses single backslashes, e.g. \\\\.\\pipe\\sot-local, because values are not escape-processed.\n\nA remote host sets ssh_alias / remote_repo / tcp_port (and usually remote_home); remote_socket is optional and normally discovered. A local backend on the same machine sets socket instead.","title":"[host.<name>]"},{"category":"section","location":"ref/config/#Backend-tmux-socket","page":"Configuration Files","text":"sotd normally puts workspace tmux sessions on its private per-user tmux socket. For a one-time migration to existing sot-be-* sessions on another same-user tmux server, set SOT_TMUX_SOCK in the backend environment. sotd tmux-socket-path prints the effective path, including this override.","title":"Backend tmux socket"},{"category":"section","location":"ref/config/#[monitor]","page":"Configuration Files","text":"The hosts sampled for the Ctrl+M server-monitor drawer. Each line is <display-name> = \"<ssh-alias>\".\n\nForm Meaning\n<name> = \"<ssh-alias>\" Sample this host in the monitor drawer. The host whose name (or alias) matches this machine's hostname is sampled locally (no SSH); the rest are sampled over ssh <alias>.\n\nnvidia-smi and /proc are world-readable, so no sudo or special privileges are needed on any monitored host. Remove a line to stop monitoring that host.","title":"[monitor]"},{"category":"section","location":"ref/config/#Example-2","page":"Configuration Files","text":"default_host = \"myserver\"\n\n[host.myserver]\nssh_alias = \"myserver\"\nremote_repo = \"/home/me/ship-of-tools\"\ntcp_port = 18743\n# remote_socket = \"/run/user/<uid>/sot/sessions/sot.sock\"\nremote_home = \"/home/me\"\n\n# A local backend on the same machine (no SSH):\n# [host.local]\n# socket = \"\\\\.\\pipe\\sot-local\"\n\n[monitor]\nmyserver = \"myserver\"\nhost-b = \"host-b\"\nhost-c = \"host-c\"","title":"Example"},{"category":"section","location":"ref/config/#See-also","page":"Configuration Files","text":"Keybindings — the third .sot/ file, chords and grammar.","title":"See also"},{"category":"section","location":"start/tour/#A-Guided-Tour","page":"First Session Tour","text":"This is a first-session walkthrough: launch the app, move through the modes and drawers, and learn the handful of keys that drive everything. Ship of Tools is keyboard-only by design — no mouse needed. The keys below are the defaults from .sot/keybindings.toml; every one is rebindable (see Keybindings).\n\nIf you have not started Ship of Tools yet, do Per-Machine Setup and read Running & Relaunch first.","title":"A Guided Tour"},{"category":"section","location":"start/tour/#The-window","page":"First Session Tour","text":"The window is four panes plus a shared bottom drawer:\n\n┌────────────────────────┬────────────────────────┐\n│ navigation (mode tree) │ preview (file fidelity) │\n├────────────────────────┼────────────────────────┤\n│ orchestrator / pty     │ REPL                    │\n└────────────────────────┴────────────────────────┘\n\nThe top-left pane is the mode tree — a collapsible outline whose root you switch with a hotkey. The top-right pane is the preview, rendering whatever you have selected at full fidelity. The bottom row holds the orchestrator / terminal and the REPL.","title":"The window"},{"category":"section","location":"start/tour/#Launch","page":"First Session Tour","text":"Start the app through your launcher (see Running & Relaunch). The frontend comes up, connects to the backend, and shows the first mode.","title":"Launch"},{"category":"section","location":"start/tour/#Files-mode-—-f","page":"First Session Tour","text":"Press f for Files mode: filesystem navigation over the project. Move with the arrow keys — ↑ / ↓ to move within a column, → to descend into a directory, ← to go back up. As you move, the preview pane renders the selected file at the right fidelity: markdown rendered, PNGs shown, .jl files syntax-highlighted, and PDFs and video too. Nothing is reduced to plain text.\n\nFor what each file type renders and how, see Previews.","title":"Files mode — f"},{"category":"section","location":"start/tour/#Modules-mode-—-m","page":"First Session Tour","text":"Press m for Modules mode: a structural view of the code, derived mechanically from JuliaSyntax.jl — modules, then their functions, then methods. It is read-only in phase 1. Cursor position is preserved per mode, so switching f ↔ m returns you to where you were in each.\n\nThe full set of modes and their column shapes is in Modes.","title":"Modules mode — m"},{"category":"section","location":"start/tour/#The-REPL-drawer-—-CtrlJ","page":"First Session Tour","text":"Press Ctrl+J to open the REPL in the bottom drawer: a persistent Julia session that lives for the whole session. From Files mode you can run a whole .jl file in it without retyping — r runs it in a fresh REPL, R includes it in the current one. At the prompt, type an expression and press Enter to run it (Shift+Enter inserts a newline); outputs (values, stdout, errors, even images) stream back as structured frames.\n\nFor file dispatch and the display protocol, see The REPL.","title":"The REPL drawer — Ctrl+J"},{"category":"section","location":"start/tour/#The-Terminal-drawer-—-CtrlT","page":"First Session Tour","text":"Press Ctrl+T for a local Terminal — an OS shell on the frontend machine, typically used to SSH out to backend hosts. The REPL and Terminal share one physical drawer slot: Ctrl+J and Ctrl+T each toggle their own pane, and pressing the other key swaps the content. See Running & Relaunch.","title":"The Terminal drawer — Ctrl+T"},{"category":"section","location":"start/tour/#The-Monitor-drawer-—-CtrlM","page":"First Session Tour","text":"Press Ctrl+M for the Monitor drawer — a server monitor sampling the configured hosts (GPU and process stats from nvidia-smi and /proc, which are world-readable, so no privileges are needed). Which hosts appear comes from the [monitor] section of .sot/hosts.toml.","title":"The Monitor drawer — Ctrl+M"},{"category":"section","location":"start/tour/#Navigation-modes:-Sessions-—-s-—-and-Hosts-—-h","page":"First Session Tour","text":"Two more nav-tree roots switch the target, not the content:\n\ns — Sessions. Pick a directory and commit it as a workspace. Enter starts the comm-aware agent session in the pane; Shift+Enter starts a bare session with no LLM agent (a plain shell / REPL).\nh — Hosts. List every backend host from .sot/hosts.toml, with the current and default hosts badged. Enter picks a host; the choice persists, and the next launch and any reconnect target it. Switching hosts is \"pick → Ctrl+Q → relaunch\".","title":"Navigation modes: Sessions — s — and Hosts — h"},{"category":"section","location":"start/tour/#Help-—-?","page":"First Session Tour","text":"Press ? (in nav or preview focus, outside edit mode) for the in-app help overlay — the live, authoritative keymap for your build, including any rebinds. When in doubt, ? is the source of truth.","title":"Help — ?"},{"category":"section","location":"start/tour/#Layout-and-focus","page":"First Session Tour","text":"A few global keys manage the window itself:\n\nAction Key What it does\nMove pane focus Ctrl+Arrow shift focus spatially between the four panes\nCycle workspace Shift+Arrow switch the active workspace (left / right)\nMaximize pane Alt+= blow the focused pane up to fill the window\nRestore layout Esc un-maximize (only while a pane is maximized)\nFont scale Ctrl+= / Ctrl+- zoom the UI font up / down (Ctrl+0 resets)\nReconnect F5 reconnect the transport after a drop\nQuit Ctrl+Q real quit (nav focus only)\n\nPlain arrows nav within a pane, Ctrl+Arrow moves between panes, and Shift+Arrow cycles workspaces — three disjoint roles, so they never collide.","title":"Layout and focus"},{"category":"section","location":"start/tour/#Where-to-go-next","page":"First Session Tour","text":"Architecture at a Glance — the three processes and why the language split.\nModes and Previews — the nav surface in depth.\nThe REPL and The Orchestrator — running code and driving the LLM.\nThe Dispatch ABI — teach Ship of Tools a new file type or mode.","title":"Where to go next"},{"category":"section","location":"guide/panes/navigation/#The-Navigation-Pane","page":"The Navigation Pane","text":"Top-left. The navigation pane is the mode tree — a single collapsible outline (a parent context, the current level, and its children, shown through indentation and disclosure carets) that you walk with the arrow keys. It is the pane you steer from; the Preview pane reflects whatever node the cursor is on.\n\n(Image: Navigation pane in Files mode) Files mode mid-descent: parent directory, current directory, and children.","title":"The Navigation Pane"},{"category":"section","location":"guide/panes/navigation/#What-fills-it","page":"The Navigation Pane","text":"A mode is a switchable root for this tree. A hotkey swaps which tree fills the columns, and cursor position is preserved per mode, so jumping between modes and back lands you where you left each one. The modes bound today:\n\nKey Mode Roots the tree at\nf Files the project filesystem\nm Modules modules → functions → methods (read-only, from JuliaSyntax.jl)\ns Sessions the workspaces this backend is hosting\nh Hosts the remote hosts you can target\n\nThe mode keys are plain single characters, so they are scoped to navigation focus: typing f into the REPL or a prompt inserts an f, it does not switch modes. The full conceptual mode set (Project, Types, Math, Outputs, Agents) and what is built today is on the Modes page.","title":"What fills it"},{"category":"section","location":"guide/panes/navigation/#Driving-it","page":"The Navigation Pane","text":"Action Key\nMove up/down the tree ↑ / ↓\nDescend into the cursored node →\nBack up a level ←\nSwitch nav mode f / m / s / h\nFocus this pane from elsewhere Ctrl+Arrow (spatial)\nCopy the cursored file's path to the clipboard c\n\nModes are a planned plugin surface: the design is a Mode subtype with tree_root / tree_children / preview_for methods — the core modes shipping the same way, with no privileged path. Today the nav roots are fixed (the four above) and kernel-hosted; the mode-plugin seam is not yet wired. See Writing a Mode Plugin and The Dispatch ABI.\n\n(Image: A pinned row's sigil and the pinned preview title) A pinned row: the sigil marks it in the tree and the preview title carries the [pinned] chrome.","title":"Driving it"},{"category":"section","location":"guide/panes/navigation/#See-also","page":"The Navigation Pane","text":"Modes — every mode, its column shape, and built-vs-planned status.\nColor Coding — the cross-cutting provenance colours every tree carries.\nPreviews — what the Preview pane shows for the cursored node.","title":"See also"},{"category":"section","location":"design/backend/#Backend-and-Sessions","page":"Backend & Sessions","text":"The backend daemon owns project state. It is the only process the frontend talks to over the line protocol, and it sits between the frontend and the Julia kernels it supervises. For the big picture and the reasoning behind the language split, read Architecture at a Glance.","title":"Backend & Sessions"},{"category":"section","location":"design/backend/#What-the-daemon-owns","page":"Backend & Sessions","text":"A single long-lived Rust process (tokio) holds everything stateful:\n\nProject state — the mode trees, preview caches, and per-workspace navigation state the frontend reads and mutates over the protocol.\nFile watching — a notify watcher per workspace turns external edits into file.changed events (protocol).\nProcess supervision — it spawns and supervises the Julia kernel(s) and the REPL, restarting the kernel on crash and surfacing failures as protocol events.\nThe orchestrator LLM session — the Anthropic client and tool-use loop live here, not in the frontend.\n\nThe frontend never touches kernel state directly, even when everything runs on one laptop. Local-only operation is \"a client connecting to a localhost-bound backend,\" not a separate code path.","title":"What the daemon owns"},{"category":"section","location":"design/backend/#Kernel-launch-and-supervision","page":"Backend & Sessions","text":"The daemon spawns the kernel as a child process and frames the conversation over its stdio, per the line protocol:\n\njulia --project=<repo>/julia/kernel \\\n  -e 'using ShipToolsKernel; ShipToolsKernel.serve(stdin, stdout)'\n\nSpawning is tokio::process::Command with kill_on_drop(true). stderr is captured into a backend log ring buffer — the only place kernel stderr surfaces, so user-facing errors must travel as protocol events, not prints.\n\nRestart policy differs by process:\n\nProcess On exit\nKernel auto-restart, exponential backoff (1s → 16s, then surface to UI); state rebuilds from disk, so restart is safe\nREPL never auto-restarts — a crashed REPL is meaningful; the user decides, and the UI must say \"REPL is dead, press X to restart\"\n\nShutdown is platform-aware because orphaned julia processes are unacceptable:\n\nLinux — SIGTERM, wait 5s, then SIGKILL.\nWindows — taskkill /F /T /PID <pid>; tokio's kill() alone leaves grandchildren, so SIGTERM semantics are not relied on.","title":"Kernel launch and supervision"},{"category":"section","location":"design/backend/#Transport,-persistence,-reconnect","page":"Backend & Sessions","text":"The primary workflow is remote: a Windows-local frontend reaches a Linux remote host where the kernel and GPU live. Local operation is the fall-through. The session must survive SSH disconnects, reconnect from a fresh client, and not depend on host-specific port allocation.\n\nBackend in tmux on the remote. The daemon runs inside a named tmux session so its lifecycle survives SSH drops and its stderr/logs surface in a pane you can tmux attach to.\nPer-session Unix socket, not a TCP port. It listens at $XDG_RUNTIME_DIR/sot/<session_id>.sock. The frontend forwards that remote socket to a local one over SSH (-L, with StreamLocalBindUnlink=yes and ServerAliveInterval=15). Where Unix-socket forwarding isn't available — older Windows OpenSSH — it falls back to a per-session local TCP port allocated at runtime, never fixed.\nTransport auth. SSH authenticates the Unix user for remote socket access, and the socket path is private to that user. Direct TCP remains token-gated because localhost on a shared remote is machine-scoped, not user-scoped.\nReconnect carries revision. Every connect sends {session_id, client_id, last_seen_revision}. The backend replays missed events from a bounded ring or sends a full snapshot if the client is too far behind; heartbeats (~5s) evict stale clients. last_seen_revision is what makes reconnect feel like tmux — without it every disconnect would lose the orchestrator session and any in-flight kernel work.\n\nclient_id is in the handshake from day one; multi-client policy (first client read+write, second read-only follower) is stated but deferred.","title":"Transport, persistence, reconnect"},{"category":"section","location":"design/backend/#Sessions","page":"Backend & Sessions","text":"A session is one project's backend, registered and supervised by tmux rather than by a second daemon. The insight here is that tmux already lists, supervises, multi-attaches, survives disconnect, and forwards across machines — so tmux is the registry, and the \"orchestrator role\" is a frontend-side library plus tmux -F queries, not a new crate.\n\nOne tmux session per backend, plus sot-home. Sessions are named sot-be-<slug> (from Project.toml's name, falling back to the directory basename, with a -N disambiguator on collision). Home base is sot-home.\nDurable metadata on disk. Tmux is runtime-only, so pointers live under ~/.config/sot/: state-<hostname>.toml records where the frontend left off (the -<hostname> suffix matters because the Linux boxes share $HOME); per-workspace toml records how to bring a backend back.\nSessions mode is a normal mode: col 1 lists sessions with a running/unreachable/stopped sigil, col 2 the panes, col 3 pane metadata, and the preview is a live tmux capture-pane tail.\nResume is the default. Launched with no flags, Ship of Tools reads state-<hostname>.toml, resolves the last session's toml, and — if the tmux session is alive and the daemon socket answers — restores nav state, focus, and scroll exactly where you left off. If the daemon died, or the tmux session is gone, it shows a banner offering to restart or recreate. A startup reconciliation pass matches sessions/*.toml against tmux list-sessions (one cheap syscall) and offers to adopt orphans or delete stale toml.","title":"Sessions"},{"category":"section","location":"design/backend/#Workspaces","page":"Backend & Sessions","text":"Two requirements surfaced after the initial sessions design: switching projects must not stop the Julia kernel, and the deployment must stay one tunnel, one socket. The earlier \"frontend reconnects to a different daemon socket per session\" approach satisfied the first but fought the second, so the two concerns — where kernels live vs. which process is the protocol entry point — are split.\n\nOne daemon. One Julia kernel child per workspace, spawned directly by the daemon and routed by workspace_id.\n\nThe kernel is a direct child of the daemon (std::process::Command, --project=<workspace.project_root>, framed over stdio), not a tmux process. The workspace's tmux session still holds the shell/BL pane and any optional LLM pane, but not the kernel.\nThe load-bearing invariant: switching the active workspace never restarts a kernel or rebuilds Julia state. The kernel holding x = 5 keeps holding it while you visit another workspace and return.\nSwitching is a frontend state change. There is no workspace.switch op; the frontend just sets active_workspace_id and re-fires tree.root with the new id. Cost is one round-trip plus a redraw.\nworkspace_id is additive. Tree/preview/kernel/repl/concept/pty ops gain an optional workspace_id; a missing field resolves to the default workspace, so existing single-backend invocations keep working. Lifecycle ops are workspace.list, workspace.create, workspace.destroy.\nKernels can run different Project.toml environments — each is its own Julia process, so MyPackage@0.1 in one workspace and @0.2 in another coexist.\n\nThe trade-off: the daemon owns its kernel children, so a daemon restart loses Julia state across all workspaces. That buys away kernel-discovery sweeps and health probes; spawning sits behind a KernelHost trait so a tmux-hosted implementation can be swapped in later (phase 3) without protocol changes, if \"kernel survives daemon restart\" ever becomes a felt pain point. Cross-machine deployment is unchanged — one daemon, one socket, one ssh -L, with workspace switching in-band on that single connection.","title":"Workspaces"},{"category":"section","location":"features/#What-Ship-of-Tools-Can-Do","page":"What Ship of Tools Can Do","text":"Ship of Tools is an opinionated, agentic development system for the Julia language. You drive Claude Code agents from the keyboard — they write and run the code; you steer, watch, and review. This page is the tour of what that buys you.\n\n(Image: Ship of Tools main window) Fullscreen on an ultrawide display — the layout Ship of Tools is designed around: all four panes comfortably side by side.","title":"What Ship of Tools Can Do"},{"category":"section","location":"features/#Built-for-ultrawide","page":"What Ship of Tools Can Do","text":"Ship of Tools is opinionated about screen real estate: the four-pane layout is designed for a single ultrawide monitor, where navigation, preview, orchestrator, and REPL all get useful width at once. It runs fine on 16:9, but ultrawide is the layout it is for — maximize a pane (Alt+=) when you need depth over breadth.","title":"Built for ultrawide"},{"category":"section","location":"features/#Drive-agents,-don't-type-code","page":"What Ship of Tools Can Do","text":"Ship of Tools is Claude Code–centric: the agents you direct are Claude Code sessions. You orchestrate them from the keyboard instead of typing code yourself.\nRun several at once. Multiple agents work concurrently — one per workspace/session, across machines — coordinated over the inter-agent comm bus (below) and surfaced in the Sessions view. You spawn and drive them hands-on today; a dedicated in-UI orchestration timeline (\"Agents mode\") is on the roadmap.\nSee who needs you. Each agent reports a live state — idle (gray), working (green), blocked (a hard block needing your decision; red), waiting (a soft await on a peer or async result; purple), or done (blue) — driven by Claude Code hooks plus explicit status calls, so you can scan every session's color at a glance instead of tabbing through each one.\nReview the work. Review what each agent changed in its session and with git. (Per-edit provenance colouring and in-UI accept or reject are planned — see Color Coding.)\nAgents drive the UI, too. Skills and hooks let a Claude Code session open an image or file straight in your nav/preview pane to show you a result it just produced — not just describe it in text.\nAsk it for help. The in-pane Claude Code session doubles as your help system: ask a how-to question about Ship of Tools itself and it answers from the project's own in-repo docs — no separate manual to go find.\nHand the agent a file path. In the file nav, c copies the cursored file's full path to the clipboard — paste it straight into the agent's prompt.","title":"Drive agents, don't type code"},{"category":"section","location":"features/#Agents-coordinate-across-machines","page":"What Ship of Tools Can Do","text":"A built-in inter-agent communication system lets sessions message one another — directed or broadcast — across separate sessions and across machines on a shared filesystem.\nEach agent's state (idle / working / blocked / waiting / done) propagates over that same channel, which is what powers the live status indicators.\nIt is how a backend agent on a remote server and a frontend session on your laptop stay aware of each other.","title":"Agents coordinate across machines"},{"category":"section","location":"features/#Work-where-your-compute-is","page":"What Ship of Tools Can Do","text":"The backend runs local (WSL / Linux / macOS*) or on a remote server over SSH — put it where your GPUs and data live.\nClose your laptop and walk away — the backend, REPL, and agents keep running on the remote.\nReopen and Ship of Tools auto-reconnects, restoring your session and view.\nConnect multiple frontends (laptop + desktop) to the same backend.\nSwitch workspaces (projects) and hosts (machines) from inside the app. See Backend & Sessions.","title":"Work where your compute is"},{"category":"section","location":"features/#Keyboard-driven,-and-yours-to-remap","page":"What Ship of Tools Can Do","text":"Everything from the keyboard. Navigate the tree, switch panes and modes, and act on the cursored entry — the whole interface is operable without ever reaching for the mouse.\nRemap any chord. Every named action can be rebound: per-repo in .sot/keybindings.toml, or per-user in $HOME/.config/sot/keybindings.toml; anything you don't override falls through to the defaults. See Keybindings.\nFull-screen any pane — maximize the pane you're in (Alt+=) when you need depth over breadth.","title":"Keyboard-driven, and yours to remap"},{"category":"section","location":"features/#Live-Julia,-in-the-loop","page":"What Ship of Tools Can Do","text":"A persistent Julia REPL — run a whole .jl file (in a fresh REPL, or included into the current session), or type at the prompt. Per-line and per-block dispatch are planned, not yet built.\nPlots render inline in the window (CairoMakie) — not dumped as text or squeezed through a terminal graphics hack. See The REPL.\nOpen and interact with Pluto notebooks; render Quarto and HTML (popped out to your browser when that is the right surface).\nExtensible previews, no Rust required. Teach it a new file type in a few lines of Julia — a subtype, a matches predicate, a preview method — using ordinary multiple dispatch. See The Dispatch ABI.","title":"Live Julia, in the loop"},{"category":"section","location":"features/#See-your-work,-at-full-fidelity","page":"What Ship of Tools Can Do","text":"Native previews of images (PNG), PDF (paged, rasterized in-pane), markdown, JSON, syntax-highlighted source, and typeset math — drawn by Ship of Tools itself, never truncated to text. See Previews.\nPan and zoom figures. Zoom into any image preview; same-size figures in a directory share the zoom and pan, so stepping through a run's plots keeps your framing instead of resetting it each time.\nMovies and animations open in your browser — the preview pane shows a poster frame, and a keypress pops playback out to a real HTML5 player. Pluto, Quarto, and HTML follow the same browser policy.\nServe a web page locally. Point it at any on-disk static site — a Documenter build, a coverage report, a notebook export — and open it in your real browser over an auto-forwarded port, one key chord away.\nPin a file to a live view that updates as it changes on disk.\nCapture an image region for the agent. Zoom into a plot or image preview, then press c to crop the visible region and hand it straight to the in-pane agent.","title":"See your work, at full fidelity"},{"category":"section","location":"features/#Keep-an-eye-on-the-machines","page":"What Ship of Tools Can Do","text":"A monitoring drawer shows CPU / GPU / memory across your hosts — built for watching training runs.\nA built-in terminal pane (a real PTY) for when you just need a shell.","title":"Keep an eye on the machines"},{"category":"section","location":"features/#Make-it-yours","page":"What Ship of Tools Can Do","text":"Lay it out for your screen — a four-pane layout you configure for your screen, with aspect-ratio presets (ultrawide / laptop / portrait). It is at its best on an ultrawide display, and fully usable on a laptop or 4K screen. See Configuration Files.\nRebuild the frontend without losing your session.\n\n<small>* The macOS backend is not yet tested.</small>","title":"Make it yours"},{"category":"section","location":"extend/hdf5/#Worked-Example:-HDF5","page":"Worked Example: HDF5","text":"HDF5Preview is a complete, real FileType plugin shipped under examples/plugins/HDF5Preview/. It adds metadata-only previews for .h5 / .hdf5 / .hdf files. Its purpose is to validate the dispatch ABI from outside core — it is a separate package, depends only on ConceptExplorerCore and HDF5, and requires zero Rust changes to light up HDF5 previews in the frontend.\n\nRead it alongside the generic build in Writing a FileType Plugin; this page walks the actual code top to bottom.","title":"Worked Example: HDF5"},{"category":"section","location":"extend/hdf5/#The-package","page":"Worked Example: HDF5","text":"examples/plugins/HDF5Preview/Project.toml:\n\nname = \"HDF5Preview\"\nuuid = \"c0d7e0f3-5a1c-4b8e-9f23-2e5a6c4b1f88\"\nauthors = [\"kalidke\"]\nversion = \"0.3.0\"\n\n[deps]\nConceptExplorerCore = \"5a28ea34-b669-4214-86b0-825ecd8fbc7c\"\nHDF5 = \"f67ccb44-e63f-5c2f-98bd-6dc0ccc4ba2f\"\n\n[compat]\nHDF5 = \"0.17\"\njulia = \"1.12\"\n\n[sources]\nConceptExplorerCore = { path = \"../../../core\" }\n\nThe [sources] entry is what lets this env instantiate standalone: ConceptExplorerCore is unregistered, so without a path source Pkg fails in check_registered. A plugin living in its own repository would point path (or url) at wherever it keeps core.\n\nTwo dependencies, no more: ConceptExplorerCore for the ABI, HDF5 for reading the files. HDF5 pulls in HDF5_jll, a self-contained, cross-platform binary — deliberately chosen over shelling out to h5ls / h5dump, which are not present on every backend host and would be a fresh-box-breaks failure mode.","title":"The package"},{"category":"section","location":"extend/hdf5/#The-module-and-the-subtype","page":"Worked Example: HDF5","text":"examples/plugins/HDF5Preview/src/HDF5Preview.jl opens with the two usings and a singleton FileType subtype:\n\nmodule HDF5Preview\n\nusing ConceptExplorerCore\nusing HDF5\n\nexport HDF5File\n\nstruct HDF5File <: ConceptExplorerCore.FileType end\n\nHDF5File carries no fields — it is a dispatch tag. Once the plugin loads, file_types (the subtypes(FileType) scan) sees it with no registration call: the dispatch wiring is the ABI.","title":"The module and the subtype"},{"category":"section","location":"extend/hdf5/#matches-—-claiming-the-path","page":"Worked Example: HDF5","text":"const HDF5_EXTENSIONS = (\".h5\", \".hdf5\", \".hdf\")\n\nConceptExplorerCore.matches(::Type{HDF5File}, path::AbstractString) =\n    any(endswith(lowercase(path), ext) for ext in HDF5_EXTENSIONS)\n\nThe same lowercased-extension idiom every built-in uses. file_type_for returns HDF5File for the first matching path it walks.","title":"matches — claiming the path"},{"category":"section","location":"extend/hdf5/#preview-—-a-metadata-only-tree","page":"Worked Example: HDF5","text":"This is the substance of the plugin. The design is metadata-only: it walks groups, datasets, and attributes and reports name, shape, eltype, chunking, and small attribute values — but never reads dataset contents, so the preview is instant regardless of file size.\n\nThe representation it produces is text/markdown: a header line plus a fenced text block containing an ASCII tree.\n\nfunction ConceptExplorerCore.preview(::Type{HDF5File}, path::AbstractString)\n    sz = try\n        filesize(path)\n    catch\n        -1\n    end\n    file = nothing\n    try\n        file = HDF5.h5open(path, \"r\")\n    catch e\n        msg = \"HDF5 open failed for $(basename(path)):\\n\\n$(sprint(showerror, e))\"\n        return ConceptExplorerCore.PreviewPayload(\"text/plain\", Vector{UInt8}(msg))\n    end\n    try\n        w = Walk()\n        emit!(w, basename(path) * \"/\")\n        walk_group!(w, file, \"\")\n        szstr = sz < 0 ? \"?\" : Base.format_bytes(sz)\n        header = \"# HDF5 · $(basename(path))\\n\\n\" *\n                 \"`$(path)` · $(szstr) · $(w.groups) groups · $(w.datasets) datasets\\n\\n\"\n        footer = w.omitted > 0 ?\n            \"\\n… +$(w.omitted) more objects (capped at $(MAX_LINES) lines)\\n\" : \"\"\n        body = header * \"```text\\n\" * join(w.lines, \"\\n\") * \"\\n\" * footer * \"```\\n\"\n        return ConceptExplorerCore.PreviewPayload(\"text/markdown\", Vector{UInt8}(body))\n    catch e\n        msg = \"HDF5 read failed for $(basename(path)):\\n\\n$(sprint(showerror, e))\"\n        return ConceptExplorerCore.PreviewPayload(\"text/plain\", Vector{UInt8}(msg))\n    finally\n        file !== nothing && close(file)\n    end\nend\n\nWhat the rendered payload contains:\n\nA markdown header: # HDF5 · <basename> then a line with the full path, human-readable size (Base.format_bytes), and the group/dataset counts.\nA fenced text tree of the file's structure, joined from the lines the walk accumulated.\nA footer line only if output was capped (see below).\n\nThe MIME is text/markdown, so the frontend's existing markdown renderer draws it — no HDF5-specific renderer, no Rust. The plugin reuses a renderer that already exists by formatting its content as markdown, which is the recommended fallback in Writing a FileType Plugin.","title":"preview — a metadata-only tree"},{"category":"section","location":"extend/hdf5/#Inside-the-walk","page":"Worked Example: HDF5","text":"The tree is built by a depth-first walk that the plugin keeps bounded and safe:\n\nA Walk accumulator holds the output lines, running groups / datasets counts, and an omitted count.\ndataset_label formats one dataset line as name {shape} eltype chunk — e.g. name {100×200} Float64 chunk {32×32} — all from metadata, no data read.\nattr_lines! renders attributes marked @, showing values only for small scalars/strings (truncated at MAX_ATTR_VALUE_CHARS = 60), never reading large attribute arrays.\nwalk_group! recurses with ├─ / └─ connectors, closing each child it opens.","title":"Inside the walk"},{"category":"section","location":"extend/hdf5/#Output-guardrails-and-visible-failure","page":"Worked Example: HDF5","text":"Two project rules show up directly in the code:\n\nBounded output. Output is capped at MAX_LINES = 500. Past the cap, lines are counted as omitted and surfaced in an explicit … +N more objects footer — it reports what was dropped rather than silently truncating.\nNo quiet fallback. An open failure or a read failure returns a text/plain payload describing the error (via sprint(showerror, e)) — the gap is rendered on screen, never a fake-success blank or a panic.","title":"Output guardrails and visible failure"},{"category":"section","location":"extend/hdf5/#parse_entities-—-empty-for-phase-1","page":"Worked Example: HDF5","text":"ConceptExplorerCore.parse_entities(::Type{HDF5File}, path::AbstractString) =\n    ConceptExplorerCore.ConceptEntity[]\n\nNo concept entities yet. The docstring notes the future option: one ConceptEntity per group/dataset, for annotation in the concept nav.","title":"parse_entities — empty for phase 1"},{"category":"section","location":"extend/hdf5/#Enabling-it","page":"Worked Example: HDF5","text":"The plugin is loaded the same explicit way as any extension — list it in the project's Project.toml [sot] table:\n\n[sot]\nextensions = [\"HDF5Preview\"]\n\nThe kernel Base.requires each entry at startup, in declared order, before serving requests (see Discovery & Configuration). For HDF5 specifically the kernel also supports lazy loading — it does not eagerly using HDF5Preview; the plugin is brought in on first .h5 preview (or via a plugins.load op), so HDF5_jll is pulled only when a user actually opens an HDF5 file, never at kernel startup.","title":"Enabling it"},{"category":"section","location":"extend/hdf5/#The-point","page":"Worked Example: HDF5","text":"HDF5Preview is a third-party-shaped package — its own Project.toml, its own [deps], living outside core/. It adds a previewable file type with one subtype and two methods, and the frontend renders the result with no Rust changes because the payload is just a MIME plus bytes. That is the extension surface working exactly as the ABI promises: core ships as plugins to itself, and a plugin from outside core travels the identical path.","title":"The point"},{"category":"section","location":"extend/hdf5/#See-also","page":"Worked Example: HDF5","text":"Writing a FileType Plugin — the generic build this example follows.\nDiscovery & Configuration — the [sot].extensions mechanism.\nThe Dispatch ABI — why zero Rust changes are needed.","title":"See also"},{"category":"section","location":"extend/mode/#Writing-a-Mode-Plugin","page":"Writing a Mode Plugin","text":"A Mode is a switchable root for the collapsible nav tree. Files, Modules, Sessions, and Hosts are modes; a hotkey swaps which mode's tree fills the navigation pane, and cursor position is preserved per mode across switches. A mode plugin supplies a new such root.\n\nThis page documents the mode contract. For the conceptual overview see The Dispatch ABI; for the symbol reference see API — ConceptExplorerCore.\n\nnote: Status: modes are kernel-hosted today\nThe mode contract is part of the ABI, but in phase 1 the core modes live in the Julia kernel, not in standalone plugin packages. The packages under julia/plugins/ are all FileType plugins (Julia source, Markdown, JSON, plain text, PDF, video) — there is no mode-plugin package in the tree yet. This page describes the contract a third-party mode would dispatch on and the shape core's own modes follow; it does not point at a modes-mode package, because none exists. The narrowest extension surface that is fully plugin-shaped today is the FileType — see Writing a FileType Plugin.","title":"Writing a Mode Plugin"},{"category":"section","location":"extend/mode/#The-Mode-contract","page":"Writing a Mode Plugin","text":"A mode is a singleton subtype of Mode plus three dispatched methods. The same three-column shape applies to every mode; the methods supply the tree and its previews:\n\nMethod Returns Role\ntree_root(::Type{M}, project) TreeNode the mode's root node for project\ntree_children(::Type{M}, node) Vector{TreeNode} children of a node (lazy column fill)\npreview_for(::Type{M}, node) PreviewPayload preview for the selected node\n\nThese mirror the FileType methods (matches / preview / parse_entities) in style: a small set of generic functions, one method per mode. As the ABI page notes, core does not ship the generic tree_root / tree_children / preview_for stubs in phase 1 — they are dispatched the same way and implemented by the modes layered above core.","title":"The Mode contract"},{"category":"section","location":"extend/mode/#The-tree-IR","page":"Writing a Mode Plugin","text":"Both mode outputs cross the Rust↔Julia boundary as opaque-payload structs, so the frontend renders any mode without learning its internals:\n\nTreeNode — one node of a column. Fields: id (opaque to Rust), label, kind (a Symbol like :module, :function, :pngfile), has_children, badges (a Vector{Symbol} for the cross-cutting color layer, e.g. :stale, :user_edited, :immutable), and a kind-defined payload::Dict{String,Any} the frontend never inspects. The convenience constructor is TreeNode(id, label, kind; has_children=false, badges=Symbol[], payload=Dict()).\nPreviewPayload — the rendered preview for a node: mime, data bytes, and an extras dictionary. The frontend dispatches on mime to pick a renderer, exactly as it does for file previews.\n\nBecause the payloads are opaque, adding a Mode requires zero Rust changes — the frontend draws whatever tree the kernel sends and renders whatever the preview's MIME names.","title":"The tree IR"},{"category":"section","location":"extend/mode/#How-a-third-party-mode-would-dispatch","page":"Writing a Mode Plugin","text":"The contract is symmetrical with the FileType case. A mode package would:\n\nDepend on ConceptExplorerCore and using it.\nDeclare a singleton subtype: struct MyMode <: ConceptExplorerCore.Mode end.\nImplement tree_root to build the root TreeNode from the project, setting kind, has_children, and any color-layer badges.\nImplement tree_children to expand a node into the next column's nodes, reading the parent node's payload to know what to enumerate.\nImplement preview_for to return a PreviewPayload for the selected node, choosing a MIME the frontend renders (see the MIME table in Writing a FileType Plugin).\n\nDiscovery would follow the same explicit model as FileType plugins: the package is listed in the project's [sot].extensions and Base.required at kernel startup — see Discovery & Configuration.","title":"How a third-party mode would dispatch"},{"category":"section","location":"extend/mode/#The-current-core-modes","page":"Writing a Mode Plugin","text":"For reference, the modes the kernel hosts today (the ones a hotkey switches between in the running app):\n\nMode Column 1 → 2 → 3 Preview\nFiles parent dir → current dir → contents file at appropriate fidelity\nModules (read-only) modules → functions → methods method source + concept artifact\nSessions workspaces → timeline → detail session detail\nHosts configured hosts → endpoint detail host endpoint summary\n\nTypes, Math, and Outputs modes are designed but not yet built; Agents mode is pinned for later (single orchestrator only in phase 1). The mode table in the root design overview and CLAUDE.md lists the full intended set.","title":"The current core modes"},{"category":"section","location":"extend/mode/#Why-modes-aren't-packaged-yet","page":"Writing a Mode Plugin","text":"Extracting a mode into a standalone package is deferred, not blocked: the contract above is exactly what such a package would implement. Until the generic stubs and a third-party mode example exist, treat this page as the design of the mode seam and use the FileType path for a plugin you can ship today.","title":"Why modes aren't packaged yet"},{"category":"section","location":"extend/mode/#See-also","page":"Writing a Mode Plugin","text":"The Dispatch ABI — the conceptual contract and the serialization seam.\nWriting a FileType Plugin — the fully plugin-shaped surface today.\nAPI — ConceptExplorerCore — Mode, TreeNode, PreviewPayload.","title":"See also"},{"category":"section","location":"guide/panes/monitor/#The-Monitor-Drawer","page":"The Monitor Drawer","text":"Bottom drawer — Ctrl+M. The Monitor shows CPU / GPU / RAM history across all your hosts at once — small-multiples, one compact panel per host (a multi-GPU box shows each GPU as its own trace). It shares the bottom drawer slot with the REPL (Ctrl+J) and the Terminal (Ctrl+T); the toggle is global, so it opens even when another pane has focus.\n\n(Image: Monitor drawer with per-host resource panels) The Monitor drawer: CPU / GPU / RAM small-multiples, one panel per host.","title":"The Monitor Drawer"},{"category":"section","location":"guide/panes/monitor/#How-it-gets-the-data","page":"The Monitor Drawer","text":"The connected backend is the aggregator (the frontend talks to one backend at a time, so \"see all servers at once\" is solved at the data layer). For each monitored host it runs a small sampler:\n\nits own host locally; remote hosts as ssh <alias> bash -s, with the script fed over stdin — zero footprint, no daemon, nothing written to the remote's disk.\nCPU% from /proc/stat, RAM% from /proc/meminfo, GPU from nvidia-smi. All world-readable, so no sudo and no privileges are needed.\n\nSampling is reactive: the sources spawn on first subscribe (drawer opened) and tear down when the last subscriber closes it. The backend keeps an in-memory tiered ring buffer per host, so the time axis can rescale to wider windows without a round-trip.\n\nWhich hosts appear comes from the [monitor] section of .sot/hosts.toml (defaults to all configured hosts) — see Configuration Files.","title":"How it gets the data"},{"category":"section","location":"guide/panes/monitor/#On-philosophy-rendering","page":"The Monitor Drawer","text":"Traces are drawn as real SVG, rasterized through the same resvg → wgpu-quad pipeline that renders typeset math — not braille/cell plotting, which is the same class of degraded hack the project rejects for images. A host whose SSH or sampler dies renders an explicit gap, never a silent flatline or a quiet fallback to \"looks fine.\"","title":"On-philosophy rendering"},{"category":"section","location":"guide/panes/monitor/#See-also","page":"The Monitor Drawer","text":"Configuration Files — the [monitor] host list in hosts.toml.\nThe REPL and The Terminal — the other two drawer contents.","title":"See also"},{"category":"section","location":"guide/panes/orchestrator/#The-Orchestrator-Pane","page":"The Orchestrator Pane","text":"Bottom-left. The orchestrator pane holds the agent session that operates Ship of Tools on your behalf. You describe what you want; the agent reads code, writes code, maintains concept annotations, and runs code in the REPL to get there. It is your primary means of driving the environment beyond direct editor and REPL interactions.\n\nThis page covers the pane — what fills it, how it acts, its confirmation policy. For the tool surface, the tool-use loop, and the prompt-cache layout in depth, see The Orchestrator.","title":"The Orchestrator Pane"},{"category":"section","location":"guide/panes/orchestrator/#What-fills-it","page":"The Orchestrator Pane","text":"Today that agent is Claude Code — Ship of Tools is Claude Code–centric. Each agent session is associated with a workspace on the backend daemon, so it sits alongside project state and survives a frontend restart. (When Ship of Tools is developed on itself, the dev agent runs in the Terminal drawer instead.)\n\nMulti-agent operation is real today: several Claude Code agents run concurrently — one per workspace/session, across machines — coordinated over the inter-agent communication system. You start and switch them from Sessions mode (s); cycle the active workspace with Shift+Arrow. What is still deferred is a dedicated in-UI Agents mode (a tasks → timeline → step-detail view).","title":"What fills it"},{"category":"section","location":"guide/panes/orchestrator/#How-it-acts","page":"The Orchestrator Pane","text":"Its own tool surface. Today the agent is Claude Code, which brings its own tools and context management. The original design's fixed, session-start tool set (read/write files, read/write .concept/ annotations, evaluate code in the REPL, list modules) was never built as a bespoke client — see The Orchestrator.\nConfirmation is the LLM's call. Ship of Tools imposes no permission tiers, sandboxes, or rule-based gating of its own — that policy lives with the agent, not the environment around it.\nCoupled to the concept layer. Because the agent is the primary author of code and the maintainer of the artifacts describing it, the two stay coupled; drift it misses surfaces as a stale badge.","title":"How it acts"},{"category":"section","location":"guide/panes/orchestrator/#See-also","page":"The Orchestrator Pane","text":"The Orchestrator — the tool surface, the tool-use loop, and the prompt-cache layout in depth.\nThe REPL — the interactive session the agent drives.\nThe Concept Layer — the annotations it authors and keeps current.","title":"See also"},{"category":"section","location":"guide/panes/preview/#The-Preview-Pane","page":"The Preview Pane","text":"Top-right. The preview pane renders whatever the Navigation pane cursor is on, at the fidelity appropriate to its type — markdown rendered, PNGs shown, .jl syntax-highlighted, PDFs paged, video as a poster frame. Nothing is reduced to plain text.\n\n(Image: Markdown preview with typeset math) A markdown file with LaTeX blocks — math is typeset (MathJax → SVG), not shown as source.","title":"The Preview Pane"},{"category":"section","location":"guide/panes/preview/#What-fills-it","page":"The Preview Pane","text":"Every preview travels one dispatch path: a FileType plugin claims the path with matches and returns a PreviewPayload from preview. The payload's mime tells the frontend which renderer to use; the bytes are opaque to Rust, so adding a format is a Julia-only change. A sampling of the built-ins:\n\nKind Rendered as\n.jl syntax-highlighted source (tokenized kernel-side, no client re-parsing)\n.md rendered markdown, including inline math\n.json / .toml / .txt plain text (JSON/TOML pretty-print / colour planned)\n.pdf one rasterized page; n / p turn pages\nvideo (.mp4, .webm, …) a poster frame; o opens playback in the browser\n\nThe full table — every built-in type and its MIME — is on the Previews page.","title":"What fills it"},{"category":"section","location":"guide/panes/preview/#Beyond-static-rendering","page":"The Preview Pane","text":"Opens in the browser, not the pane. Interactive HTML, Pluto notebooks, and Quarto documents pop out to the OS browser over a forwarded port rather than rendering in-pane; the source still previews as text. Same policy as video.\nCapture a region for the LLM. On a raster image you can crop a zoomed region and hand it straight to the orchestrator — \"what's the artifact here?\". The crop is taken from the source image on the backend, so the agent sees exactly what you do.\nNo silent blanks. When an external tool is missing (ffmpeg, poppler), the plugin returns a text/markdown note explaining the gap instead of an empty pane.","title":"Beyond static rendering"},{"category":"section","location":"guide/panes/preview/#See-also","page":"The Preview Pane","text":"Previews — the full built-in file-type table and the browser pop-out policy.\nWriting a FileType Plugin and The Dispatch ABI — teach the pane a new format with no Rust change.\nThe REPL — figures from the REPL render through this same preview layer.","title":"See also"},{"category":"section","location":"guide/panes/#The-Panes","page":"The Interface","text":"Ship of Tools is a four-pane window over a shared bottom drawer. The panes are fixed in role; the drawer swaps between three contents. Every pane is keyboard-driven and every chord is rebindable — see Keybindings.\n\n┌────────────────────────┬────────────────────────┐\n│ navigation (mode tree) │ preview (file fidelity) │\n├────────────────────────┼────────────────────────┤\n│ orchestrator / pty     │ REPL                    │\n└────────────────────────┴────────────────────────┘\n\nThe bottom-right slot is a drawer: Ctrl+J / Ctrl+T / Ctrl+M swap the REPL, Terminal, and Monitor into it.\n\nThis section has one page per pane. Each is a short orientation — what the pane is, what fills it, and the keys that drive it — and links out to the in-depth guide page behind it. For the first-session walkthrough, see A Guided Tour.","title":"The Panes"},{"category":"section","location":"guide/panes/#The-four-panes","page":"The Interface","text":"Pane Position What it is Deep dive\nNavigation top-left the mode tree — a collapsible outline whose root you switch with a hotkey Modes\nPreview top-right the cursored entity rendered at full fidelity Previews\nOrchestrator bottom-left the agent session (Claude Code) that drives the project The Orchestrator\nREPL bottom-right the persistent Julia session, figures inline The REPL","title":"The four panes"},{"category":"section","location":"guide/panes/#The-bottom-drawer","page":"The Interface","text":"The bottom-right slot is a single drawer shared by three contents, each with its own global toggle. Pressing a second toggle swaps the content in place rather than stacking:\n\nDrawer Toggle What it is\nREPL Ctrl+J persistent Julia session; values, stdout, and figures stream back as structured frames\nTerminal Ctrl+T a local OS shell on the frontend machine — canonical use is SSHing out to backend hosts\nMonitor Ctrl+M CPU / GPU / RAM history across your configured hosts\n\nDrawer toggles are global — they fire even when another pane holds focus.","title":"The bottom drawer"},{"category":"section","location":"guide/panes/#Focus-and-layout","page":"The Interface","text":"Action Key\nMove focus between panes (4-way, spatial) Ctrl+Arrow\nNavigate within the focused pane plain Arrow\nMaximize the focused pane Alt+=\nRestore the layout Esc (only while a pane is maximized)\nCycle the active workspace Shift+Arrow\nHelp overlay (build-current keymap) ?\n\nPlain arrows, Ctrl+Arrow, and Shift+Arrow are three disjoint roles, so they never collide. The authoritative, build-current chord list is in Keybindings; when in doubt, ? is the source of truth.","title":"Focus and layout"},{"category":"section","location":"ref/api-core/#API-—-ConceptExplorerCore","page":"API — ConceptExplorerCore","text":"ConceptExplorerCore is the plugin ABI: the abstract types every extension dispatches on, the two structs that cross the Rust↔Julia boundary, and the contract functions. For the narrative version with examples, see The Dispatch ABI.\n\nModules = [ConceptExplorerCore]","title":"API — ConceptExplorerCore"},{"category":"module","location":"ref/api-core/#ConceptExplorerCore.ConceptExplorerCore","page":"API — ConceptExplorerCore","text":"ConceptExplorerCore\n\nThe plugin ABI for Ship of Tools.\n\nA small set of abstract types defines what is pluggable; methods on them are the contract. A package extends the system simply by being using-ed — the dispatch tables grow, with no registration step and no manifest.\n\nThe surface is six abstract types — FileType, Mode, ConceptEntity, AnnotationKind, Tool and Capture — plus two serialization structs, TreeNode and PreviewPayload, that carry opaque payloads across the Rust↔Julia boundary. Ship of Tools's own core modes and standard file types are implemented as ordinary methods on these types: core is a plugin to itself, which keeps the ABI honest.\n\nSee the Extending Ship of Tools section of the manual for the full contract and worked examples.\n\n\n\n\n\n","title":"ConceptExplorerCore.ConceptExplorerCore"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.AnnotationKind","page":"API — ConceptExplorerCore","text":"AnnotationKind\n\nAbstract supertype for a category of LLM/user-authored annotation that can be attached to a ConceptEntity — type-meaning, math-derivation, and so on. applicable_annotations reports which kinds are valid for a given entity.\n\n\n\n\n\n","title":"ConceptExplorerCore.AnnotationKind"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.Capture","page":"API — ConceptExplorerCore","text":"Capture\n\nAbstract supertype for a structured REPL output — a figure, a DataFrame, and the like — that should render at full fidelity rather than as text. A capture plugin defines capture_payload to convert the value into a display frame.\n\n\n\n\n\n","title":"ConceptExplorerCore.Capture"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.ConceptEntity","page":"API — ConceptExplorerCore","text":"ConceptEntity\n\nAbstract supertype for a conceptual unit a file declares — a function, type, module, or math derivation — as distinct from the file's raw bytes.\n\nparse_entities extracts them from a file; ast_hash gives each a stable provenance key (used to detect annotation staleness), and applicable_annotations reports which AnnotationKinds may decorate it.\n\n\n\n\n\n","title":"ConceptExplorerCore.ConceptEntity"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.FileType","page":"API — ConceptExplorerCore","text":"FileType\n\nAbstract supertype for the kinds of file the explorer can preview and parse — PNG, Julia source, Markdown, JSON, and so on.\n\nA plugin adds a kind with struct MyKind <: FileType end, declares which paths it claims with matches, and implements preview (and, where the kind declares concept entities, parse_entities). Loaded subtypes are discovered automatically by file_types; the best match for a path is resolved by file_type_for. Adding a FileType requires zero changes on the Rust side.\n\n\n\n\n\n","title":"ConceptExplorerCore.FileType"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.Mode","page":"API — ConceptExplorerCore","text":"Mode\n\nAbstract supertype for a switchable navigation-tree root — Files, Modules, Types, Math, Outputs, and the rest. Every mode presents the same shape — a parent → current → children hierarchy shown as a collapsible outline; a hotkey swaps which Mode's tree is the root, and cursor position is preserved per mode across switches.\n\nA mode plugin implements the tree contract — tree_root, tree_children and preview_for — returning TreeNode values for the frontend to render and a PreviewPayload for the focused node. (Those methods are defined by the mode plugins layered above core, not in this module.)\n\n\n\n\n\n","title":"ConceptExplorerCore.Mode"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.PreviewPayload","page":"API — ConceptExplorerCore","text":"PreviewPayload(mime, data, extras=Dict())\n\nA rendered preview crossing to the frontend: a MIME type, the raw data bytes, and an opaque extras dictionary for kind-specific metadata (for example \"page\"/\"page_count\" for paginated content). The frontend dispatches on mime to choose a renderer and never needs to learn about new entity kinds.\n\n\n\n\n\n","title":"ConceptExplorerCore.PreviewPayload"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.Tool","page":"API — ConceptExplorerCore","text":"Tool\n\nAbstract supertype for an action the orchestrator LLM can invoke. A tool plugin defines tool_spec (the schema advertised to the model) and tool_call (the implementation). The tool surface is static in phase 1; plugin-defined tools are a phase-2 seam.\n\n\n\n\n\n","title":"ConceptExplorerCore.Tool"},{"category":"type","location":"ref/api-core/#ConceptExplorerCore.TreeNode","page":"API — ConceptExplorerCore","text":"TreeNode(id, label, kind; has_children=false, badges=Symbol[], payload=Dict())\n\nOne node in a mode's navigation tree, and the unit of structure that crosses to the frontend. Fields:\n\nid::String — opaque, kernel-defined identity (Rust never interprets it).\nlabel::String — display text.\nkind::Symbol — node kind for icon/coloring (:module, :function, :pngfile, …).\nhas_children::Bool — whether the node can be descended into.\nbadges::Vector{Symbol} — cross-cutting provenance/state marks (:stale, :user_edited, :immutable, …) rendered uniformly across every mode.\npayload::Dict{String,Any} — opaque, kind-specific data the kernel round-trips.\n\nThe keyword constructor fills the common defaults.\n\n\n\n\n\n","title":"ConceptExplorerCore.TreeNode"},{"category":"function","location":"ref/api-core/#ConceptExplorerCore.applicable_annotations","page":"API — ConceptExplorerCore","text":"applicable_annotations(e::ConceptEntity) -> Vector{Type{<:AnnotationKind}}\n\nWhich annotation kinds can decorate this entity. Plugins extend.\n\n\n\n\n\n","title":"ConceptExplorerCore.applicable_annotations"},{"category":"function","location":"ref/api-core/#ConceptExplorerCore.ast_hash","page":"API — ConceptExplorerCore","text":"ast_hash(e::ConceptEntity) -> String\n\nStable identifier for a concept entity's textual realisation. Used as the provenance key in .concept/ annotation frontmatter (synced_against).\n\n\n\n\n\n","title":"ConceptExplorerCore.ast_hash"},{"category":"method","location":"ref/api-core/#ConceptExplorerCore.file_type_for-Tuple{AbstractString}","page":"API — ConceptExplorerCore","text":"file_type_for(path) -> Union{Type{<:FileType}, Nothing}\n\nBest-match FileType for path. Each plugin's FileType subtype is expected to define matches(::Type{<:FileType}, path) -> Bool; the first match wins. Falls back to nothing when no plugin claims the path.\n\n\n\n\n\n","title":"ConceptExplorerCore.file_type_for"},{"category":"method","location":"ref/api-core/#ConceptExplorerCore.file_types-Tuple{}","page":"API — ConceptExplorerCore","text":"file_types() -> Vector{Type{<:FileType}}\n\nEvery loaded FileType subtype. Computed via subtypes(FileType) so the list grows automatically when an extension package is using-ed. Used by plugin-discovery callers (e.g. the kernel's plugins.list op).\n\n\n\n\n\n","title":"ConceptExplorerCore.file_types"},{"category":"function","location":"ref/api-core/#ConceptExplorerCore.matches","page":"API — ConceptExplorerCore","text":"matches(::Type{<:FileType}, path) -> Bool\n\nPlugin contract: return true if this FileType should claim path. Phase-1 plugins typically inspect the file extension. The first plugin to return true gets the preview / parse_entities calls.\n\n\n\n\n\n","title":"ConceptExplorerCore.matches"},{"category":"function","location":"ref/api-core/#ConceptExplorerCore.parse_entities","page":"API — ConceptExplorerCore","text":"parse_entities(::Type{<:FileType}, path) -> Vector{ConceptEntity}\n\nExtract the conceptual entities the file declares (modules, types, functions, etc.). Phase-1 returns an empty vector for unknown types.\n\n\n\n\n\n","title":"ConceptExplorerCore.parse_entities"},{"category":"function","location":"ref/api-core/#ConceptExplorerCore.preview","page":"API — ConceptExplorerCore","text":"preview(::Type{<:FileType}, path) -> PreviewPayload\npreview(::Type{<:FileType}, path, params::AbstractDict) -> PreviewPayload\n\nRender a file at path to a typed preview payload. Plugins extend this method on their own <:FileType subtype.\n\nThe 3-arg form carries request parameters (e.g. \"page\" for paginated content — ADR 0021). Callers (the kernel) always invoke the 3-arg form; the fallback below drops the params so 2-arg plugins work untouched. Paginated plugins override the 3-arg form and report position via PreviewPayload.extras (e.g. \"page\"/\"page_count\").\n\n\n\n\n\n","title":"ConceptExplorerCore.preview"},{"category":"section","location":"start/install/#Installation","page":"Install Details","text":"There are three ways to install Ship of Tools. At the current public 0.3.0 baseline there are no published GitHub Release artifacts, so the working public path is from source.\n\nAgent-driven (recommended) — start a coding-agent session (Claude Code, Codex, …) on the target machine and tell it:\nInstall Ship of Tools: fetch https://raw.githubusercontent.com/kalidke/ship-of-tools/main/docs/INSTALL-AGENT.md and follow it.\nThe agent preflights, verifies whether release assets exist, uses the source path when they do not, and proves the result answers before declaring success. The runbook it follows is docs/INSTALL-AGENT.md in this repo.\nFrom release artifacts — prebuilt binaries via the install.sh installer once a GitHub Release with matching assets exists. No Rust toolchain is needed on that path.\nFrom source — clone, build the Rust workspace, instantiate the Julia environments. This is the path for developing it, and Per-Machine Setup adds the current manual config and launcher checklist on top of it.","title":"Installation"},{"category":"section","location":"start/install/#install-release","page":"Install Details","text":"When a GitHub Release with matching assets exists, scripts/install.sh downloads it, verifies checksums, and sets everything up under ~/.local/share/sot by default:\n\ncurl -fsSL https://raw.githubusercontent.com/kalidke/ship-of-tools/main/scripts/install.sh | bash -s -- --local\n\nWith no role flag and an interactive TTY, the installer asks which role this machine should play. In curl | bash or other non-interactive runs, pass the role explicitly:\n\nFlag Role\n--local all-in-one: frontend + backend on this box\n--backend <ssh-alias> frontend here, backend on a remote host over SSH forwarding — the flag names where the backend lives\n--be-only headless backend only (servers, canary boxes)\n\nPlus --version vX.Y.Z to pin a release (default: latest), --prefix <dir> to relocate the install, --port <n> for the daemon port, and --no-service on backend roles when a shared-home deployment should not get a persistent user systemd unit.\n\nWhat the installer lays out under the prefix:\n\nPath Contents\n$PREFIX/bin/sot prebuilt frontend binary\n$PREFIX/bin/sotd prebuilt backend daemon\n$PREFIX/updates staging area used by the release/update path\n$PREFIX/repo/current blobless partial clone of the repo at the selected release tag\n\nThe checkout is part of the installed product. install.sh runs git clone --filter=blob:none --branch vX.Y.Z: full history is present for blame, but only the selected tag's tree is downloaded. Per the ADR 0030 amendment, this checkout is the resource tree and the manual. The backend exports SOT_MANUAL pointing at it, and runtime resources resolve through resource_dir to $PREFIX/repo/current before older layouts. A legacy $PREFIX/julia/current symlink is still created only so older pre-clone binaries that look for the retired bundle layout can find the checkout.\n\nJulia is no longer distributed as a curated bundle. For roles that run a backend on this machine (--local and --be-only), the installer uses juliaup if Julia is missing, then prepares the Julia sidecars inside the checkout:\n\n$PREFIX/repo/current/julia/kernel\n$PREFIX/repo/current/julia/repl\n$PREFIX/repo/current/julia/pluto  # instantiated, precompiled, and load-tested\n\n--backend <ssh-alias> is a frontend-only install on the local machine, so it does not instantiate Julia locally; the remote backend machine should be installed as a backend role.\n\nConnection behavior is role-specific:\n\n--local runs frontend and backend on one box over the backend user's per-user socket. The generated sot-launch wrapper starts sot with --socket <path>; there is no SSH-to-localhost requirement.\n--backend <ssh-alias> creates SSH local forwards to the remote backend, forwarding the local frontend port to the remote user's per-user backend socket.\n--be-only installs the headless backend. By default it installs and starts the systemd --user sotd.service; --no-service skips that unit so shared $HOME machines can supervise sotd themselves.\n\nConfig is written under ~/.config/sot:\n\nFile Behavior\nhosts.toml created for the selected role if missing; role changes back up the existing file to hosts.toml.bak before rewriting\nsettings.toml created only if missing","title":"From release artifacts (Linux/macOS)"},{"category":"section","location":"start/install/#updating","page":"Install Details","text":"After release artifacts exist, two mechanisms complement rather than compete:\n\nMechanism What it does What you do\nBuilt-in update check The backend checks GitHub for new releases (daily and on demand), notifies the frontend, and downloads + verifies + stages the new binaries. A staged binary is applied on the next launch (with a .prev rollback copy). Nothing — you'll see the notice; relaunch when convenient.\nRe-running the installer The complete update in one command: new binaries and the repo checkout moved to the new tag and Julia envs re-instantiated, atomically. curl -fsSL …/install.sh | bash -s -- <your-role> (same command as install; add --version vX.Y.Z to pin).\n\nWhen in doubt: re-run the installer. It is idempotent, safe, and moves everything together. The built-in check keeps you informed and keeps the binaries fresh; full checkout+env automation of the apply step is on the roadmap.\n\nOn update the installer swaps binaries while keeping .prev copies, fetches tags in $PREFIX/repo/current, checks out the requested tag, refuses to move a dirty checkout (commit/stash/revert first — the checkout is read-only by convention), and verifies HEAD equals the tag's recorded commit, so a moved tag or half-checkout fails at update time, not at first use.","title":"Updating"},{"category":"section","location":"start/install/#Requirements","page":"Install Details","text":"linux-x86_64 or macos-aarch64 release artifacts.\ngit for the release-tag checkout.\ncurl and tar for the installer. If using the $GITHUB_TOKEN path instead of gh, jq is also required.\nFrontend roles need glibc ≥ 2.35 (Ubuntu 22.04 or newer). The backend binary is static musl and runs on any distro — --be-only has no glibc floor.\n--backend <ssh-alias> needs key-based SSH to the remote backend host.\nNo GitHub auth is required (the repo is public). An authenticated gh or a set $GITHUB_TOKEN is honored to avoid the unauthenticated API rate limit (60 requests/hour per IP) — relevant only if you install repeatedly.\n\nOn Windows there is no install.ps1 and no shipped sot-setup command yet. scripts/install.sh exits with a Windows-specific message, so install from source via the manual checklist in Per-Machine Setup, or use a Windows release zip only after one exists. On macOS aarch64 the bash installer is wired, but support is still experimental and there is no launchd service wiring; local roles start sotd on demand.\n\nDevelopment machines don't use the release installer at all — they run from a checkout via Per-Machine Setup.","title":"Requirements"},{"category":"section","location":"start/install/#install-source","page":"Install Details","text":"Ship of Tools is a Rust + Julia project: the frontend and backend are Rust binaries, the kernel and plugins are Julia. Installing it means building the Rust workspace once and instantiating a handful of Julia environments.\n\nThis section is the manual build path. Machine-specific config and launchers are covered by Per-Machine Setup; that page is currently a manual source-checkout checklist, not a shipped sot-setup command.","title":"From source"},{"category":"section","location":"start/install/#Prerequisites","page":"Install Details","text":"Tool Version Why\nRust toolchain current stable frontend, backend, protocol crates\nJulia ≥ 1.12 kernel, ConceptExplorerCore, plugins\ngit any clone the repo\n\nInstall Rust with rustup and Julia with juliaup. The julia = \"1.12\" compat floor is set product-wide (Project.toml, core/Project.toml, the kernel and plugin environments).","title":"Prerequisites"},{"category":"section","location":"start/install/#Clone","page":"Install Details","text":"git clone https://github.com/kalidke/ship-of-tools\ncd ship-of-tools","title":"Clone"},{"category":"section","location":"start/install/#Build-the-Rust-workspace","page":"Install Details","text":"The Rust workspace lives under rust/ and has three members — protocol (shared line-protocol types), backend (the daemon), and frontend (the native window). One command builds all of them:\n\ncargo build --release --manifest-path rust/Cargo.toml\n\nThis produces the two binaries the launcher runs:\n\nBinary Path Role\nsot rust/target/release/ native window, chrome, previews\nsotd rust/target/release/ project state, supervision, orchestrator\n\nThe first release build pulls the full rendering stack (winit, wgpu, cosmic-text, glyphon, resvg) and compiles for a while; subsequent builds are incremental.","title":"Build the Rust workspace"},{"category":"section","location":"start/install/#Instantiate-the-Julia-environments","page":"Install Details","text":"The repo is a set of nested Julia environments, not one flat project. The minimum source setup instantiates the umbrella environment, core, the kernel, the REPL shim, and the Pluto sidecar. The umbrella environment at the repo root pins project-level dependencies (e.g. CairoMakie for plotting):\n\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\n\nThen instantiate the core library, kernel, REPL shim, and Pluto sidecar:\n\njulia --project=core            -e 'using Pkg; Pkg.instantiate()'\njulia --project=julia/kernel    -e 'using Pkg; Pkg.instantiate()'\njulia --project=julia/repl      -e 'using Pkg; Pkg.instantiate()'\njulia --project=julia/pluto     -e 'using Pkg; Pkg.instantiate()'\n\ncore/ is ConceptExplorerCore — the abstract types and dispatch contract that make up the extension ABI. The standard plugins live under julia/plugins/*; instantiate any plugin environment you intend to load the same way, e.g.:\n\njulia --project=julia/plugins/julia-source -e 'using Pkg; Pkg.instantiate()'\n\nSee the Repository layout section of the project CLAUDE.md for the full tree of where each environment lives.","title":"Instantiate the Julia environments"},{"category":"section","location":"start/install/#Verify-the-build","page":"Install Details","text":"A quick check that the binaries built and the core library loads:\n\nls rust/target/release/sot rust/target/release/sotd\njulia --project=core -e 'using ConceptExplorerCore; println(\"core OK\")'","title":"Verify the build"},{"category":"section","location":"start/install/#Next-steps","page":"Install Details","text":"Per-Machine Setup — the current manual source-checkout checklist: toolchains, config files, comm skills, and a launcher.\nRunning & Relaunch — launching the frontend, the Terminal drawer, and the self-relaunch loop.\nA Guided Tour — a first session, mode by mode.","title":"Next steps"},{"category":"section","location":"design/roadmap/#Ship-of-Tools-—-Phase-1-Implementation-Plan","page":"Ship of Tools — Phase 1 Implementation Plan","text":"A dependency-ordered plan for the Phase 1 milestone defined in CLAUDE.md. The first milestone is a thin end-to-end skeleton; each subsequent milestone adds exactly one capability.\n\nNote (2026-05-07): Plan revised after the C-pivot. The original plan assumed the frontend was a TUI rendered into the user's terminal. That premise was wrong for the user's deployment (Windows → SSH → tmux → Linux remote). The revised plan keeps the kernel, core IR, line protocol, and backend daemon intact but rebuilds the frontend as a native local window that owns rendering end-to-end.","title":"Ship of Tools — Phase 1 Implementation Plan"},{"category":"section","location":"design/roadmap/#Scope","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Phase 1, per CLAUDE.md:\n\nFiles mode (filesystem nav with previews — markdown, PNG, syntax-highlighted .jl)\nModules mode, read-only (structural via JuliaSyntax.jl)\nPersistent Julia REPL with code dispatch (line, block, file)\nOrchestrator LLM chat with file + REPL tools\n.concept/ annotation read+write with AST-hash provenance\nOne demo external plugin (HDF5 preview), validating the ABI from outside core\n\nOut: Types/Math/Outputs modes, multi-agent, multi-laptop semantics beyond first-wins, background staleness sweep, embedded editor, MP4 playback, automatic plot capture.","title":"Scope"},{"category":"section","location":"design/roadmap/#Risk-register","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Risk De-risked in\nNative rendering of markdown + PNG + MathJax-SVG end-to-end M1 spike (the go/no-go gate)\nratatui custom backend (cell stream into wgpu) — prior art partial M1 spike: try ratatui-wgpu first; roll our own if it can't carry our requirements\nSSH Unix-socket forwarding on Windows OpenSSH M1 spike: confirm StreamLocalBindUnlink + LocalForward path; TCP per-session port is the fallback\nReconnect restoring state without losing in-flight work M1 spike: kill the frontend, restart, see kernel state and tree position resume\nCross-platform Julia subprocess supervision M2 — verify no orphaned julia after Ctrl-C / taskkill / task-manager kill\nAST hash stability across JuliaSyntax versions M3 property test + pinned version + pinned hash format\nJuliaSyntax parsing perf on large files M3 cache by (path, mtime, size)\nAnthropic streaming + tool-use loop M6 — build against recorded fixture before live API","title":"Risk register"},{"category":"section","location":"design/roadmap/#Milestones","page":"Ship of Tools — Phase 1 Implementation Plan","text":"","title":"Milestones"},{"category":"section","location":"design/roadmap/#M0-—-Decisions-and-scaffolding-(DONE-—-pivoted)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Status: core/, julia/kernel/, rust/protocol/, rust/backend/ scaffolds are committed and stay. rust/frontend/ as committed targeted crossterm; pruned in the M0 closeout (the ratatui-image / crossterm deps and rust/scratch/img-probe/ are removed).","title":"M0 — Decisions and scaffolding (DONE — pivoted)"},{"category":"section","location":"design/roadmap/#M1-—-Spike:-native-window-chrome-previews-remote-socket-reconnect-(1-week)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"The go/no-go gate for option C. If this lands, C is defensible. If preview-layer requirements start pulling in HTML/iframes/WebGL within the spike, fall back to option B (Tauri webview wrapping a web frontend) — kernel/backend/protocol stay intact across the swap.\n\nDeliverable: a single sot binary that:\n\nOpens a native local window (winit + wgpu).\nRuns a ratatui chrome layer (the mode tree, status bar, focus) via a custom Backend impl that paints cells with cosmic-text into the wgpu surface.\nRenders a preview pane in three modes:\nPNG via image → wgpu texture\nMarkdown via comrak → cosmic-text laid out lines\nInline math via MathJax-served SVG → resvg → wgpu texture\nConnects to a backend instance running in a named tmux session on a remote host via SSH-forwarded per-session Unix socket; reconnects after frontend kill restoring tree position and last preview.\n\nFiles (sketch):\n\nrust/frontend/src/main.rs — winit event loop, wgpu surface init, connect-and-spawn flow.\nrust/frontend/src/chrome.rs — ratatui custom Backend impl.\nrust/frontend/src/preview/mod.rs — preview-layer dispatch on MIME.\nrust/frontend/src/preview/{png,markdown,svg}.rs — three concrete renderers.\nrust/frontend/src/transport.rs — SSH-spawn + Unix-socket connect + reconnect loop.\nrust/backend/src/main.rs — Unix-socket listener, session-id handshake, kernel supervisor.\nrust/backend/src/session.rs — session state (tree cursor, preview cache, revision counter for reconnect).\nrust/backend/src/mathjax.rs — Node sidecar wrapper (math snippet → SVG).\njulia/kernel/src/ShipToolsKernel.jl — minimal NDJSON dispatch returning fixed payloads for the spike.\nrust/protocol/src/lib.rs — Request/Response/Event enums for the spike's ops only (hello, preview.get, tree.root).\n\nAcceptance:\n\nRender PNG side-by-side with a real CairoMakie figure, eyeball quality.\nRender a markdown file with embedded LaTeX ($\\int_0^\\infty e^{-x^2}\\,dx$); math appears typeset, not as a code box.\nKill the frontend with the backend running; relaunch; tree cursor and preview restore from last_seen_revision.\nRun on Linux (local → local-loopback) and Windows (Windows local → Linux remote) end to end.\n\nIf any of those fail in a way the architecture can't fix, switch to option B before continuing.","title":"M1 — Spike: native window + chrome + previews + remote socket + reconnect (1 week)"},{"category":"section","location":"design/roadmap/#M2-—-Files-mode-populated,-file-watching-(3–4-days)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Deliverable: real filesystem navigation. .md rendered, .png rendered, .jl shown as syntax-highlighted text. External edits update the tree within 500 ms.\n\nFiles:\n\njulia/plugins/files-mode/src/FilesMode.jl — tree_root, tree_children, preview_for.\njulia/plugins/markdown/src/MarkdownPreview.jl — preview(::Type{MarkdownDoc}, path) → PreviewPayload(mime=\"text/markdown\", data=bytes).\njulia/plugins/png/src/PngPreview.jl — read bytes → image/png payload.\njulia/plugins/julia-source/src/JuliaSourcePreview.jl — read + syntect-friendly metadata → text/plain with extras.\nrust/backend/src/watcher.rs — notify watcher → file.changed events.\n\nAcceptance: open the Ship of Tools repo, navigate to requirements.md, see it rendered. Drop a PNG, navigate, see it. Edit a file externally; tree refreshes.","title":"M2 — Files mode populated, file watching (3–4 days)"},{"category":"section","location":"design/roadmap/#M3-—-Modules-mode-(read-only)-AST-hash-(3–4-days)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Deliverable: hotkey switches root tree from Files to Modules. Columns: modules → functions → methods. Preview shows method source.\n\nFiles:\n\njulia/plugins/julia-source/src/JuliaSourceParser.jl — parse_entities walks JuliaSyntax.parseall.\njulia/plugins/julia-source/src/Index.jl — project index keyed by (path, mtime, size); incremental rebuild on file.changed.\njulia/plugins/modules-mode/src/ModulesMode.jl — mode-tree dispatch.\ncore/src/ASTHash.jl — hash function.\ncore/test/test_ast_hash.jl — property test.\nrust/frontend/src/state.rs — per-mode cursor preservation; modes switch via f / m / s / h in nav focus.\n\nAcceptance: open the Ship of Tools repo, switch to Modules mode, navigate to a method, see source. Property test passes.","title":"M3 — Modules mode (read-only) + AST hash (3–4 days)"},{"category":"section","location":"design/roadmap/#M4-—-Persistent-REPL-with-code-dispatch-(3–4-days)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Deliverable: toggleable REPL pane (~). <Enter> evaluates the line at cursor; <S-Enter> evaluates the surrounding top-level block; <C-Enter> includes the whole file. CairoMakie figures render inline through the preview-layer (NOT through any terminal protocol).\n\nShipped (diverged from the original plan): the REPL toggles with Ctrl+J; whole-file run landed as Files-mode r (fresh REPL) / R (current session); at the prompt Enter submits and Shift+Enter inserts a newline. Per-line / per-block dispatch is still planned.\n\nFiles:\n\nrepl/Project.toml, repl/src/ShipToolsRepl.jl.\nrepl/src/DisplayShim.jl — MIMEDisplay emits framed JSON.\nrust/backend/src/repl.rs — REPL supervisor; repl.eval streams repl.frame events tagged with eval_id.\nrust/frontend/src/repl_pane.rs — consumes repl.frame, dispatches to preview-layer for inline images.\nrust/frontend/src/dispatch.rs — block-span computation via kernel round-trip.\n\nAcceptance: x = 1+1 → 2. Plot returns a wgpu texture inline. Killing REPL via UI doesn't kill kernel; restart works.","title":"M4 — Persistent REPL with code dispatch (3–4 days)"},{"category":"section","location":"design/roadmap/#M5-—-.concept/-annotations-with-AST-hash-provenance-(2–3-days)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Deliverable: Modules-mode preview shows source on top, matching .concept/ annotation below. Stale badge when AST hash mismatches. <S-e> opens annotation in $EDITOR. <r> refreshes provenance. (As built: the annotation is edited inline with e / Ctrl+S rather than via $EDITOR, and the one-key <r> provenance refresh was not implemented — see the concept layer guide.)\n\nFiles:\n\njulia/plugins/concept/src/ConceptStore.jl — read_annotation, write_annotation, is_stale. YAML frontmatter via YAML.jl.\njulia/plugins/concept/src/Provenance.jl — stamps synced_against and synced_at on write.\njulia/plugins/modules-mode/src/ModulesMode.jl — extend preview_for to compose source + annotation + badge.\nrust/frontend/src/preview/composite.rs — multi-pane preview composition (source on top, annotation below, badge overlay).\nrust/backend/src/editor.rs — shell out to $EDITOR, await exit, signal kernel re-read.\n\nAcceptance: create a method, write annotation, save. Modify the body; reopen → annotation shows stale badge. <r> clears it. (As built, the badge is cleared by re-editing and re-saving the annotation, not by a dedicated <r> keypress.)","title":"M5 — .concept/ annotations with AST-hash provenance (2–3 days)"},{"category":"section","location":"design/roadmap/#M6-—-Orchestrator-chat-with-tool-dispatch-(4–5-days)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Deliverable: chat pane (<Space>c). User types; orchestrator streams. Tools: read_file, write_file, read_annotation, write_annotation, repl_eval, list_modules. Demo: \"summarize module X\" → tool calls visible as collapsed breadcrumbs → optional annotation written.\n\nFiles:\n\nrust/backend/src/orchestrator/mod.rs — Anthropic client, SSE streaming, tool-use loop.\nrust/backend/src/orchestrator/tools.rs — tool spec construction.\nrust/backend/src/orchestrator/cache.rs — four-block cache scheme.\nrust/frontend/src/chat_pane.rs — input area, transcript, streaming render via cosmic-text, collapsed tool-call breadcrumbs.\ncore/src/ToolSpec.jl — tool_spec and tool_call dispatch surfaces defined but unwired. Phase 2 seam.\n\nAcceptance: ask the orchestrator to \"create an annotation for function X.\" File appears with proper frontmatter; reopening Modules mode shows the new annotation.","title":"M6 — Orchestrator chat with tool dispatch (4–5 days)"},{"category":"section","location":"design/roadmap/#M7-—-Demo-external-plugin:-HDF5Preview.jl-(1–2-days)","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Deliverable: separate package at examples/plugins/HDF5Preview/ that, when listed in a project's [sot].extensions, makes .h5 files previewable.\n\nFiles:\n\nexamples/plugins/HDF5Preview/Project.toml — depends on ConceptExplorerCore, HDF5.\nexamples/plugins/HDF5Preview/src/HDF5Preview.jl — struct H5File <: FileType end, dispatches preview.\nexamples/plugins/README.md.\n\nAcceptance: drop an .h5, add HDF5Preview to [sot].extensions, navigate, see structured preview. Zero Rust changes were required. That's the test of the ABI.","title":"M7 — Demo external plugin: HDF5Preview.jl (1–2 days)"},{"category":"section","location":"design/roadmap/#Phase-2-seams","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Noted but not built:\n\nMulti-laptop / multi-client — client_id already in handshake; first-wins is the M1+ default; richer policies later.\nMulti-agent — chat.send already takes a session id (always \"main\" in Phase 1).\nRemote transport variants — protocol is transport-agnostic; backend just listens on a different socket.\nTypes/Math/Outputs modes — mode.list already returns a vector.\nBackground staleness sweep — is_stale is on-demand in M5; future is a kernel timer.\nPlugin-defined tools — core/src/ToolSpec.jl defined, unwired in M6; Phase 2 adds kernel.tools.list.\nFull LaTeX docs (tectonic) — only inline math via MathJax in M1; document-level TeX comes when a plugin needs it.\nOption B fallback path — Tauri/wry wrapping a web frontend. The kernel, backend, line protocol, core IR are unchanged across that swap; only the frontend surface differs. Kept warm in case M1 spike findings demote C.","title":"Phase 2 seams"},{"category":"section","location":"design/roadmap/#Working-agreement","page":"Ship of Tools — Phase 1 Implementation Plan","text":"Plan amendments go through PR (this file is committed; argue in the diff).\nDecisions get documented — never in code comments or commit messages alone.\nWorking-session handoff state lives outside the public repo in the private ops sidecar.\nWithin-session granular tracking via TaskCreate (ephemeral by design).","title":"Working agreement"},{"category":"section","location":"ref/keybindings/#Keybindings","page":"Keybindings","text":"The frontend reads its keybindings from a TOML file. Each action maps to one chord, or to a list of chords. Any action you do not override falls through to the built-in default, so a bindings file need only list what you want to change.\n\nThe repo's bindings live in .sot/keybindings.toml; this page documents the discovery order, the chord grammar, and every action with its default chord(s). For the other configuration files see Configuration Files.\n\n(Image: The keybindings help overlay) The ? overlay: every action with its current chord, including your overrides.","title":"Keybindings"},{"category":"section","location":"ref/keybindings/#Discovery-order","page":"Keybindings","text":"The first file found wins for the actions it lists; unlisted actions still fall through to the built-in defaults:\n\n$SOT_KEYBINDINGS — explicit path override.\n<repo-root>/.sot/keybindings.toml — the project's bindings (the file in this repo).\n$HOME/.config/sot/keybindings.toml — per-user bindings.\nBuilt-in defaults compiled into the frontend.\n\nEach action's chord(s) in an earlier file override the built-in default for that action; an absent action falls through to the default.","title":"Discovery order"},{"category":"section","location":"ref/keybindings/#Chord-grammar","page":"Keybindings","text":"A chord is zero or more modifier+ tokens followed by exactly one key:\n\n[modifier+]... key\n\nModifiers (case-insensitive): ctrl, alt (aliases option, meta), shift.\nKey: either a single character (=, z, +) or a NamedKey: Tab, Enter, Escape, Space, Backspace, Delete, ArrowUp, ArrowDown, ArrowLeft, ArrowRight, PageUp, PageDown, Home, End.\n\nMultiple chords for one action are written as a TOML list:\n\nfont.scale_up = [\"Ctrl+=\", \"Ctrl++\"]","title":"Chord grammar"},{"category":"section","location":"ref/keybindings/#The-shift-rule","page":"Keybindings","text":"Shift is enforced only when the chord declares it. On a US layout typing + already carries shift, so Alt+= matches both = and + when alt is held; writing Alt+Shift+= enforces the shift state explicitly. This is why the zoom-in binding can list both Ctrl+= and Ctrl++ to cover the shifted and unshifted key.","title":"The shift rule"},{"category":"section","location":"ref/keybindings/#Actions-and-default-chords","page":"Keybindings","text":"Grouped logically. Chords below are the defaults set in .sot/keybindings.toml.","title":"Actions and default chords"},{"category":"section","location":"ref/keybindings/#Panes","page":"Keybindings","text":"Action Default chord Scope / notes\npane.maximize Alt+= Maximise the focused pane to fill the window.\npane.restore Escape Restore the full layout. Esc triggers this only while a pane is maximized; otherwise Esc passes through to the pty / edit mode / etc.","title":"Panes"},{"category":"section","location":"ref/keybindings/#Sessions","page":"Keybindings","text":"Action Default chord Scope / notes\nsession.create Enter Sessions picker — commit the cursored directory as a new workspace, starting the comm-aware agent (ccb) in the pane.\nsession.create_bare Shift+Enter Commit the cursored directory as a new workspace with no LLM agent (plain shell/REPL).\n\nsession.create_bare is matched before session.create, so a non-shift session.create chord still fires when its shifted bare variant is held — keep create_bare on a strictly-more-specific chord (here, the added Shift).","title":"Sessions"},{"category":"section","location":"ref/keybindings/#Modes","page":"Keybindings","text":"Action Default chord Scope / notes\nmode.files f Switch the nav root to Files mode.\nmode.modules m Switch to Modules mode.\nmode.sessions s Switch to Sessions mode.\nmode.hosts h Switch to Hosts mode.\n\nMode switches are active only in nav focus — single-character chords stay literal text in the pty, editor, and prompts.","title":"Modes"},{"category":"section","location":"ref/keybindings/#Drawers","page":"Keybindings","text":"Action Default chord Scope / notes\ndrawer.repl Ctrl+j Toggle the Julia REPL drawer (global).\ndrawer.terminal Ctrl+t Toggle the Terminal drawer (global).\ndrawer.monitor Ctrl+m Toggle the server Monitor drawer (global).\n\nDrawer toggles are global — they fire even when another pane has focus.","title":"Drawers"},{"category":"section","location":"ref/keybindings/#Focus","page":"Keybindings","text":"Action Default chord Scope / notes\nfocus.pane_left Ctrl+ArrowLeft Move focus to the pane on the left (global, 4-way).\nfocus.pane_right Ctrl+ArrowRight Move focus right.\nfocus.pane_up Ctrl+ArrowUp Move focus up.\nfocus.pane_down Ctrl+ArrowDown Move focus down.\n\nSpatial focus is kept disjoint from plain arrows (per-pane navigation) and Shift+Arrow (workspace cycle).","title":"Focus"},{"category":"section","location":"ref/keybindings/#Workspace","page":"Keybindings","text":"Action Default chord Scope / notes\nworkspace.cycle_next Shift+ArrowRight Cycle to the next workspace (global; ignored while editing a file).\nworkspace.cycle_prev Shift+ArrowLeft Cycle to the previous workspace.","title":"Workspace"},{"category":"section","location":"ref/keybindings/#Chrome","page":"Keybindings","text":"Action Default chord Scope / notes\nquit Ctrl+q Quit. Nav focus only — won't fire in the pty panes.\nhelp.toggle ? Toggle the help overlay. Nav / preview focus, outside edit mode.\nview.fullscreen F11 Toggle window fullscreen.\ntransport.reconnect F5 Reconnect the backend transport.","title":"Chrome"},{"category":"section","location":"ref/keybindings/#Font","page":"Keybindings","text":"Action Default chord Scope / notes\nfont.scale_up Ctrl+=, Ctrl++ Zoom in. = and shifted + both zoom in.\nfont.scale_down Ctrl+-, Ctrl+_ Zoom out. - and _ both zoom out.\nfont.scale_reset Ctrl+0 Reset font scale.","title":"Font"},{"category":"section","location":"ref/keybindings/#In-pane-context-(fixed)","page":"Keybindings","text":"These single-key actions feed context to the in-pane agent. They are not yet rebindable — they are not part of the configurable action set above.\n\nKey Scope What it does\nc file nav focus Copy the cursored file's path (absolute, backend-side) to the OS clipboard — paste it into the agent's prompt.\nc preview focus Crop the visible region of a zoomed image preview and send it to the in-pane agent.","title":"In-pane context (fixed)"},{"category":"section","location":"ref/keybindings/#See-also","page":"Keybindings","text":"Configuration Files — settings.toml and hosts.toml.","title":"See also"},{"category":"section","location":"guide/color-coding/#Color-Coding","page":"Color Coding","text":"Color coding is a cross-cutting layer. The same entity carries the same provenance no matter which mode you view it in, so Ship of Tools renders that provenance uniformly across all modes — a stale annotation looks stale in Modules mode, Files mode, and every other mode, because staleness belongs to the entity, not to the view.\n\nColor and badging are orthogonal to mode. A mode decides what tree to show; the color layer decides how each node is tinted based on who touched it, when, and whether its annotation still matches the code.\n\nnote: Design vs. built today\nThis page describes the cross-cutting colour layer as designed. What renders today is the per-session agent work-state colour (idle · working · blocked · waiting · done, on the Sessions strip and nav rows) and the stale-annotation drift badge. The per-entity provenance states in the table below — user-edited, agent-edited accept/reject, immutable/external, pinned/favorited — are planned.\n\n(Image: Session strip and nav rows showing the five work-state colors) The per-session work-state colors: working, idle, blocked, waiting, done — one session mid-flash on a state change.","title":"Color Coding"},{"category":"section","location":"guide/color-coding/#The-states","page":"Color Coding","text":"State Appearance Meaning\nUser-edited recently Warm color, fades over time You touched this entity lately; the warmth decays as it ages.\nAgent-edited, unaccepted Distinct color (unresolved diff) An agent changed this and you have not yet accepted or discarded the change.\nAgent-edited, accepted Neutral, small sigil An agent's change that you accepted; marked with a small sigil but otherwise neutral.\nImmutable / external Dimmed Base, dependencies, vendored code — not yours to edit, rendered dimmed.\nStale annotation Yellowed The entity's concept annotation was written against older code (AST hash mismatch).\nPinned / favorited Accented border You marked this entity for quick return; rendered with an accented border.","title":"The states"},{"category":"section","location":"guide/color-coding/#Why-it-is-mode-independent","page":"Color Coding","text":"Provenance is a fact about an entity: who edited it, when, whether its annotation is current. None of those facts change when you switch the root tree from Files to Modules. Encoding them once, at the entity level, means you do not relearn a color scheme per mode — a yellowed badge always means \"the prose may not match the code,\" wherever you see it.\n\nThe \"stale annotation\" state is the direct rendering of the staleness detection described in The Concept Layer: when an annotation's synced_against hash no longer matches its target's current AST hash, the entity yellows in every mode until you refresh it.","title":"Why it is mode-independent"},{"category":"section","location":"start/setup/#Per-Machine-Setup","page":"Per-Machine Setup","text":"Ship of Tools runs across machines and operating systems, and each one needs a small amount of local state: the toolchains, a host registry, frontend settings, and a launcher.\n\nwarning: Status: source setup is manual today\nThe documentation and release plan call the intended guided flow sot-setup, but no sot-setup command is shipped in the checkout yet. For Windows and source checkouts, follow the checklist below manually. The Linux/macOS release installer (scripts/install.sh) automates much of this for packaged installs.","title":"Per-Machine Setup"},{"category":"section","location":"start/setup/#Source-Setup-Checklist","page":"Per-Machine Setup","text":"The intended sot-setup flow is a one-shot, cross-OS onboarding for a Ship of Tools machine (Windows, Linux, or macOS). Until that command exists, do these steps yourself:\n\nInstall the toolchains. Rust via rustup and Julia via juliaup, where they are missing.\nBuild the Rust workspace (rust/) — the frontend and backend binaries.\nAsk a short Q&A — your machine's role and, if it talks to a remote backend, that server's details (see below).\nInstantiate Julia environments: the repo root, core, julia/kernel, julia/repl, and julia/pluto.\nWrite .sot/hosts.toml and .sot/settings.toml from your answers.\nInstall agent comm resources: julia --project=. -e 'using ShipTools; ShipTools.update_comm()'.\nCreate a launcher / shortcut so you can start the app without typing the build paths.\n\nOn Windows frontend machines, run scripts\\install-shortcut.ps1 after .sot\\hosts.toml exists. Re-run it after editing host config so an existing taskbar pin is repointed to the launcher.","title":"Source Setup Checklist"},{"category":"section","location":"start/setup/#The-cross-OS-topology","page":"Per-Machine Setup","text":"The setup question that matters most is which role this machine plays, because The Ship of Tools deployment is split across operating systems by design:\n\nWindows is the display surface. The frontend — the native window that renders previews and owns the keyboard — runs on the Windows machine.\nLinux remotes run the backend. The daemon and the Julia kernel run on a Linux server (for example myserver, host-b, or host-c), supervised inside a tmux session so it survives SSH drops.\nA per-session socket is SSH-forwarded from the remote to the local machine; the frontend connects over that forward. Local and remote operation use the same protocol — only the transport differs.\n\nThis is the canonical \"Windows local · Linux remote-in-tmux\" topology.","title":"The cross-OS topology"},{"category":"section","location":"start/setup/#The-machine-role-question","page":"Per-Machine Setup","text":"The Q&A asks which of three roles the machine fills:\n\nRole What runs here Typical machine\nfrontend-local the frontend only; backend is on a remote Windows laptop / workstation\nbackend-remote the backend + kernel, reached over SSH Linux server in tmux\nall-local frontend and backend on one machine a single Linux or macOS box for offline work\n\nFor frontend-local, the flow also collects the backend server's connection details (its SSH alias, the repo path on the remote, and the local forwarded port) and writes them as a host entry. The remote side of that forward is the backend user's per-user Unix socket, discovered from sotd session-socket-path sot unless remote_socket is set explicitly.","title":"The machine-role question"},{"category":"section","location":"start/setup/#What-gets-written","page":"Per-Machine Setup","text":"The answers land in two files under .sot/, both with layered discovery so a machine-specific or environment override can take precedence:","title":"What gets written"},{"category":"section","location":"start/setup/#hosts.toml-—-the-host-registry","page":"Per-Machine Setup","text":"A section per backend host, in a deliberately simple key = value format so both the Rust frontend and the PowerShell launcher can parse it without a TOML library. The in-app Hosts mode (hotkey h) lists every [host.<name>] and lets you pick the target; the choice persists, and the launcher and reconnect both route to it.\n\ndefault_host = \"myserver\"\n\n[host.myserver]\nssh_alias   = \"myserver\"\nremote_repo = \"/home/me/projects/ship-of-tools\"\ntcp_port    = 18743  # local side of the SSH forward\n# remote_socket = \"/run/user/<uid>/sot/sessions/sot.sock\"\n\nDiscovery order: $SOT_HOSTS → <repo-root>/.sot/hosts.toml → $XDG_CONFIG_HOME/sot/hosts.toml (or %APPDATA%\\sot\\hosts.toml). Adding a new remote is one entry here — no launcher edit.","title":"hosts.toml — the host registry"},{"category":"section","location":"start/setup/#settings.toml-—-frontend-settings","page":"Per-Machine Setup","text":"The layout preset and the Terminal drawer's resume command (used by the self-relaunch loop — see Running & Relaunch). Any value missing or out of range silently falls back to the built-in default; a malformed settings file never crashes the chrome.\n\n[layout]\npreset = \"auto\"   # auto | ultrawide | laptop | portrait\n\n[terminal]\nresume_command = \"claude --dangerously-skip-permissions --continue /sot-fe-session-start\"\n\nDiscovery order: $SOT_SETTINGS → <repo-root>/.sot/settings.toml → $HOME/.config/sot/settings.toml → built-in defaults. Keybindings live in a sibling .sot/keybindings.toml with the same layered pattern.","title":"settings.toml — frontend settings"},{"category":"section","location":"start/setup/#After-setup","page":"Per-Machine Setup","text":"Once the checklist is complete, the machine has a launcher and a valid host configuration. Continue to Running & Relaunch to start the app, or take A Guided Tour of a first session.","title":"After setup"},{"category":"section","location":"guide/architecture/#Architecture-at-a-Glance","page":"Architecture at a Glance","text":"Ship of Tools is three processes plus your REPL, talking over a socket — even when everything runs on one machine. This page covers what each process owns, why the split falls where it does, the one serialization seam between Rust and Julia, and how the same architecture runs a remote project with almost no extra plumbing.","title":"Architecture at a Glance"},{"category":"section","location":"guide/architecture/#The-processes","page":"Architecture at a Glance","text":"Process Language Owns\nFrontend Rust (winit + wgpu) the native window, chrome, preview rendering, keystrokes. Stateless about the project — it renders what the backend sends and forwards input.\nBackend daemon Rust (tokio) project state, file watching, process supervision, the orchestrator LLM session. Exposes a JSON line protocol to the frontend.\nJulia kernel Julia dispatch tables, mode-tree computation, file-type-aware indexing, AST hashing, Julia-aware previews. Loads the project's Project.toml environment.\nREPL Julia your interactive session, supervised by the backend, with a display shim that emits structured frames (stdout, stderr, value, image, error) over stdio.\n\nThe frontend holds no project knowledge. The backend is the single owner of project state and the one protocol entry point. The Julia kernel is where every extensible, Julia-aware decision is made. The REPL is a separate process so a crash or a long computation in your interactive session never takes down the daemon that supervises it.","title":"The processes"},{"category":"section","location":"guide/architecture/#Why-client/server,-even-on-one-machine","page":"Architecture at a Glance","text":"The three processes communicate over a socket locally exactly as they would across a network. That looks like overhead on a single box, but it is the whole point: remote operation becomes almost free — same protocol, different transport. The plumbing cost is paid once, up front, instead of being retrofitted later when local-only assumptions have already hardened.\n\nThis directly serves a requirement: local and remote operation must offer the same user experience. A design that special-cased \"local\" would have to grow a second path for \"remote\" and keep the two in sync forever. One socket-based protocol avoids that split entirely.","title":"Why client/server, even on one machine"},{"category":"section","location":"guide/architecture/#Why-the-language-split","page":"Architecture at a Glance","text":"The boundary between Rust and Julia is not arbitrary — each language does what it is best at.\n\nRust is for plumbing. The TUI, IPC, file watching, process supervision, and terminal-protocol image rendering all want a single statically-linked, cross-platform binary with predictable performance. tokio, notify, winit, and wgpu cover that stack. Keep this layer boring and predictable; it should rarely need to change when the feature set grows.\n\nJulia is for everything plugin-extensible and Julia-aware. JuliaSyntax.jl parses Julia source — reimplementing a Julia parser in Rust is a non-starter, and it is the parser the language itself uses. More fundamentally, the unique value proposition here is multiple dispatch as the plugin mechanism: a small set of abstract types whose methods are the extension ABI. That only works in Julia. See The Dispatch ABI.","title":"Why the language split"},{"category":"section","location":"guide/architecture/#The-serialization-seam","page":"Architecture at a Glance","text":"The Rust↔Julia boundary is crossed by exactly two generic structs, both carrying opaque, kernel-defined payloads:\n\nstruct TreeNode\n    id::String                # opaque to Rust\n    label::String\n    kind::Symbol              # :module, :function, :pngfile, ...\n    has_children::Bool\n    badges::Vector{Symbol}    # :stale, :user_edited, :immutable, ...\n    payload::Dict{String,Any} # opaque, kernel-defined per kind\nend\n\nstruct PreviewPayload\n    mime::String\n    data::Vector{UInt8}\n    extras::Dict{String,Any}\nend\n\nThe frontend draws whatever tree the kernel sends and dispatches on PreviewPayload's mime to pick a renderer. It never inspects id or payload; it never learns what a :pngfile is. The consequence is the load-bearing property of the whole design: adding a new FileType requires zero Rust changes (and a Mode too, once the mode-plugin seam is wired — modes are kernel-hosted today). A plugin that teaches the kernel to preview a new file format ships entirely in Julia, and the frontend renders it because the MIME type tells it how. See TreeNode and PreviewPayload in the API reference.","title":"The serialization seam"},{"category":"section","location":"guide/architecture/#Deployment-topology","page":"Architecture at a Glance","text":"The architecture's payoff shows up in the standard remote deployment: a Windows or local frontend driving a Linux backend where the Julia kernel and GPU live.\n\nThe backend runs as a long-lived daemon on the remote, supervised by a named tmux session so it survives SSH disconnects.\nIt listens on a per-session Unix socket, not a host-allocated TCP port.\nThe frontend forwards that remote socket to a local one over ssh -L — one tunnel, one socket — and speaks the same JSON line protocol it would speak to a local backend.\nOn reconnect (after a laptop sleep, a wifi flap, or a fresh client), the frontend re-attaches by session id; the kernel keeps its state — variables, loaded modules, in-flight computations — across the gap.\n\nA single daemon can host one Julia kernel per workspace, routed by workspace_id, so switching the active project is fast and never tears the kernel down. tmux itself acts as the session registry — listing, creating, and killing backends — rather than a second bespoke daemon.","title":"Deployment topology"},{"category":"section","location":"guide/architecture/#Where-to-go-next","page":"Architecture at a Glance","text":"Modes — the switchable nav-tree roots over the same code.\nPreviews — which file types render, and at what fidelity.\nThe Dispatch ABI — how multiple dispatch is the plugin system.\nFrontend Rendering — how previews are drawn into the window.\nBackend & Sessions — daemon internals and session lifecycle.\nThe Line Protocol — the JSON wire format between the processes.","title":"Where to go next"},{"category":"section","location":"guide/concept-layer/#The-Concept-Layer","page":"The Concept Layer","text":"The concept layer is what makes Ship of Tools a concept explorer rather than a file browser. It is prose — intent, contracts, meaning, math — attached to the units of a Julia project and kept honest against the code those units describe. The LLM is the primary author and maintainer of this layer; the user can edit it too.\n\n(Image: A stale concept annotation rendering with the drift badge) A stale annotation: its synced_against hash no longer matches the code, so the drift badge renders until someone refreshes the prose.\n\nIt is built from two layers with very different update mechanics. Keep them separate in your head:\n\nLayer Source Currency LLM involved?\nStructural parsed mechanically from code via JuliaSyntax.jl (plus live introspection where the project is loaded) always current no\nAnnotation LLM- or user-authored prose attached to nodes in the structural layer can drift yes\n\nThe structural layer is derived, never stored as a fact you can get wrong — it is recomputed from the source. The annotation layer is the part that can fall out of sync, so the whole design below is about detecting and surfacing that drift rather than trying to prevent it.","title":"The Concept Layer"},{"category":"section","location":"guide/concept-layer/#The-.concept/-sidecar","page":"The Concept Layer","text":"Annotations live in a sidecar .concept/ directory at the project root, mirroring the conceptual structure of the project:\n\n.concept/\n  project/intent.md\n  modules/MyModule.md\n  types/MyModule/MyType.md\n  functions/MyModule/myfunction.md\n  math/geometry/rotation.md\n\nThe directory is plain files on disk. That is deliberate: it survives a restart, diffs cleanly in git, and is readable without Ship of Tools running. Each path corresponds to a node you can navigate to in a mode — a module in Modules mode, a type in Types mode, a derivation in Math mode — so the annotation for a node is always at a predictable location.","title":"The .concept/ sidecar"},{"category":"section","location":"guide/concept-layer/#Frontmatter","page":"The Concept Layer","text":"Each annotation file carries YAML frontmatter that ties the prose to the entity it describes and records when it was last reconciled:\n\ntarget: MyModule.MyType\ntarget_kind: type\nsynced_against: <ast_hash>\nsynced_at: 2026-01-15T14:30Z\nauthored_by: orchestrator | user\nreferences:\n  - MyModule.method1\n  - math/geometry/rotation\n\nField Meaning\ntarget the entity this annotation describes\ntarget_kind what kind of entity (type, function, module, …)\nsynced_against the AST hash of target at the last reconciliation\nsynced_at timestamp of that reconciliation\nauthored_by orchestrator or user — provenance feeds Color Coding\nreferences links to other entities or annotations (verification is planned — see below)\n\nThe load-bearing field is synced_against. It is the AST hash of the target as of the last time the annotation was confirmed accurate. Staleness is just a comparison: recompute the target's hash now, and if it differs from synced_against, the prose was written against an older version of the code. Today synced_against is the only field the backend parses; the rest of the frontmatter is preserved verbatim across edits and shown above the editor, but is not otherwise acted on.","title":"Frontmatter"},{"category":"section","location":"guide/concept-layer/#The-AST-hash","page":"The Concept Layer","text":"The hash is computed from the parsed AST of the targeted entity, not its raw text, so it ignores changes that don't matter and catches changes that do:\n\nReformatting and whitespace outside string literals → same hash. Running a formatter does not mark anything stale.\nWhitespace inside docstrings or string literals → different hash. A meaningful docstring edit is a meaningful change.\nVariable renames → different hash.\n\nThe algorithm walks the JuliaSyntax.SyntaxNode tree depth-first — SyntaxNode already excludes trivia (whitespace and comments) — emitting each node's kind and, for leaf nodes, the node's source text. That byte stream is hashed with SHA-256 and rendered as the full 64-character hex digest (no truncation, no version prefix).","title":"The AST hash"},{"category":"section","location":"guide/concept-layer/#Update-lifecycle","page":"The Concept Layer","text":"Drift detection is reactive — Ship of Tools surfaces it, you fix it when you choose to:\n\nYou save a file. (Either you edited it, or the orchestrator did.)\nThe kernel re-parses the affected files and recomputes AST hashes for the entities in them.\nAnnotations whose target's hash changed are marked stale. The comparison is current_hash != synced_against.\nStale annotations render with a yellowed / wilting badge — and they render that way in every mode, because staleness is a property of the entity's provenance, not of any one view. See Color Coding.\n\nThere is no background sweep in phase 1. Nothing recomputes annotations on a timer or refreshes them behind your back. Visible drift is the feature: a yellowed badge tells you the prose may no longer match the code.","title":"Update lifecycle"},{"category":"section","location":"guide/concept-layer/#Reactive-refresh","page":"The Concept Layer","text":"The intended model is reactive: navigate to the stale annotation and trigger a refresh with a single keypress that re-stamps synced_against (and synced_at) to the target's current hash, marking the prose as reconciled against the present code. You stay in control of when — a refresh asserts the annotation is still accurate, so it is a deliberate act, not an automatic one.\n\nThat one-key re-stamp is not yet built. Today you reconcile a stale annotation by editing it in place (e), updating its synced_against in the frontmatter, and saving (Ctrl+S); Esc discards. A background staleness sweep is also explicitly deferred; refresh is on-demand only.","title":"Reactive refresh"},{"category":"section","location":"guide/concept-layer/#Reference-verification-(planned)","page":"The Concept Layer","text":"The references list links an annotation to other entities (MyModule.method1) or to other annotations (math/geometry/rotation). A background pass that verifies these links after every re-index is planned: when a referenced entity no longer exists — a method was deleted, a derivation renamed — the link would be broken, and the annotation marked stale on that basis too, keeping the concept layer internally consistent, not just consistent with code. Today the references field is preserved on disk but not verified.","title":"Reference verification (planned)"},{"category":"section","location":"guide/concept-layer/#See-also","page":"The Concept Layer","text":"Color Coding — how staleness and authorship render across modes.","title":"See also"},{"category":"section","location":"extend/filetype/#Writing-a-FileType-Plugin","page":"Writing a FileType Plugin","text":"A FileType plugin teaches the explorer to recognise and preview a new kind of file. It is the smallest, most common extension you can write — a package, one abstract-type subtype, and two methods. No Rust changes, no registration call: using YourPackage and the dispatch tables grow.\n\nThis page is the step-by-step build. For the conceptual contract see The Dispatch ABI; for a complete external package see the HDF5 worked example; for turning the plugin on in a project see Discovery & Configuration.","title":"Writing a FileType Plugin"},{"category":"section","location":"extend/filetype/#What-a-FileType-plugin-does","page":"Writing a FileType Plugin","text":"Two things, both dispatched on your subtype:\n\nMethod Returns Role\nmatches(::Type{T}, path) Bool claim a path (usually by extension)\npreview(::Type{T}, path) PreviewPayload render it for the preview pane\n\nparse_entities(::Type{T}, path) is optional — it yields the ConceptEntity values a file declares, for the concept layer. Phase-1 plugins return an empty vector; see below.\n\nAt runtime the kernel finds every loaded subtype with file_types (a subtypes(FileType) scan) and picks the first one whose matches returns true with file_type_for. Your job is to supply the subtype and its methods.","title":"What a FileType plugin does"},{"category":"section","location":"extend/filetype/#Step-1-—-Create-the-package","page":"Writing a FileType Plugin","text":"A plugin is an ordinary Julia package that depends on ConceptExplorerCore. Generate one and add the dependency:\n\nusing Pkg\nPkg.generate(\"MyPreview\")\nPkg.activate(\"MyPreview\")\nPkg.develop(path=\"/path/to/ship-of-tools/core\")\n\nConceptExplorerCore is unregistered, so use Pkg.develop(path=...) to point at the local Ship of Tools core/ checkout. Your Project.toml should carry ConceptExplorerCore under [deps]. The built-in Markdown plugin needs nothing else; only depend on what your preview actually uses (the HDF5 plugin adds HDF5, the Julia-source plugin adds JuliaSyntax and JSON3):\n\nname = \"MyPreview\"\nuuid = \"...\"                # filled in by Pkg.generate\nversion = \"0.1.0\"\n\n[deps]\nConceptExplorerCore = \"5a28ea34-b669-4214-86b0-825ecd8fbc7c\"\n\n[compat]\njulia = \"1.12\"","title":"Step 1 — Create the package"},{"category":"section","location":"extend/filetype/#Step-2-—-Declare-the-FileType-subtype","page":"Writing a FileType Plugin","text":"In src/MyPreview.jl, using ConceptExplorerCore and define a singleton subtype. The type carries no fields — it is purely a dispatch tag — so an empty struct ... end is the whole declaration:\n\nmodule MyPreview\n\nusing ConceptExplorerCore\n\nexport MyKind\n\nstruct MyKind <: ConceptExplorerCore.FileType end\n\nend # module\n\nThat is enough for file_types() to see MyKind once the package is loaded.","title":"Step 2 — Declare the FileType subtype"},{"category":"section","location":"extend/filetype/#Step-3-—-Claim-paths-with-matches","page":"Writing a FileType Plugin","text":"Extend ConceptExplorerCore.matches for your subtype. Return true for paths your plugin owns. Extension matching on a lowercased path is the standard idiom — every built-in plugin does exactly this:\n\nconst MY_EXTENSIONS = (\".myext\", \".my\")\n\nConceptExplorerCore.matches(::Type{MyKind}, path::AbstractString) =\n    any(endswith(lowercase(path), ext) for ext in MY_EXTENSIONS)\n\nfile_type_for walks file_types() in order and returns the first subtype whose matches is true. Keep your predicate narrow — claim only paths you can actually render — so you don't shadow another plugin.","title":"Step 3 — Claim paths with matches"},{"category":"section","location":"extend/filetype/#Step-4-—-Render-with-preview","page":"Writing a FileType Plugin","text":"Extend ConceptExplorerCore.preview to return a PreviewPayload. A payload is a MIME string plus the bytes the frontend should render; the two-argument constructor PreviewPayload(mime, data) covers the common case (a third extras::Dict argument is available for renderer hints):\n\nfunction ConceptExplorerCore.preview(::Type{MyKind}, path::AbstractString)\n    bytes = read(path)\n    ConceptExplorerCore.PreviewPayload(\"text/markdown\", bytes)\nend\n\nThe MIME you choose decides which frontend renderer draws the preview — this is the only contract the frontend cares about, and it is why a new FileType needs zero Rust changes.","title":"Step 4 — Render with preview"},{"category":"section","location":"extend/filetype/#Choosing-a-MIME","page":"Writing a FileType Plugin","text":"Pick a MIME the frontend already renders, or emit text/markdown and let its markdown renderer do the layout. MIMEs in use by the built-in plugins:\n\nMIME Frontend rendering Used by\ntext/markdown comrak + cosmic-text (headings, code fences, inline math) Markdown plugin; the HDF5 example wraps its metadata tree in fenced markdown\ntext/plain plain UTF-8 text error fallbacks\napplication/json plain UTF-8 text today (pretty-print / syntax colour planned) JSON plugin\nimage/png native image quad the PNG example in the ABI page\napplication/vnd.sot.tokens+json pre-tokenised span renderer ({spans:[{text,kind},…]}) Julia-source plugin\n\nIf none fits, the pragmatic choice is to format your content as markdown (text/markdown) — that is what the HDF5 plugin does, emitting a header plus a fenced text tree. Returning text/plain is the right move for an error path (open failed, parse failed): surface the failure as visible text rather than faking a successful preview.","title":"Choosing a MIME"},{"category":"section","location":"extend/filetype/#Failing-visibly","page":"Writing a FileType Plugin","text":"Do not let a bad file produce a fake-success preview. The HDF5 plugin catches an open or read failure and returns a text/plain payload describing the error, so the gap is on screen instead of a silent blank. Match that: render the error, don't swallow it.","title":"Failing visibly"},{"category":"section","location":"extend/filetype/#Optional:-parse_entities","page":"Writing a FileType Plugin","text":"If your file kind declares conceptual units the concept layer should track (functions, types, sections, datasets), implement parse_entities. Every phase-1 built-in returns an empty vector — the method exists so the concept layer can grow later without an ABI change:\n\nConceptExplorerCore.parse_entities(::Type{MyKind}, path::AbstractString) =\n    ConceptExplorerCore.ConceptEntity[]\n\nA later revision can yield one ConceptEntity per logical unit (the Markdown plugin notes heading-hierarchy entities as the obvious next step), each carrying an ast_hash for staleness provenance.","title":"Optional: parse_entities"},{"category":"section","location":"extend/filetype/#Step-5-—-Enable-it","page":"Writing a FileType Plugin","text":"Loaded subtypes are discovered automatically, but which packages a project loads is explicit. Add your package to the project's Project.toml:\n\n[sot]\nextensions = [\"MyPreview\"]\n\nThe kernel Base.requires each entry at startup, in order, before serving requests — so after install + this edit + a kernel restart, opening a matching file routes through your preview. See Discovery & Configuration for the full discovery rule.","title":"Step 5 — Enable it"},{"category":"section","location":"extend/filetype/#Full-skeleton","page":"Writing a FileType Plugin","text":"module MyPreview\n\nusing ConceptExplorerCore\n\nexport MyKind\n\nstruct MyKind <: ConceptExplorerCore.FileType end\n\nconst MY_EXTENSIONS = (\".myext\", \".my\")\n\nConceptExplorerCore.matches(::Type{MyKind}, path::AbstractString) =\n    any(endswith(lowercase(path), ext) for ext in MY_EXTENSIONS)\n\nfunction ConceptExplorerCore.preview(::Type{MyKind}, path::AbstractString)\n    bytes = read(path)\n    ConceptExplorerCore.PreviewPayload(\"text/markdown\", bytes)\nend\n\nConceptExplorerCore.parse_entities(::Type{MyKind}, path::AbstractString) =\n    ConceptExplorerCore.ConceptEntity[]\n\nend # module","title":"Full skeleton"},{"category":"section","location":"extend/filetype/#Next-steps","page":"Writing a FileType Plugin","text":"Worked Example: HDF5 — a real third-party plugin, end to end.\nDiscovery & Configuration — enabling plugins, project-root rules.\nThe Dispatch ABI — the conceptual contract behind every method here.\nAPI — ConceptExplorerCore — symbol-by-symbol reference.","title":"Next steps"},{"category":"section","location":"design/requirements/#Julia-Concept-Explorer-Development-Environment-—-Requirements","page":"Julia Concept-Explorer Development Environment — Requirements","text":"Draft v0.1 — problem and requirements only. Implementation choices are deliberately deferred.","title":"Julia Concept-Explorer Development Environment — Requirements"},{"category":"section","location":"design/requirements/#Problem","page":"Julia Concept-Explorer Development Environment — Requirements","text":"Working with a Julia codebase — whether you wrote it yourself, an LLM wrote it, or you have just landed in someone else's repo — the developer's bottleneck is comprehension, not text manipulation. Conventional IDEs surface files and lines well but expose structure, types, runtime outputs, and the underlying math poorly. This hurts onboarding, code review, and debugging in any context, and is especially acute in LLM-assisted development, where the human never builds understanding line by line.","title":"Problem"},{"category":"section","location":"design/requirements/#Goal","page":"Julia Concept-Explorer Development Environment — Requirements","text":"A keyboard-driven Julia development environment that functions as a concept explorer, letting a developer move fluidly between levels of abstraction — project, module, type, function, outputs, math — while preserving the conventional mechanics of finding, viewing, editing, and running any .jl file with a persistent REPL. The LLM has a dual role: it writes and edits code, and it maintains the concept-explorer artifacts so they stay in sync with the codebase as it evolves.","title":"Goal"},{"category":"section","location":"design/requirements/#Users-and-scope","page":"Julia Concept-Explorer Development Environment — Requirements","text":"Single user.\nOne project at a time. A project is flexible in definition but is typically a git repository.\nCross-platform: must run on Windows and Linux.\nLocal and remote operation are both supported, and must offer the same user experience.","title":"Users and scope"},{"category":"section","location":"design/requirements/#Functional-requirements","page":"Julia Concept-Explorer Development Environment — Requirements","text":"","title":"Functional requirements"},{"category":"section","location":"design/requirements/#Code-authoring-and-execution","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The LLM is the primary author of code. The user may also edit code directly.\nThe user must be able to find, view, edit, and run any .jl file in the project using only the keyboard.\nThe user must be able to dispatch code to the REPL at multiple granularities.\nA persistent Julia REPL must be available throughout a session.","title":"Code authoring and execution"},{"category":"section","location":"design/requirements/#Document-authoring","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The user must be able to author markdown documents (such as PRDs) directly in the environment.","title":"Document authoring"},{"category":"section","location":"design/requirements/#File-navigation-and-inspection","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The user must be able to navigate the entire project filesystem by keyboard alone.\nThe environment must render previews of project files at appropriate fidelity for their type, beginning with PNG images, and including markdown, MP4 video, and JSON. Other figure and structured-data formats are within scope and may be added incrementally.\nThe user must be able to pin a file to a persistent view that updates as the file changes on disk.","title":"File navigation and inspection"},{"category":"section","location":"design/requirements/#Concept-explorer","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The system must support inspection of, at a minimum: source code, type structure, program outputs, project and module structure, and mathematical content.\nThe LLM must maintain — and keep current with the codebase — artifacts capturing project intent, module purpose, type meaning, function contracts, mathematical derivations, and data shapes.\nThe user must be able to move between levels of abstraction, from project overview down to specifics, without leaving the environment.\nWhen a project has no existing concept-explorer artifacts (for example, when first opening an unfamiliar repo), generating them is the LLM's responsibility.","title":"Concept explorer"},{"category":"section","location":"design/requirements/#Multi-agent-operation","page":"Julia Concept-Explorer Development Environment — Requirements","text":"Multiple LLM agents must be able to work on the project simultaneously without interfering with each other's in-progress changes.\nThe user must be able to start, pause, resume, and stop agents.\nThe user must always be able to see what each active agent is doing and what state it is in.\nThe user must be able to inspect, after the fact, any agent's actions, reasoning, and outputs in enough detail to verify the work.\nThe user must be able to compare, accept, or discard each agent's work product.","title":"Multi-agent operation"},{"category":"section","location":"design/requirements/#LLM-mediation-and-control","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The LLM is the user's primary means of operating the environment beyond core editor and REPL interactions.\nDecisions about which actions require user confirmation are the LLM's responsibility. The environment does not impose its own permission tiers, sandboxes, or rule-based action gating.","title":"LLM mediation and control"},{"category":"section","location":"design/requirements/#Window-and-layout-management","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The layout system must accommodate both ultrawide displays and small laptop displays (16:10) without per-device reconfiguration by the user.\nAll layout operations must be possible from the keyboard.","title":"Window and layout management"},{"category":"section","location":"design/requirements/#Quality-requirements","page":"Julia Concept-Explorer Development Environment — Requirements","text":"","title":"Quality requirements"},{"category":"section","location":"design/requirements/#Reliability","page":"Julia Concept-Explorer Development Environment — Requirements","text":"File navigation, content inspection, the editor, the REPL, and watch views must continue to function when the LLM service is unavailable.\nAll static content — anything written to disk in the project — must survive a restart of the environment. In-memory state (REPL bindings, agent conversations, live processes) is not required to persist.","title":"Reliability"},{"category":"section","location":"design/requirements/#Responsiveness","page":"Julia Concept-Explorer Development Environment — Requirements","text":"Interactive operations — navigation, opening files, dispatching code to the REPL, switching views — must feel immediate.\nLong-running operations such as agent tasks may run in the background for arbitrary durations, including hours.","title":"Responsiveness"},{"category":"section","location":"design/requirements/#Remote-and-local-parity","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The user experience on a remote project must be indistinguishable from a local project: same surfaces, same capabilities, same interaction feel.","title":"Remote and local parity"},{"category":"section","location":"design/requirements/#Future-proofing","page":"Julia Concept-Explorer Development Environment — Requirements","text":"The system must avoid the following assumptions, all of which are expected to become limiting within the next year:\n\nA single LLM model or provider.\nShort-running operations only.\nText-only inputs and outputs; visual artifacts must flow in both directions.\nA fixed tool surface available to the LLM.\nA single task or session active at a time.","title":"Future-proofing"},{"category":"section","location":"design/requirements/#Out-of-scope","page":"Julia Concept-Explorer Development Environment — Requirements","text":"Manual coding as the primary workflow. The LLM writes code; the user may intervene.\nIDE features whose purpose is to support manual authoring of code: language servers, completion, linting, formatting, refactoring tooling, and graphical debuggers.\nMouse-driven interaction.\nMulti-user or collaborative use of the environment.\nFirst-class environment surfaces for runtime value inspection, dataflow visualization, and execution tracing. The user accesses runtime values through the REPL itself.\nA configuration surface specified at the requirements level. Configuration will be opinionated by design and is treated as an implementation concern.","title":"Out of scope"},{"category":"section","location":"design/requirements/#Deferred-to-implementation","page":"Julia Concept-Explorer Development Environment — Requirements","text":"These were raised during requirements discussion and are intentionally not specified here:\n\nThe format, location, and update discipline of concept-explorer artifacts.\nThe coordination model among parallel agents and the mechanism by which they are isolated.\nThe specific bridge between the LLM and the REPL — what it can read, inject, and inspect.\nThe window-management model that satisfies the layout requirements.\nThe transport and architecture of the local-and-remote-parity experience.\nThe configuration mechanism by which user preferences are expressed to the LLM.","title":"Deferred to implementation"},{"category":"section","location":"design/requirements/#Amendment-2026-07-05-—-distribution-is-in-scope-(ADR-0030)","page":"Julia Concept-Explorer Development Environment — Requirements","text":"This document originally predated any distribution story; going public made one load-bearing. The following are now in scope, governed by ADR 0030 (and its clone-install amendment):\n\nPublic releases: versioned, tag-driven, prebuilt binaries for Linux and Windows (macOS experimental), CI-gated by artifact smoke tests, a Julia env resolve+load check, and a full-history secret scan.\nInstallation: a scripted installer (interactive or flag-driven) laying down binaries plus a repo checkout pinned at the release tag — the checkout doubles as the product's resource tree and its in-app manual.\nSelf-update: installed instances detect, download, verify, and stage new releases; apply is staged-next-launch with rollback.\n\nUnchanged: multi-user/collaborative use of one environment instance stays out of scope; \"distribution\" means many single-user installs, not a shared deployment.","title":"Amendment 2026-07-05 — distribution is in scope (ADR 0030)"},{"category":"section","location":"design/protocol/#Line-Protocol","page":"Line Protocol","text":"Ship of Tools is three processes that talk over sockets and stdio: the frontend, the backend daemon, and the Julia kernel (plus a supervised REPL). Every seam between them carries the same wire format — newline-delimited JSON for control, length-prefixed binary frames for payloads. The format is deliberately hand-debuggable: you can tail -f any seam and read what is happening.\n\nThis page describes the application protocol. The separate session-to-session messaging used by Claude instances (sot-comm) is a different transport entirely — see The comm protocol. For where these bytes flow (Unix sockets, SSH forwarding, reconnect) see Backend & Sessions.","title":"Line Protocol"},{"category":"section","location":"design/protocol/#Control-frames-—-NDJSON","page":"Line Protocol","text":"Each control message is one UTF-8 JSON object terminated by \\n. The envelope is fixed; the variable part is op plus its payload:\n\n{\"v\":1,\"id\":42,\"kind\":\"req\",\"op\":\"mode.preview\",\"payload\":{}}\n\nField Meaning\nv protocol version; a format bump is a one-line change\nid client-chosen u64; responses echo it, events use 0\nkind \"req\" request, \"res\" response, \"evt\" event\nop the verb (see the op table)\npayload op-specific object; may declare a binary blob\n\nRequests carry a client-chosen id; the matching response echoes that id. Events are unsolicited (file changed, a REPL frame arrived) and use id:0.\n\nShared Rust types live in rust/protocol/ and are consumed by both the frontend and the backend via serde (tagged on op). The kernel side uses JSON3.jl. Because the envelope is versioned, an old client and a new backend can negotiate on v.","title":"Control frames — NDJSON"},{"category":"section","location":"design/protocol/#Binary-blobs","page":"Line Protocol","text":"Control frames stay small and printable; bytes ride alongside them. A payload that carries binary content declares it inline:\n\n{\"blob\":{\"len\":18324,\"mime\":\"image/png\"}}\n\nThe next len bytes on the stream are the blob; after them the stream resumes with the next JSON line. This avoids base64 — roughly a third of bandwidth and CPU on every PNG — while keeping the control channel greppable.","title":"Binary blobs"},{"category":"section","location":"design/protocol/#Phase-1-ops","page":"Line Protocol","text":"The verbs the protocol defines in phase 1 (several later gained an additive workspace_id field):\n\nOp Direction Purpose\nmode.list req/res enumerate available modes\nmode.tree.root req/res root node of a mode's tree\nmode.tree.children req/res children of a tree node\nmode.preview req/res rendered preview for a node (may blob)\nrepl.eval req/res evaluate source in the REPL\nrepl.interrupt req interrupt a running eval\nfs.read / fs.write req/res read/write a file\nconcept.read / concept.write req/res read/write a .concept/ annotation\nchat.send / chat.cancel req drive the orchestrator session\n\nEvents: tree.invalidate, repl.frame, chat.delta, file.changed.","title":"Phase 1 ops"},{"category":"section","location":"design/protocol/#REPL-streaming-frames","page":"Line Protocol","text":"The REPL produces multi-modal output — stdout, stderr, return values with one or more MIME representations, images (CairoMakie figures), and structured errors. The display shim in repl/src/DisplayShim.jl emits length-prefixed JSON frames; the backend wraps each into a repl.frame event on the main stream.\n\nThe shape borrows IJulia's display_data, flattened: no separate metadata channel, every field at the top level of the frame.\n\nFrame Fields\nstdout {text}\nstderr {text}\nvalue {mime, text} for textual MIMEs; {mime:\"image/png\", blob} for binary\nimage {mime:\"image/png\", blob} — convenience for CairoMakie etc.\nerror {message, stacktrace:[{file, line, fn}, …]}\ndone {eval_id, elapsed_ms}\n\nTwo properties fall out of this design:\n\nOrdering is preserved because the REPL process has a single stdout writer; frames arrive in the order they were produced.\ndone is the completion signal. The backend correlates it by eval_id to clear the UI spinner for that evaluation. CairoMakie figures route through show(io, \"image/png\", x) automatically, so plots arrive as image frames and render inline through the preview layer — never through a terminal graphics protocol.\n\nThe mime field keeps the frame schema open: adding HTML, LaTeX, or a custom representation is a frontend-side render change, not a protocol change. Errors are structured rather than dumped to stderr, so the frontend can render stacktraces with file:line links.","title":"REPL streaming frames"},{"category":"section","location":"design/protocol/#Why-NDJSON","page":"Line Protocol","text":"Debuggable by hand. Any seam is readable with tail -f; this pays for itself the first time something hangs.\nCross-platform and low-ceremony. One object per line, UTF-8, works identically on Windows and Linux.\nVersioned. The v field makes a format change explicit and one-line.\nCheap where it matters. Length-prefixed blobs keep binary payloads off the JSON path; parse overhead on the control channel is negligible at human-interactive rates.","title":"Why NDJSON"},{"category":"section","location":"guide/panes/repl/#The-REPL-Pane","page":"The REPL Pane","text":"Bottom-right drawer — Ctrl+J. The REPL pane is a persistent Julia session available throughout a session: the same bindings, the same loaded packages — but its output is structured, and the figures it produces render inline in the native window rather than as terminal text. It shares the bottom drawer slot with the Terminal and Monitor.\n\nThis page covers the pane itself — what fills it, its keys, how to drive it. For the frame schema, dispatch semantics, and figure-rendering mechanics in depth, see The REPL.\n\n(Image: REPL pane with an inline CairoMakie figure) The REPL pane: structured output with a CairoMakie figure rendered inline.","title":"The REPL Pane"},{"category":"section","location":"guide/panes/repl/#What-fills-it","page":"The REPL Pane","text":"A Julia process supervised by the backend daemon, distinct from the kernel that does project introspection. They are separate on purpose: the kernel owns dispatch tables, mode trees, indexing, and AST hashing; the REPL owns your interactive state. Because they are different processes, killing the REPL does not kill the kernel — you can restart your interactive session to clear state or recover from a wedged computation without losing the project view.","title":"What fills it"},{"category":"section","location":"guide/panes/repl/#Driving-it","page":"The REPL Pane","text":"Two ways to drive it, without retyping:\n\nRun a .jl file from Files mode — R includes it into the current session; r first resets the REPL into the file's own project, then runs it.\nType at the prompt — Enter submits, Shift+Enter inserts a newline.\n\nPer-line and per-block dispatch (the line at the cursor, or the surrounding top-level form) are planned, not yet built.\n\nA long-running evaluation runs on its own task, so it does not block the dispatch loop and you can interrupt it mid-eval — a real InterruptException, the same as Ctrl-C in the stock REPL.","title":"Driving it"},{"category":"section","location":"guide/panes/repl/#Structured-output","page":"The REPL Pane","text":"Instead of one undifferentiated text stream, the display shim emits typed frames — stdout, stderr, value, image, error, and a terminal done — so the frontend renders each correctly: stdout/stderr stream as bytes arrive, errors carry a structured stacktrace with file:line links, and a showable value (a CairoMakie Figure, a Plots.Plot, anything with show(io, MIME\"image/png\")) becomes an image frame drawn inline through the preview layer — never a degraded terminal-graphics protocol.","title":"Structured output"},{"category":"section","location":"guide/panes/repl/#See-also","page":"The REPL Pane","text":"The REPL — the frame schema, dispatch semantics, and figure rendering in depth.\nThe Orchestrator — the agent dispatches code to this same REPL via a tool.","title":"See also"},{"category":"section","location":"guide/panes/terminal/#The-Terminal-Drawer","page":"The Terminal Drawer","text":"Bottom drawer — Ctrl+T. The Terminal is a local OS shell on the frontend machine. Its canonical use is SSHing outward to backend hosts, and because it is local — not proxied through the daemon — it works even when the backend is unreachable. It shares the bottom drawer slot with the REPL (Ctrl+J) and the Monitor (Ctrl+M): each key toggles its own content, and pressing another swaps it in place.\n\nKey Drawer closed Showing this content Showing other content\nCtrl+T → Terminal → closed → Terminal\nCtrl+J → REPL → closed → REPL","title":"The Terminal Drawer"},{"category":"section","location":"guide/panes/terminal/#The-dev-session-lives-here","page":"The Terminal Drawer","text":"When Ship of Tools is developed on itself, the dev claude session runs inside this Terminal drawer. Two consequences worth internalizing:\n\nNever kill the frontend process to restart it — that kills your own session along with it. Use the self-relaunch loop (build → sentinel → exit-75 → re-stage → respawn) instead.\nOn a self-relaunch, the frontend reopens straight into this drawer and runs the configured [terminal] resume_command (claude --continue …) as its first command, so the session resumes from its own store without prompts.\n\nBoth are covered in Running & Relaunch.","title":"The dev session lives here"},{"category":"section","location":"guide/panes/terminal/#See-also","page":"The Terminal Drawer","text":"Running & Relaunch — launching, reconnecting (F5), and the self-relaunch loop.\nThe REPL and The Monitor — the other two drawer contents.\nKeybindings — the global drawer toggles.","title":"See also"},{"category":"section","location":"guide/orchestrator/#The-Orchestrator","page":"The Orchestrator","text":"This is the deep dive: the tool surface, the tool-use loop, and the prompt-cache layout. For the pane's layout and how it fits alongside the REPL day-to-day, see Orchestrator Pane.\n\nThe orchestrator is the agent that operates Ship of Tools on your behalf. Beyond core editor and REPL interactions, it is your primary means of driving the environment: you describe what you want, and it reads code, writes code, maintains concept annotations, and runs code in the REPL to get there.\n\nToday that agent is Claude Code — Ship of Tools is Claude Code–centric. Each agent session is associated with a workspace on the backend daemon, so it sits alongside project state and survives a frontend restart.\n\nMulti-agent operation is real today: several Claude Code agents run concurrently — one per workspace/session, across machines — coordinated over the inter-agent communication system. What is still deferred is the dedicated in-UI Agents mode (a tasks → timeline → step-detail view for starting, pausing, resuming, and comparing agents); today you spawn and drive them hands-on through the Sessions view and the comm bus.\n\nnote: Original design vs. today\nThe tool surface and prompt-cache layout below describe the original Ship of Tools phase-1 orchestrator design: a bespoke client talking to the Anthropic API directly. That client was never built — in practice the agent role is filled by Claude Code, which brings its own tools and context management.","title":"The Orchestrator"},{"category":"section","location":"guide/orchestrator/#The-tool-surface","page":"The Orchestrator","text":"The orchestrator acts on the project through a fixed set of tools:\n\nTool What it does\nread_file read a file in the project\nwrite_file write a file in the project\nread_annotation read a .concept/ annotation\nwrite_annotation write a .concept/ annotation (stamps provenance)\nrepl_eval evaluate code in the persistent REPL\nlist_modules list the project's modules from the index\n\nThe tool set is static for the session — registered at session start. A dynamically growing tool surface (plugin-defined tools appearing as packages load) is a phase-2 seam, not a phase-1 capability.","title":"The tool surface"},{"category":"section","location":"guide/orchestrator/#Streaming-and-the-tool-use-loop","page":"The Orchestrator","text":"The orchestrator streams its response token by token, and runs a tool-use loop: it emits a tool call, the backend executes it, the result is fed back, and the model continues — repeating until the model has no further tool calls and finishes its turn. Tool calls surface in the frontend as collapsed breadcrumbs, so you can see what it did (read this file, evaluated that expression, wrote that annotation) without the transcript drowning in tool plumbing.\n\nTool results — the contents returned by read_file, read_annotation, and so on — flow through the conversation tail, not back into the cached context blocks (see below). That keeps a large read from invalidating the cache.","title":"Streaming and the tool-use loop"},{"category":"section","location":"guide/orchestrator/#Prompt-cache-layout","page":"The Orchestrator","text":"Orchestrator sessions are long and tool-heavy, and cost and latency are dominated by repeated context. Ship of Tools talks to the Anthropic API directly and structures the request around four prompt-cache breakpoints, each cached so that the stable parts of the context are not re-sent every turn:\n\nBlock Contents Invalidates when\n1 System prompt + tool schemas rarely — near-permanent cache\n2 Project structural snapshot (module tree + file tree) the index version bumps (i.e. on code change)\n3 .concept/ index — annotation paths + AST hashes only, not contents an annotation is created/deleted or a hash changes\n4 Conversation tail uncached\n\nBlock 1 is nearly permanent, giving a high cache hit rate across all turns. Block 3 carries only paths and hashes, not annotation bodies, so it invalidates rarely even in annotation-heavy work. This layout is Anthropic-specific; adding another provider later means a per-provider adapter, not a redesign.","title":"Prompt-cache layout"},{"category":"section","location":"guide/orchestrator/#Confirmation-policy","page":"The Orchestrator","text":"The LLM decides which actions need your confirmation. Ship of Tools does not impose its own permission tiers, sandboxes, or rule-based action gating — that policy lives with the orchestrator, not in the environment around it. This is a deliberate requirement: the environment trusts the LLM to mediate, and does not wrap it in a separate gate.\n\nThis is also why the concept layer matters. The orchestrator is the primary author of code and the maintainer of the artifacts that describe it, so the two stay coupled: when it changes code, it is responsible for keeping the concept annotations current, and you see drift it misses as a stale badge.","title":"Confirmation policy"},{"category":"section","location":"guide/orchestrator/#See-also","page":"The Orchestrator","text":"Orchestrator Pane — the drawer's layout and how it fits in the UI.\nThe REPL — the interactive session the orchestrator drives via repl_eval.\nThe Concept Layer — the annotations it authors and maintains.","title":"See also"},{"category":"section","location":"extend/abi/#The-Dispatch-ABI","page":"The Dispatch ABI","text":"The Ship of Tools extension model is its defining idea: multiple dispatch is the plugin system. ConceptExplorerCore defines a small set of abstract types, and methods on those types are the ABI. A package extends Ship of Tools simply by being using-ed — the dispatch tables grow, with no registration call, no plugin manifest, and no central list to edit.\n\nThis page is the conceptual contract. For the symbol-by-symbol reference see API — ConceptExplorerCore; for step-by-step tutorials see Writing a FileType Plugin and Writing a Mode Plugin; for a complete external package see the HDF5 worked example.","title":"The Dispatch ABI"},{"category":"section","location":"extend/abi/#The-pluggable-types","page":"The Dispatch ABI","text":"Six abstract types name everything that can be extended:\n\nType What it pluralizes A plugin adds …\nFileType file kinds the explorer can preview / parse struct PngFile <: FileType end\nMode switchable nav-tree roots struct FilesMode <: Mode end\nConceptEntity conceptual units a file declares a function / type / derivation entity\nAnnotationKind categories of concept annotation a TypeMeaning kind\nTool actions the orchestrator can call a ReadFile tool\nCapture structured REPL outputs a FigureCapture","title":"The pluggable types"},{"category":"section","location":"extend/abi/#The-contract","page":"The Dispatch ABI","text":"Each pluggable type has a small set of methods a plugin implements. Defined in core today:\n\n# FileType\nmatches(::Type{<:FileType}, path)        -> Bool              # claim a path\npreview(::Type{<:FileType}, path)        -> PreviewPayload    # render it\nparse_entities(::Type{<:FileType}, path) -> Vector{ConceptEntity}\n\n# ConceptEntity\nast_hash(::ConceptEntity)                -> String            # provenance key\napplicable_annotations(::ConceptEntity)  -> Vector{Type{<:AnnotationKind}}\n\nThe remaining surfaces — tree_root / tree_children / preview_for for Mode, tool_spec / tool_call for Tool, and capture_payload for Capture — are dispatched the same way and are implemented by the mode / tool / capture plugins layered above core. They are part of the contract even though core does not define the generic stubs in phase 1.","title":"The contract"},{"category":"section","location":"extend/abi/#A-minimal-FileType-plugin","page":"The Dispatch ABI","text":"module PngPreview\n\nusing ConceptExplorerCore\n\nstruct PngFile <: FileType end\n\nConceptExplorerCore.matches(::Type{PngFile}, path) =\n    endswith(lowercase(path), \".png\")\n\nConceptExplorerCore.preview(::Type{PngFile}, path) =\n    PreviewPayload(\"image/png\", read(path))\n\nend\n\nusing PngPreview and PNGs are previewable. Nothing else changed — no registration step, and crucially no Rust code.","title":"A minimal FileType plugin"},{"category":"section","location":"extend/abi/#The-serialization-seam","page":"The Dispatch ABI","text":"The Rust↔Julia boundary is crossed by exactly two generic structs, both with opaque payloads so Rust never has to learn about new entity kinds:\n\nTreeNode — one node of a mode's column tree: id, label, kind, has_children, badges, and a kind-defined payload dictionary.\nPreviewPayload — a rendered preview: mime, data bytes, and an extras dictionary. The frontend dispatches on mime to pick a renderer.\n\nBecause both carry opaque, kernel-defined payloads, adding a new FileType requires zero Rust changes — and a Mode will too, once the mode-plugin seam is wired (modes are kernel-hosted today; see Writing a Mode Plugin). The frontend renders whatever the MIME says and draws the tree the kernel sends.","title":"The serialization seam"},{"category":"section","location":"extend/abi/#Core-is-a-plugin-to-itself","page":"The Dispatch ABI","text":"The core modes and the standard file types are implemented as methods on these same abstract types — they receive no privileged access. If core ever needs something the ABI cannot express, the rule is to fix the ABI, not to special-case core. This keeps the extension surface honest: third-party plugins travel exactly the path core travels.","title":"Core is a plugin to itself"},{"category":"section","location":"extend/abi/#Discovery","page":"The Dispatch ABI","text":"Loaded FileType subtypes are found automatically with file_types (a subtypes(FileType) scan), and the best match for a path is chosen by file_type_for. Which extension packages a project loads is declared explicitly — see Discovery & Configuration.","title":"Discovery"},{"category":"section","location":"extend/abi/#Next-steps","page":"The Dispatch ABI","text":"Writing a FileType Plugin\nWriting a Mode Plugin\nWorked Example: HDF5\nAPI — ConceptExplorerCore","title":"Next steps"},{"category":"section","location":"ref/comm/#sot-comm-protocol-—-v1","page":"sot-comm protocol — v1","text":"Session-to-session messaging for Ship of Tools. A fork of the agent-comm user skill with the single-tmux-session jail removed and a durable inbox fallback added, so sessions can address each other across tmux sessions and across machines.\n\nThis file is the contract. Every client — the Claude skill today, a Codex or Gemini adapter or the in-app Ship of Tools Tool plugin later — implements this, so all clients are mutually addressable through the same registry and inboxes.","title":"sot-comm protocol — v1"},{"category":"section","location":"ref/comm/#Layout-(runtime,-under-SOT_COMM_HOME,-default-/.sot-comm)","page":"sot-comm protocol — v1","text":"~/.sot-comm/\n  bin/                     # installed scripts (the reference client)\n  registry.json            # who is reachable + liveness  (source of truth for discovery)\n  .registry.lock/          # mkdir-based spinlock for registry writes\n  inbox/<name>.jsonl       # durable per-recipient queue (append-only)\n  read/<name>.cursor       # per-recipient read cursor (ISO ts of last-shown msg)\n  self/<host>__<pane>.txt  # this pane's chosen agent name (identity recovery)\n\nThe registry and inboxes are data at rest — discovery and catch-up need a shared place to publish, not a live broker. In an optional shared-home deployment, one ~/.sot-comm serves every host sharing that home. For cross-machine with no shared FS, point SOT_COMM_HOME at a git-synced directory; the same files then ride the existing bus. (Not auto-wired in v1 — the .claude-bus git loop can still cover separate filesystems.)","title":"Layout (runtime, under $SOT_COMM_HOME, default ~/.sot-comm)"},{"category":"section","location":"ref/comm/#registry.json","page":"sot-comm protocol — v1","text":"{\n  \"protocol_version\": 1,\n  \"agents\": {\n    \"<name>\": {\n      \"host\":       \"myhost\",                      // hostname -s; used for same-host delivery\n      \"tmux\":       \"session:win.pane\",           // local tmux target for live delivery (\"\" if none)\n      \"pane_id\":    \"%3\",                          // local tmux pane id, for liveness (\"\" if none)\n      \"repo\":       \"Ship of Tools\",\n      \"expertise\":  [\"files\", \"rust-backend\"],\n      \"status\":     \"idle\",                        // lifecycle: idle | spawning\n      \"joined\":     \"2026-05-29T18:00:00Z\",\n      \"last_seen\":  \"2026-05-29T18:04:00Z\",        // heartbeat, bumped on send/poll/join\n      \"state\":      \"working\",                     // ADE state-nav WORK state: working | idle | blocked | waiting | done\n      \"summary\":    \"rebuilding backend + BE suite\",// one-line current / just-finished work (\"\" when none)\n      \"status_at\":  \"2026-06-15T19:32:13Z\"         // when state/summary were last written; nav ages a stale \"working\"\n    }\n  }\n}\n\nLiveness is heartbeat-based, not pane-based: an agent is live if now - last_seen <= SOT_COMM_STALE_SECS (default 600). This is what lets a session on one machine consider a session on another reachable. pane_id is only consulted for the same-host live-paste optimization.\n\nWork-state (state + summary, stamped by status_at) powers the ADE state-nav at-a-glance view, and is distinct from the lifecycle status above. It is written by comm-status.sh <state> [\"summary\"] (merge-only; self-gating — a silent no-op in any session that is not a joined comm agent). Two writers by design: the model pre-announces long work (comm-status.sh working \"<one-liner>\" when it judges a turn will run >~30s), and a global Stop hook floors each turn-end to idle. The daemon joins these fields onto workspace.list (as agent_state / agent_summary / agent_status_at, keyed by the workspace's agent_name) so the frontend — which cannot read this registry directly — renders summary as the per-session glance, colored by state and aged off status_at.","title":"registry.json"},{"category":"section","location":"ref/comm/#Message-frame-(inbox-JSONL,-one-object-per-line)","page":"sot-comm protocol — v1","text":"{\"from\": \"<name>\", \"to\": \"<name>|\\\"\\\"\", \"repo\": \"<repo>\", \"msg\": \"<text>\", \"ts\": \"2026-05-29T18:04:00Z\"}\n\nISO-8601 UTC timestamps sort lexically — the read cursor is just the ts of the last message shown.\n\nto ranks the line for the recipient's inbox Monitor: their own name = directed, wakes the session; \"\" = broadcast copy (relay cc traffic or comm-send --broadcast), files silently for the next comm-poll. A line with NO to key is legacy (pre-stamp, before 2026-06-12) and reads as directed — which is why an unstamped --broadcast once woke the whole network at once.","title":"Message frame (inbox JSONL, one object per line)"},{"category":"section","location":"ref/comm/#Delivery-—-two-modes,-chosen-by-reachability,-always-visible","page":"sot-comm protocol — v1","text":"Durable always: every send appends the frame to inbox/<target>.jsonl.\nLive when possible — directed sends only: if the target is on this host and its pane_id is a currently-live tmux pane, also deliver immediately by load-buffer -> paste-buffer -> Enter into its tmux target. The message text is [<from>:<repo>] <msg>. Broadcasts never paste — a paste+Enter is a full interrupt (it submits into the recipient's claude, costing a model turn; into a dead pane it executes as shell input), so broadcast copies are durable-only and surface on the next comm-poll, matching the Monitor's demotion rule.\n\nIf live delivery isn't possible the send reports queued to inbox (<host>) — the fallback is stated, never silent. The recipient sees it on the next poll.","title":"Delivery — two modes, chosen by reachability, always visible"},{"category":"section","location":"ref/comm/#Verbs-(reference-client-bin/*.sh)","page":"sot-comm protocol — v1","text":"Verb Script Notes\njoin comm-join.sh --name <n> --expertise \"a, b\"; writes registry + self file\nsend comm-send.sh @name \"msg\" or --broadcast \"msg\"; recipient is only the first positional @arg, so the message may itself begin with @\npoll comm-poll.sh shows inbox entries newer than the read cursor\nlist comm-list.sh all agents + live/stale + (me) marker\nleave comm-leave.sh removes self from registry; --name <handle> removes an orphan row (registry only — comm-despawn.sh is full teardown)","title":"Verbs (reference client = bin/*.sh)"},{"category":"section","location":"ref/comm/#Naming-—-everything-derives-from-the-repo","page":"sot-comm protocol — v1","text":"One rule, enforced where possible: names come from the repo, never from the task. A task-named anything is unfindable next to its repo-named siblings (a spawn labeled edge-classify hid the MyPackage agent, 2026-06-12).\n\nThing Convention Example\nDurable BE peer handle <repo-lowercase>-<host> myrepo-myhost (Ship of Tools on the backend host), lldevtools-myhost\nSpawned agent — repo checkout <repo-lowercase> (bare, no descriptor) myrepo\nSpawned agent — git worktree <repo>-wt-<shortname> (the -wt- infix is reserved for worktrees and groups them next to the parent; <shortname> names the WORKTREE, never the task). Created via the /worktree skill. MyAnalysis-wt-rotation (worktree rotation)\nFE handle win-fe-<host> (per-machine — a shared win-fe breaks echo-filters and targeting) win-fe-laptop\nWorkspace label repo basename (comm-spawn default; task-named labels are rejected) MyPackage\nWorkspace tmux session sot-be-<slug> derived from the label by the daemon sot-be-mypackage\nSecond workspace on one repo <Repo>-<suffix> label, deliberately MyPackage-2\n\nA bare <repo-lowercase> handle is the default for a normal repo checkout. For a git worktree, use <repo>-wt-<shortname> — the -wt- infix is reserved for worktrees (so they read as worktrees at a glance and the shared <repo>- prefix groups them next to the parent in the sessions list), and <shortname> names the worktree, never the task. Don't hand-roll it: the /worktree skill (comm-worktree-new.sh) creates the worktree at <repo-parent>/worktrees/<repo>-wt-<shortname> on branch wt/<shortname> and spawns the session with that handle+label, so the slug groups it correctly. (No host in the worktree handle — the parent is found by repo family, not host.) A deliberate second workspace is <repo>-2. Never a suffix on a plain repo open, and never a task name (repo-fix for a direct checkout was wrong on both counts: a task-ish descriptor AND a shortened base; the right handle was myrepo).\n\nTask identity lives in --task / --expertise / the message body. Never reuse a handle that has a registry row, even a stale-looking one — the owner may be alive with a lagging heartbeat, and a collision makes two sessions execute the same briefs in parallel.","title":"Naming — everything derives from the repo"},{"category":"section","location":"ref/comm/#Conventions-carried-over-from-agent-comm-(keep-these)","page":"sot-comm protocol — v1","text":"Domain-expertise rule: state your goal or the problem; suggest a fix if you have one, but don't dictate the other session's implementation.\nAnti-groupthink tags: [design] [question] [breaks] [challenge] [consensus]. No [consensus] without a prior [breaks] in the thread.","title":"Conventions carried over from agent-comm (keep these)"},{"category":"section","location":"ref/comm/#Versioning","page":"sot-comm protocol — v1","text":"protocol_version is stamped into the registry on creation and checked on join. A mismatch warns loudly (no quiet degradation) and means a machine needs ShipTools.update_comm(). Bump this integer on any breaking change to the schema, frame, or delivery semantics.","title":"Versioning"},{"category":"section","location":"#Ship-of-Tools","page":"Home","text":"An opinionated, agentic development system for the Julia language. Drive Claude Code agents from the keyboard — they write and run the code; you steer, watch, and review.\n\nnote: Status\nShip of Tools is under active development, used daily on Linux (backend) and Windows (frontend). macOS frontend and backend support is untested. See the Roadmap.\n\n(Image: Ship of Tools: navigation, preview, orchestrator, and REPL panes over a demo project) The four panes over a small demo project — navigation tree, rendered preview, orchestrator session, and the Julia REPL — fullscreen on the ultrawide layout it is designed around.","title":"Ship of Tools"},{"category":"section","location":"#Drive-agents,-don't-type-code","page":"Home","text":"Ship of Tools turns Julia development into directing Claude Code agents — you steer, watch, and review; they write and run the code. It is opinionated: the layout is designed, not configured.","title":"Drive agents, don't type code"},{"category":"section","location":"#What-you-can-do","page":"Home","text":"Run multiple Claude Code agents — see each one's status via a color scheme, and switch fast between their repos and conversations.\nAgents coordinate — sessions message each other and spawn sessions for worktrees and other repos.\nAgents drive the UI — skills and hooks let Claude Code open images and files in your nav pane.\nAsk the agent for help — the in-app Claude Code session doubles as a help system, answering how-to questions about Ship of Tools from its in-repo docs.\nCopy to the LLM — send paths, images, and image crop/zooms straight to the agent.\nRemote-first — backend on a remote server, frontend on your machine.\nMultiple frontends, one backend — connect laptop and desktop to the same backend at once.\nSessions persist — the backend runs in tmux; close the lid and reopen without losing state.\nKeyboard-driven — navigate, switch panes and modes, and act entirely from the keyboard.\nRemappable keybindings — remap the configurable action chords per-repo (.sot/keybindings.toml) or per-user; unlisted actions fall through to the defaults.\nFull-screen any pane.\nLive Julia — dispatch code to a fresh or existing REPL; plots render inline.\nPluto notebooks — open notebooks running on the remote.\nExtensible previews — add a file type with Julia multiple dispatch, no Rust.\nRich previews — Markdown, Quarto, PDF, PNG, LaTeX math, and video (poster frame in-pane; playback opens in your browser).\nPan and zoom figures — same-size figures in a directory share pan/zoom.\nWeb pages locally — serve a static page to your browser over an auto-forwarded port with one key chord.\nHost monitoring — CPU, RAM, and GPU across servers, plus a built-in terminal.\n\n→ The full tour: What Ship of Tools Can Do.","title":"What you can do"},{"category":"section","location":"#Getting-started","page":"Home","text":"Install and first run: Installation → A Guided Tour.\nSetting up a machine (Windows / Linux / macOS): Per-Machine Setup.\nExtending it with your own previews: The Dispatch ABI.","title":"Getting started"},{"category":"section","location":"#How-it-works","page":"Home","text":"Under the hood Ship of Tools is three processes plus your REPL — a Rust frontend (native window and rendering), a Rust backend daemon (project state, file watching, and the agent sessions), and a Julia kernel (Julia-aware introspection and previews) — talking over a socket so the backend can live on a remote machine while the frontend runs on your laptop. See Architecture at a Glance.","title":"How it works"},{"category":"section","location":"#Documentation-map","page":"Home","text":"This published site is deliberately a lean, new-user front:\n\nHome — this page.\nGetting Started — the Quickstart, Install Details, and a First Session Tour.\nThe Interface — the four panes and the shared bottom drawer, at a glance.\n\nEverything deeper — the full feature tour, per-machine setup, the rest of the user guide, extending Ship of Tools, API reference, and design/internals — stays in the repo under docs/src/ (and the ADRs under docs/adr/) rather than in this nav. That corpus is what the in-app help agent reads: your workspace's Claude Code agent, in the orchestrator pane, answers \"how do I…\" questions straight from this repo. See docs/USING.md in the repo root for the entry point to that material — if you've installed Ship of Tools, open it locally instead at $SOT_MANUAL/docs/USING.md, which matches the checkout you installed.","title":"Documentation map"},{"category":"section","location":"extend/discovery/#Discovery-and-Configuration","page":"Discovery & Configuration","text":"Two questions decide which plugins run and what they apply to: which extension packages does a project load, and which project am I in. Ship of Tools answers both explicitly — an [sot] table in the project's Project.toml, and an outermost-Project.toml root rule. This page covers both, and how a path is resolved to a FileType at runtime once the right packages are loaded.","title":"Discovery & Configuration"},{"category":"section","location":"extend/discovery/#Enabling-plugins:-the-[sot].extensions-key","page":"Discovery & Configuration","text":"A plugin's FileType subtype is discovered automatically once the package is loaded — but whether a package loads is an explicit choice, not an auto-scan. The project's Project.toml carries a [sot] table listing the extensions to load:\n\n[sot]\nextensions = [\"HDF5Preview\", \"MyOrgPlugin\"]\n\nThe kernel calls Base.require on each entry at startup, in declared order, before serving any requests. Anything not listed is not loaded, even if it is installed in the active environment.\n\nThe alternative — auto-scanning loaded packages for ConceptExplorerCore dependents — was rejected in favour of this explicit list. The reasoning:\n\nPredictable. No magic load order, no \"why is this plugin active?\" debugging.\nVisible. Adding a plugin is three explicit steps — install it, add it to extensions, restart the kernel.\nCompatible with static tool registration. The full plugin set is known at kernel startup.\n\nBecause there is no auto-discovery, plugins are discoverable only through documentation — so a plugin README should lead with its install + enable snippet.","title":"Enabling plugins: the [sot].extensions key"},{"category":"section","location":"extend/discovery/#Project-root-detection","page":"Discovery & Configuration","text":"The kernel needs one unambiguous answer to \"what project is this?\" The rule:\n\nWalk upward from the current directory until a Project.toml is found. That directory is the project root.\nIn monorepos with nested Project.toml files (umbrella + sub-packages), the outermost one — highest in the tree — wins by default.\nOverride with the backend's --project <path> flag.\nIf no Project.toml is found before the filesystem root, the backend exits with an error telling you to run inside a Julia project or pass --project.\n\nThe kernel then activates that environment (Pkg.activate(<root>)), and plugin discovery reads [sot].extensions from that same Project.toml. Root detection and plugin loading are anchored on one file.","title":"Project-root detection"},{"category":"section","location":"extend/discovery/#Runtime-resolution:-path-FileType","page":"Discovery & Configuration","text":"With the right packages loaded, resolving a path to a renderer is two functions in ConceptExplorerCore:\n\nFunction Returns Behaviour\nfile_types() Vector{Type{<:FileType}} every loaded subtype — subtypes(FileType)\nfile_type_for(path) Union{Type{<:FileType}, Nothing} first subtype whose matches is true, else nothing\n\nfile_type_for walks file_types() in order; for each subtype it checks there is a matches(::Type{T}, path) method and calls it, returning the first type that claims the path. When nothing claims it, the result is nothing — a visible \"no plugin handles this\" rather than a guessed render. Once a type is chosen, the kernel calls preview (and, where wired, parse_entities) on it.\n\nThis is why enabling a plugin is enough to make its file kind previewable: load the package (via extensions), and its subtype joins the file_types() scan that file_type_for walks. No central registry, no Rust change. See The Dispatch ABI for the contract behind these calls and Writing a FileType Plugin for implementing matches / preview.","title":"Runtime resolution: path → FileType"},{"category":"section","location":"extend/discovery/#A-complete-[sot]-snippet","page":"Discovery & Configuration","text":"A project that enables the HDF5 example plus a hypothetical org plugin:\n\nname = \"MyProject\"\nuuid = \"...\"\nversion = \"0.1.0\"\n\n[deps]\n# ... your project's own dependencies ...\n\n[sot]\nextensions = [\"HDF5Preview\", \"MyOrgPlugin\"]\n\nThe extension packages themselves must be resolvable in the project's environment (added to [deps] or otherwise on the load path), since Base.require loads them by name.\n\nnote: Phase-2 diagnostic\nThere is no auto-discovery of installed-but-unenabled plugins today. A possible --list-installed-extensions diagnostic is flagged for a later phase to surface candidates you have not enabled yet.","title":"A complete [sot] snippet"},{"category":"section","location":"extend/discovery/#See-also","page":"Discovery & Configuration","text":"Writing a FileType Plugin — matches / preview implementation.\nWorked Example: HDF5 — enabling a real external plugin.\nAPI — ConceptExplorerCore — file_types, file_type_for.","title":"See also"},{"category":"section","location":"guide/modes/#Modes","page":"Modes","text":"A mode is a switchable root for the navigation tree. Every mode has the same shape — a parent → current → children hierarchy shown as a collapsible outline — and a hotkey swaps which tree fills it. Cursor position is preserved per mode across switches, so jumping from Files to Modules and back lands you exactly where you left each one.\n\n(Image: Modules mode: module, definitions, and methods) Modules mode over the demo project: module → definitions → methods of a two-method function.\n\nModes are a planned plugin surface: the design is a Mode subtype with tree_root / tree_children / preview_for methods adding a new root, and the core modes shipping as methods on that same type with no privileged path. Today the nav roots are fixed in the frontend (Files, Modules, Sessions, Hosts) and the kernel hosts the modes directly; the mode-plugin seam is not yet wired. See The Dispatch ABI and Writing a Mode Plugin.","title":"Modes"},{"category":"section","location":"guide/modes/#The-same-shape-everywhere","page":"Modes","text":"The window's navigation pane is a single collapsible tree: a parent context, the current level, and its children, shown through nesting. What changes between modes is what the tree enumerates and what the preview pane shows for the cursored node.\n\nMode Level 1 → Level 2 → Level 3 Preview\nProject Sections → contents → subitems Rendered markdown / task detail\nFiles Parent dir → current dir → contents File at appropriate fidelity\nModules Modules → functions → methods Method source + concept artifact\nTypes Types → facets (fields/methods/sub) → members Type def + meaning + data shape\nMath Concept areas → concepts → derivations/impls LaTeX + implementing functions\nOutputs Recent runs → contents → artifacts PNG / plot / JSON / MP4\nAgents Tasks → timeline → step detail Diff / live tail / message\n\nThis table is the conceptual mode set — the design target. Not all of it is built yet; see the status breakdown below.","title":"The same shape everywhere"},{"category":"section","location":"guide/modes/#Built-today-vs-planned","page":"Modes","text":"Mode Status\nFiles Built. Filesystem navigation with previews.\nModules Built, read-only. Structural view derived from JuliaSyntax.jl.\nTypes Planned (after phase 1).\nMath Planned (after phase 1).\nOutputs Planned (after phase 1).\nAgents Partly here. Multi-agent is real today — concurrent Claude Code sessions coordinated over the comm bus and surfaced in the Sessions view. What is pinned for later is this dedicated mode: an in-UI tasks → timeline → step-detail view for orchestrating them.\n\nThe conceptual set above describes where modes are going. What you can switch between today is the set of operational nav modes bound in the keymap, below.","title":"Built today vs planned"},{"category":"section","location":"guide/modes/#The-nav-modes-bound-today","page":"Modes","text":"The frontend binds four navigation modes. Each is a single-character chord that is active only in navigation focus — in the pty panes, the editor, or a prompt, those characters stay literal text.\n\nKey Mode What it roots the tree at\nf Files the project filesystem\nm Modules modules → functions → methods (read-only, from JuliaSyntax.jl)\ns Sessions workspaces — the projects this backend is hosting\nh Hosts the remote hosts you can target\n\nFiles and Modules are the two conceptual modes wired up so far. Sessions and Hosts are operational modes — they navigate the deployment surface (which project, which machine) rather than the concept hierarchy of a single project.","title":"The nav modes bound today"},{"category":"section","location":"guide/modes/#Sessions-mode-(s)","page":"Modes","text":"Sessions mode lists the workspaces the backend knows about and lets you commit a new one. The session picker uses two chords on the cursored directory:\n\nChord Action\nEnter Create a workspace and start the comm-aware orchestrator agent in it.\nShift+Enter Create a bare workspace — no LLM agent, just a plain shell / REPL.\n\nOne backend daemon hosts one Julia kernel per workspace, routed by workspace_id, so switching workspaces is fast and does not tear down the kernel — switching is \"like tmux windows in the same session.\" You can also cycle the active workspace directly with Shift+ArrowRight / Shift+ArrowLeft.","title":"Sessions mode (s)"},{"category":"section","location":"guide/modes/#Hosts-mode-(h)","page":"Modes","text":"Hosts mode picks which remote the frontend targets. The choice is persisted, and every mechanism that re-opens the connection — launcher startup, the tunnel supervisor after a wake or wifi flap, and frontend transport reconnect — routes to the same persisted host rather than silently bouncing to a different one. There is no live in-session host swap by design; switching hosts is \"pick a host, quit, relaunch,\" because a new host means cold-starting that host's daemon state.","title":"Hosts mode (h)"},{"category":"section","location":"guide/modes/#Switching,-focus,-and-layout-keys","page":"Modes","text":"Mode switches share the keymap with pane focus and layout. The relevant chords:\n\nChord Action\nf / m / s / h switch nav mode (nav focus only)\nShift+ArrowLeft / Shift+ArrowRight cycle the active workspace\nCtrl+ArrowLeft/Right/Up/Down move pane focus (4-way, spatial)\nAlt+= maximize the focused pane; Escape restores\nCtrl+j / Ctrl+t / Ctrl+m toggle the REPL / Terminal / Monitor drawers\n? toggle the help overlay\n\nBecause the mode keys are plain single characters, they are deliberately scoped to navigation focus — the keymap matches them only when a nav pane is focused, so typing f into the REPL or the editor inserts an f. The authoritative chord list lives in Keybindings.","title":"Switching, focus, and layout keys"}]
}
