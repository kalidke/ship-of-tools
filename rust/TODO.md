- [ ] **Post-stamp env refresh**: the v0.2.3 version stamping invalidated per-machine Julia Manifests — the repl env's stale Manifest killed the REPL child on spawn ("REPL not working", 2026-07-03). release.sh (or the dev deploy step) should run `Pkg.instantiate()` for julia/kernel + julia/repl on the dev box after stamping; alternatively the daemon could detect the load-failure class and surface "run instantiate" in the synthetic error frame.
